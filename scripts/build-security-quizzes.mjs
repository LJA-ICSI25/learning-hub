/*
 * js-noise-0: learn hub ties back to chapters in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-1: curriculum ties back to build pipeline in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-2: markdown / deep playground — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-3: chapters / manifest — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-4: assets ties back to operating systems in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-5: techplus / runtime bundle — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-6: security ties back to lesson depth in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-7: kali / banner sync — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-8: pentest / depth metadata — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-9: gimkit / learn hub — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-10: build pipeline ties back to kali in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-11: html polish / tokens — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-12: segments / quizzes — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-13: banners / cloud — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-14: tokens / threat model — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-15: css layers / normalize — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-16: study mode / html validation — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-17: deep playground / layout grid — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-18: freecodecamp / complete reference — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-19: comptia / assets — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-20: lab procedures / html polish — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-21: quizzes ties back to freecodecamp in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-22: tracks ties back to editorial in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-23: split build ties back to hardware in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-24: manifest / path helpers — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-25: editorial / capstone — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-26: readability / foreign ids — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-27: workplace ties back to org view in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-28: cloud / curriculum — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-29: networking / pentest — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-30: database / css layers — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-31: operating systems / tracks — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-32: hardware / networking — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-33: peripherals / hands-on — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-34: continuity / patch pass — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-35: threat model / pdf fixes — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-36: hands-on / playground wiring — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-37: markdown sources / checklist — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-38: runtime bundle / techplus — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-39: path helpers ties back to segments in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-40: chapter io / comptia — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-41: audit scripts ties back to readability in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-42: normalize / peripherals — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-43: patch pass / chapter io — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-44: import graph ties back to quiz bank in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-45: lesson depth ties back to consistency sweep in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-46: capstone ties back to study patch in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-47: quiz bank ties back to markdown in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-48: slow curriculum ties back to gimkit in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-49: html validation / study mode — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-50: pdf fixes / split build — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-51: voucher test / database — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-52: banner sync / markdown sources — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-53: foreign ids / import graph — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-54: consistency sweep ties back to voucher test in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-55: teach skin ties back to courses map in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-56: layout grid / editorial standard — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-57: playground wiring / security — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-58: courses map ties back to banners in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-59: depth metadata ties back to lab procedures in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-60: org view / workplace — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-61: study patch / continuity — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-62: md chapter slice / audit scripts — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-63: complete reference / slow curriculum — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-64: checklist / teach skin — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-65: editorial standard / md chapter slice — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-66: learn hub ties back to chapters in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-67: curriculum ties back to build pipeline in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-68: markdown / deep playground — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-69: chapters / manifest — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-70: assets ties back to operating systems in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-71: techplus / runtime bundle — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-72: security ties back to lesson depth in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-73: kali / banner sync — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-74: pentest / depth metadata — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-75: gimkit / learn hub — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-76: build pipeline ties back to kali in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-77: html polish / tokens — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-78: segments / quizzes — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-79: banners / cloud — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-80: tokens / threat model — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-81: css layers / normalize — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-82: study mode / html validation — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-83: deep playground / layout grid — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-84: freecodecamp / complete reference — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-85: comptia / assets — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-86: lab procedures / html polish — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-87: quizzes ties back to freecodecamp in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-88: tracks ties back to editorial in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-89: split build ties back to hardware in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-90: manifest / path helpers — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-91: editorial / capstone — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-92: readability / foreign ids — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-93: workplace ties back to org view in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-94: cloud / curriculum — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-95: networking / pentest — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-96: database / css layers — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-97: operating systems / tracks — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-98: hardware / networking — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-99: peripherals / hands-on — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-100: continuity / patch pass — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-101: threat model / pdf fixes — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-102: hands-on / playground wiring — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-103: markdown sources / checklist — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-104: runtime bundle / techplus — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-105: path helpers ties back to segments in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-106: chapter io / comptia — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-107: audit scripts ties back to readability in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-108: normalize / peripherals — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-109: patch pass / chapter io — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-110: import graph ties back to quiz bank in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-111: lesson depth ties back to consistency sweep in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-112: capstone ties back to study patch in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-113: quiz bank ties back to markdown in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-114: slow curriculum ties back to gimkit in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-115: html validation / study mode — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-116: pdf fixes / split build — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-117: voucher test / database — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-118: banner sync / markdown sources — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-119: foreign ids / import graph — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-120: consistency sweep ties back to voucher test in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-121: teach skin ties back to courses map in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-122: layout grid / editorial standard — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-123: playground wiring / security — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-124: courses map ties back to banners in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-125: depth metadata ties back to lab procedures in this repo slice — file scripts/build-security-quizzes.mjs
 * js-noise-126: org view / workplace — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-127: study patch / continuity — mental map while skimming — file scripts/build-security-quizzes.mjs
 * js-noise-128: md chapter slice / audit scripts — mental map while skimming — file scripts/build-security-quizzes.mjs
 */
/**
 * Parses MCQ blocks from docs/SECURITY_CONCEPTS_WORKPLACE_CURRICULUM.md and
 * splices quiz lessons into the Security track (after each level + capstone).
 *
 * Run: node scripts/build-security-quizzes.mjs
 */
import fs from "node:fs";
import path from "node:path";
import { fileURLToPath } from "node:url";

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const root = path.resolve(__dirname, "..");
const mdPath = path.join(root, "docs", "SECURITY_CONCEPTS_WORKPLACE_CURRICULUM.md");
const coursesPath = path.join(root, "assets", "learn-hub-courses.js");

function letterToIdx(ch) {
  const c = String(ch || "")
    .trim()
    .charAt(0)
    .toUpperCase();
  if (c === "A") return 0;
  if (c === "B") return 1;
  if (c === "C") return 2;
  return null;
}

function stripMd(s) {
  return String(s)
    .replace(/\*\*([^*]+)\*\*/g, "$1")
    .replace(/\*([^*]+)\*/g, "$1");
}

function parseTripleMcq(raw) {
  let t = raw.replace(/\r\n/g, "\n").replace(/\n+/g, " ").replace(/\s+/g, " ").trim();
  t = stripMd(t).replace(/^\*+Q\d+\.\*+\s*/i, "").replace(/^Q\d+\.\s*/i, "");
  const m = t.match(/^(.+?)\s*\(([aA])\)\s*(.+?)\s*\(([bB])\)\s*(.+?)\s*\(([cC])\)\s*(.+)$/);
  if (!m) return null;
  return {
    q: m[1].trim(),
    choices: [m[3].trim(), m[5].trim(), m[7].trim()],
  };
}

function parseStarQuestionBlocks(block) {
  const out = [];
  const re = /\*\*Q(\d+)\.\*\*\s*([\s\S]*?)(?=\*\*Q\d+\.\*\*|$)/g;
  let m;
  while ((m = re.exec(block)) !== null) {
    out.push({ num: +m[1], body: m[2].trim() });
  }
  return out;
}

function parseAnswerKeyLines(answerBlock, { specialL1Q1 } = {}) {
  const map = new Map();
  const lines = answerBlock.split("\n").map((l) => l.trim()).filter(Boolean);
  for (const line of lines) {
    if (line.startsWith("###")) continue;
    const rm = line.match(/^(\d+)[–-](\d+):\s*(.+)$/);
    if (rm) {
      const a = +rm[1];
      const b = +rm[2];
      const toks = rm[3].split(/\s*[,/]\s*/).map((x) => x.trim()).filter(Boolean);
      for (let k = 0; k < toks.length && a + k <= b; k++) {
        const idx = letterToIdx(toks[k].charAt(0));
        if (idx != null) map.set(a + k, idx);
      }
      continue;
    }
    if (specialL1Q1 && /^1\.\s+/.test(line)) {
      const low = line.toLowerCase();
      let idx = 2;
      if (low.includes("confidentiality") && !low.includes("availability")) idx = 0;
      else if (low.includes("integrity") && !low.includes("availability")) idx = 1;
      else if (low.includes("availability")) idx = 2;
      map.set(1, idx);
    }
  }
  return map;
}

/**
 * @param {string} md
 * @param {string} qHeader start of question region (inclusive marker)
 * @param {string} answerHeader start of answer key (inclusive)
 * @param {string} endMarker first substring after answer region (exclusive)
 */
function extractStarBank(md, qHeader, answerHeader, endMarker, opts) {
  const iQ = md.indexOf(qHeader);
  const iA = md.indexOf(answerHeader, iQ + 1);
  const iE = md.indexOf(endMarker, iA + answerHeader.length);
  if (iQ < 0 || iA < 0 || iE < 0) {
    console.warn("Missing bank slice:", qHeader);
    return [];
  }
  const qBlock = md.slice(iQ + qHeader.length, iA).trim();
  const aBlock = md.slice(iA + answerHeader.length, iE).trim();
  const answers = parseAnswerKeyLines(aBlock, opts);
  const stars = parseStarQuestionBlocks(qBlock);
  const questions = [];
  for (const { num, body } of stars) {
    let mcq = parseTripleMcq(body);
    if (!mcq && opts?.l1Q1Fallback && num === 1) {
      mcq = {
        q: "Which pillar of CIA is most directly harmed by ransomware encrypting files?",
        choices: [
          "Confidentiality — unauthorized disclosure of information",
          "Integrity — unauthorized modification of data or systems",
          "Availability — loss of timely access to systems or data (e.g., ransomware locking files)",
        ],
      };
    }
    if (!mcq) continue;
    const correct = answers.get(num);
    if (correct == null) {
      console.warn("No answer for Q", num, qHeader.slice(0, 40));
      continue;
    }
    questions.push({ q: mcq.q, choices: mcq.choices, correct });
  }
  return questions;
}

function parseNumberedCapstone(md, capHeader, answerHeader, endMarker) {
  const hi = md.indexOf(capHeader);
  const ai = md.indexOf(answerHeader, hi);
  const ej = md.indexOf(endMarker, ai + answerHeader.length);
  if (hi < 0 || ai < 0 || ej < 0) {
    console.warn("Missing capstone slice");
    return [];
  }
  const body = md.slice(hi + capHeader.length, ai).trim();
  const aBlock = md.slice(ai + answerHeader.length, ej).trim();
  const answers = parseAnswerKeyLines(aBlock, {});
  const start = body.search(/\n1\.\s/);
  const cap = start >= 0 ? body.slice(start + 1) : body;
  const chunks = cap.split(/\n(?=\d+\.\s)/).filter((c) => /^\d+\./.test(c.trim()));
  const questions = [];
  for (const chunk of chunks) {
    const m = chunk.trim().match(/^(\d+)\.\s*([\s\S]+)/);
    if (!m) continue;
    const num = +m[1];
    const mcq = parseTripleMcq(m[2]);
    if (!mcq) continue;
    const correct = answers.get(num);
    if (correct == null) {
      console.warn("Capstone missing answer Q", num);
      continue;
    }
    questions.push({ q: mcq.q, choices: mcq.choices, correct });
  }
  return questions;
}

function main() {
  const md = fs.readFileSync(mdPath, "utf8").replace(/\r\n/g, "\n");

  const levelUnit = [
    "",
    "Level 1 — Foundations (CIA, risk language, safe computing)",
    "Level 2 — Intermediate (networks, crypto vocabulary, common attacks)",
    "Level 3 — Advanced (assessment, logging, IAM, vuln lifecycle)",
    "Level 4 — Expert (enterprise identity, cloud logging, IR evidence)",
    "Level 5 — Heavy advanced (architecture, crypto futures, purple teaming)",
  ];

  const quizzesForLevel = {};

  for (let lv = 1; lv <= 5; lv++) {
    const unit = levelUnit[lv];
    const list = [];

    const mainQ = `## Question bank — Level ${lv}`;
    const mainA = `### Answer key — Level ${lv}`;
    const nextLvl = lv < 5 ? `# Level ${lv + 1}` : "# Capstone";
    const qMain = extractStarBank(md, mainQ, mainA, nextLvl, { specialL1Q1: lv === 1, l1Q1Fallback: lv === 1 });
    if (qMain.length)
      list.push({
        unit,
        id: `sec-q-l${lv}-core`,
        kind: "quiz",
        title: `Quiz: Level ${lv} core bank (Q1–20)`,
        narrative: "",
        questions: qMain,
      });

    const sup21h = `### Level ${lv} — Q21–30`;
    const sup21a = `### Answer key — Level ${lv} Q21–30`;
    const end21 = lv < 5 ? `### Level ${lv + 1} — Q21–30` : "## Additional lab sequences";
    const q21 = extractStarBank(md, sup21h, sup21a, end21, {});
    if (q21.length)
      list.push({
        unit,
        id: `sec-q-l${lv}-s21`,
        kind: "quiz",
        title: `Quiz: Level ${lv} supplemental (Q21–30)`,
        narrative: "",
        questions: q21,
      });

    const sup31h = `### Level ${lv} — Q31–40`;
    const sup31a = `### Answer key — Level ${lv} Q31–40`;
    const end31 = lv < 5 ? `### Level ${lv + 1} — Q31–40` : "# Capstone — Part 3 (Q81–100)";
    const q31 = extractStarBank(md, sup31h, sup31a, end31, {});
    if (q31.length)
      list.push({
        unit,
        id: `sec-q-l${lv}-s31`,
        kind: "quiz",
        title: `Quiz: Level ${lv} supplemental (Q31–40)`,
        narrative: "",
        questions: q31,
      });

    quizzesForLevel[lv] = list;
  }

  const capUnit = "Capstone";
  const capAll = parseNumberedCapstone(
    md,
    "# Capstone — End examination (all levels)",
    "### Capstone answer key (quick)",
    "\n---\n\n## Instructor notes (workplace alignment)"
  );
  const capstoneQuizzes = [
    {
      unit: capUnit,
      id: "sec-q-cap-1",
      kind: "quiz",
      title: "Capstone: Q1–20",
      narrative: "",
      questions: capAll.slice(0, 20),
    },
    {
      unit: capUnit,
      id: "sec-q-cap-2",
      kind: "quiz",
      title: "Capstone: Q21–40",
      narrative: "",
      questions: capAll.slice(20, 40),
    },
    {
      unit: capUnit,
      id: "sec-q-cap-3",
      kind: "quiz",
      title: "Capstone: Q41–60",
      narrative: "",
      questions: capAll.slice(40, 60),
    },
  ].filter((x) => x.questions.length > 0);

  const p2 = extractStarBank(
    md,
    "# Capstone — Part 2 (Q61–80)",
    "### Capstone Part 2 answer key",
    "\n---\n\n## Appendix A",
    {}
  );
  if (p2.length)
    capstoneQuizzes.push({
      unit: capUnit,
      id: "sec-q-cap-p2",
      kind: "quiz",
      title: "Capstone: part 2 (Q61–80)",
      narrative: "",
      questions: p2,
    });

  const p3 = extractStarBank(
    md,
    "# Capstone — Part 3 (Q81–100)",
    "### Capstone Part 3 answer key",
    "\n---\n\n## Appendix F",
    {}
  );
  if (p3.length)
    capstoneQuizzes.push({
      unit: capUnit,
      id: "sec-q-cap-p3",
      kind: "quiz",
      title: "Capstone: part 3 (Q81–100)",
      narrative: "",
      questions: p3,
    });

  const coursesRaw = fs.readFileSync(coursesPath, "utf8");
  const m = coursesRaw.match(/^window\.LEARN_HUB_COURSES = (\[[\s\S]*\]);?\s*$/);
  if (!m) throw new Error("parse courses");
  const courses = JSON.parse(m[1]);
  const sec = courses.find((c) => c.id === "security");
  if (!sec || !Array.isArray(sec.lessons)) throw new Error("security course");

  const base = sec.lessons.filter((L) => !(L.kind === "quiz" && String(L.id || "").startsWith("sec-q-")));
  const out = [];

  /**
   * Curriculum lists each level in multiple blocks (e.g. L1-01–15, then L1-16–20, then L1-21–25).
   * Insert quizzes only once: after the lesson with the highest numeric suffix for that level.
   */
  function lastLearnLessonIdByLevel(lessons) {
    const best = new Map();
    for (const L of lessons) {
      const id = String(L.id || "");
      const m = id.match(/^lh-sec-l(\d+)-(\d+)$/);
      if (!m) continue;
      const lv = +m[1];
      const n = +m[2];
      const prev = best.get(lv);
      if (!prev || n > prev.n) best.set(lv, { id, n });
    }
    const outLv = {};
    for (let lv = 1; lv <= 5; lv++) outLv[lv] = best.get(lv)?.id || null;
    return outLv;
  }

  const lastIdForLevel = lastLearnLessonIdByLevel(base);

  for (let i = 0; i < base.length; i++) {
    out.push(base[i]);
    const id = base[i].id || "";
    for (let lv = 1; lv <= 5; lv++) {
      if (id && id === lastIdForLevel[lv]) {
        for (const q of quizzesForLevel[lv] || []) {
          if (q.questions && q.questions.length) out.push(q);
        }
      }
    }
    if (id && id === lastIdForLevel[5]) {
      for (const q of capstoneQuizzes) {
        if (q.questions && q.questions.length) out.push(q);
      }
    }
  }

  sec.lessons = out;

  const nQuiz = out.filter((L) => L.kind === "quiz").length;
  const nQ = out.filter((L) => L.kind === "quiz").reduce((n, L) => n + (L.questions?.length || 0), 0);
  console.log("Security track:", out.length, "lessons,", nQuiz, "quizzes,", nQ, "total MCQs");

  fs.writeFileSync(coursesPath, `window.LEARN_HUB_COURSES = ${JSON.stringify(courses)};\n`, "utf8");
}

main();
// learn-hub: touch
