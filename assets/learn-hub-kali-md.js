/*
 * js-noise-0: learn hub ties back to chapters in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-1: curriculum ties back to build pipeline in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-2: markdown ties back to deep playground in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-3: chapters ties back to manifest in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-4: assets / operating systems — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-5: techplus ties back to runtime bundle in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-6: security / lesson depth — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-7: kali ties back to banner sync in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-8: pentest / depth metadata — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-9: gimkit / learn hub — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-10: build pipeline ties back to kali in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-11: html polish / tokens — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-12: segments / quizzes — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-13: banners / cloud — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-14: tokens / threat model — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-15: css layers / normalize — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-16: study mode / html validation — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-17: deep playground ties back to layout grid in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-18: freecodecamp ties back to complete reference in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-19: comptia / assets — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-20: lab procedures / html polish — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-21: quizzes / freecodecamp — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-22: tracks / editorial — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-23: split build ties back to hardware in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-24: manifest / path helpers — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-25: editorial ties back to capstone in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-26: readability / foreign ids — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-27: workplace / org view — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-28: cloud / curriculum — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-29: networking / pentest — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-30: database ties back to css layers in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-31: operating systems / tracks — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-32: hardware / networking — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-33: peripherals ties back to hands-on in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-34: continuity / patch pass — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-35: threat model / pdf fixes — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-36: hands-on ties back to playground wiring in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-37: markdown sources ties back to checklist in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-38: runtime bundle / techplus — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-39: path helpers / segments — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-40: chapter io ties back to comptia in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-41: audit scripts ties back to readability in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-42: normalize / peripherals — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-43: patch pass / chapter io — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-44: import graph / quiz bank — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-45: lesson depth ties back to consistency sweep in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-46: capstone / study patch — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-47: quiz bank / markdown — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-48: slow curriculum ties back to gimkit in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-49: html validation / study mode — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-50: pdf fixes / split build — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-51: voucher test / database — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-52: banner sync / markdown sources — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-53: foreign ids / import graph — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-54: consistency sweep / voucher test — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-55: teach skin ties back to courses map in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-56: layout grid / editorial standard — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-57: playground wiring / security — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-58: courses map / banners — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-59: depth metadata / lab procedures — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-60: org view / workplace — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-61: study patch / continuity — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-62: md chapter slice ties back to audit scripts in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-63: complete reference / slow curriculum — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-64: checklist / teach skin — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-65: editorial standard ties back to md chapter slice in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-66: learn hub ties back to chapters in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-67: curriculum ties back to build pipeline in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-68: markdown ties back to deep playground in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-69: chapters ties back to manifest in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-70: assets / operating systems — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-71: techplus ties back to runtime bundle in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-72: security / lesson depth — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-73: kali ties back to banner sync in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-74: pentest / depth metadata — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-75: gimkit / learn hub — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-76: build pipeline ties back to kali in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-77: html polish / tokens — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-78: segments / quizzes — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-79: banners / cloud — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-80: tokens / threat model — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-81: css layers / normalize — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-82: study mode / html validation — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-83: deep playground ties back to layout grid in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-84: freecodecamp ties back to complete reference in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-85: comptia / assets — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-86: lab procedures / html polish — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-87: quizzes / freecodecamp — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-88: tracks / editorial — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-89: split build ties back to hardware in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-90: manifest / path helpers — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-91: editorial ties back to capstone in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-92: readability / foreign ids — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-93: workplace / org view — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-94: cloud / curriculum — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-95: networking / pentest — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-96: database ties back to css layers in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-97: operating systems / tracks — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-98: hardware / networking — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-99: peripherals ties back to hands-on in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-100: continuity / patch pass — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-101: threat model / pdf fixes — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-102: hands-on ties back to playground wiring in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-103: markdown sources ties back to checklist in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-104: runtime bundle / techplus — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-105: path helpers / segments — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-106: chapter io ties back to comptia in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-107: audit scripts ties back to readability in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-108: normalize / peripherals — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-109: patch pass / chapter io — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-110: import graph / quiz bank — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-111: lesson depth ties back to consistency sweep in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-112: capstone / study patch — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-113: quiz bank / markdown — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-114: slow curriculum ties back to gimkit in this repo slice — file assets/learn-hub-kali-md.js
 * js-noise-115: html validation / study mode — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-116: pdf fixes / split build — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-117: voucher test / database — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-118: banner sync / markdown sources — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-119: foreign ids / import graph — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-120: consistency sweep / voucher test — mental map while skimming — file assets/learn-hub-kali-md.js
 * js-noise-121: teach skin ties back to courses map in this repo slice — file assets/learn-hub-kali-md.js
 */
/* Auto-generated by scripts/build-kali-from-markdown.mjs — edit docs/KALI_HANDS_ON_LAB_CURRICULUM.md (or npm run emit:kali-slow) then re-run */
(function () {
  var C = {"labs-kali-intro":"<p class=\"lh-tg-source\"><strong>Kali Linux Hands-On Lab Curriculum</strong> · <span class=\"lh-tg-chapter\">Start — how to use this curriculum</span> · Lesson <code>labs-kali-intro</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h1>Kali Linux Hands-On Lab Curriculum</h1>\n<p><strong>Audience:</strong> Learners who want <strong>deep</strong>, practical experience across web fundamentals, scripting, databases, and certification-aligned security topics.</p>\n<p><strong>Pedagogy (read once):</strong> This path is intentionally <strong>slow</strong>. Each sidebar lesson is <strong>one lab</strong> (or one setup chapter). Do <strong>not</strong> rush. For each lab: read the <strong>Background</strong> and <strong>Commands explained</strong> sections <em>before</em> typing anything. Expect <strong>45–120+ minutes</strong> per lab depending on depth—some may spill across two sessions. Keep a <strong>lab notebook</strong> (what you ran, what happened, what confused you, what you verified).</p>\n<p><strong>Environment:</strong> Every lab assumes a <strong>Kali Linux virtual machine</strong> (VMware, VirtualBox, or Hyper-V). Take a <strong>VM snapshot</strong> before labs that change services, firewalls, or SSH—especially in the Security+ section.</p>\n<p><strong>Conventions:</strong></p>\n<ul>\n<li><code>$</code> in prose means “normal user shell”; use <code>sudo</code> only when the lab says to.</li>\n<li>Replace placeholders (<code>yourname</code>, IPs, hostnames) with your own values.</li>\n<li>After each lab, write a short <strong>lab report</strong>: objective, commands run (or key outputs), verification evidence, one <strong>“what I’d automate next time”</strong> note.</li>\n</ul>\n<hr>\n</div>","labs-kali-setup":"<p class=\"lh-tg-source\"><strong>Why this block matters</strong> · <span class=\"lh-tg-chapter\">Start — prepare your VM once</span> · Lesson <code>labs-kali-setup</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h2>Global setup (do once per VM)</h2>\n<p><strong>Suggested pace:</strong> 90–150 minutes (includes downloads and reboot if needed). You may split across two sessions after a clean snapshot.</p>\n<p><strong>Outcomes:</strong> A Kali VM with updated packages, a personal lab workspace, <strong>Apache2</strong> serving <code>/var/www/html</code>, <strong>MariaDB</strong> installed and hardened with <code>mysql_secure_installation</code>, and <strong>Python 3</strong> with venv/pip ready.</p>\n<h3>Why this block matters</h3>\n<p>You will reuse Apache and MariaDB in many later labs. Installing them once—<strong>carefully</strong>—saves repeated context switching and avoids subtle “it worked yesterday” configuration drift.</p>\n<h3>1. Update package index and installed packages</h3>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo apt update &amp;&amp; sudo apt full-upgrade -y\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> <code>apt update</code> refreshes the local <strong>package index</strong> (metadata about what versions exist on your configured mirrors). <code>apt full-upgrade</code> installs newer versions and may remove obsolete packages to satisfy dependencies.</li>\n<li><strong>Why we use it here:</strong> Kali is a rolling distribution; starting from a known-good updated state reduces “mystery errors” caused by stale libraries or half-installed security updates.</li>\n</ul>\n<p><strong>Procedure:</strong> Run the command once; wait for it to finish. If the kernel was upgraded, <strong>reboot</strong> when prompted (<code>sudo reboot</code>) and continue after the VM is back.</p>\n<h3>2. Create a workspace tree</h3>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">mkdir -p ~/labs/{html,css,js,python,sql,securityplus,techplus}\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> <code>mkdir -p</code> creates directories and <strong>does not error</strong> if they already exist. Braces expand to multiple folder names under <code>~/labs/</code>.</li>\n<li><strong>Why we use it here:</strong> Keeps your experiments out of <code>/root</code> and out of package-managed paths. You can back up <code>~/labs</code> as a single unit.</li>\n</ul>\n<h3>3. Install and enable Apache2</h3>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo apt install -y apache2\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> <code>apt install</code> downloads and installs the <strong>apache2</strong> package (the HTTP server). <code>-y</code> auto-confirms prompts.</li>\n<li><strong>Why we use it here:</strong> Apache will serve your HTML/CSS/JS labs from <code>/var/www/html</code> by default on Debian-based systems.</li>\n</ul>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo systemctl enable apache2\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> <code>systemctl enable</code> registers the service so it <strong>starts on boot</strong>.</li>\n<li><strong>Why we use it here:</strong> After reboots, your web root is still served without remembering to start Apache manually.</li>\n</ul>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo systemctl start apache2\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> <code>systemctl start</code> runs the service <strong>now</strong>.</li>\n<li><strong>Why we use it here:</strong> Enabling does not always start immediately on all images; starting explicitly confirms the daemon is up.</li>\n</ul>\n<p><strong>Verify:</strong> <code>curl -I http://127.0.0.1/</code> should return HTTP <code>200</code> or <code>403</code> (depending on default index). <code>systemctl status apache2</code> should show <strong>active (running)</strong>.</p>\n<h3>4. Install MariaDB (MySQL-compatible server)</h3>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo apt install -y mariadb-server mariadb-client\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Installs the <strong>database server</strong> (<code>mariadb-server</code>) and <strong>client tools</strong> (<code>mariadb-client</code>) used to connect and administer databases.</li>\n<li><strong>Why we use it here:</strong> Kali commonly ships MariaDB; SQL labs assume this stack.</li>\n</ul>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo systemctl enable mariadb &amp;&amp; sudo systemctl start mariadb\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Same <strong>enable + start</strong> pattern as Apache for the database daemon.</li>\n<li><strong>Why we use it here:</strong> Ensures <code>mariadb</code> is running before you create databases.</li>\n</ul>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo mysql_secure_installation\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> An <strong>interactive hardening script</strong>: sets root auth plugin choices, can remove anonymous users, disallow remote root login, remove test database, reload privileges.</li>\n<li><strong>Why we use it here:</strong> Default installs are learner-friendly but not production-hardened; this closes common gaps <strong>on your lab VM</strong>.</li>\n</ul>\n<p>Follow the prompts conservatively for a <strong>lab VM</strong> (you still want local <code>sudo mariadb</code> admin access). Write down any <strong>non-default</strong> choices in your lab notes.</p>\n<h3>5. Python tooling</h3>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo apt install -y python3 python3-venv python3-pip\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Installs the interpreter (<code>python3</code>), the <strong>venv</strong> module for isolated environments, and <strong>pip</strong> for packages.</li>\n<li><strong>Why we use it here:</strong> Later Python labs assume you can create <code>.venv</code> and install packages without touching system Python.</li>\n</ul>\n<h3>6. Optional quality-of-life tools</h3>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo apt install -y git curl tmux\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Common utilities: <strong>git</strong> (version control), <strong>curl</strong> (HTTP from CLI), <strong>tmux</strong> (terminal multiplexing).</li>\n<li><strong>Why we use it here:</strong> Not strictly required for every lab but used constantly in real admin work.</li>\n</ul>\n<p><strong>Final verification checklist</strong></p>\n<ul>\n<li><input disabled=\"\" type=\"checkbox\"> <code>apache2</code> active; <code>http://127.0.0.1/</code> responds from the VM.</li>\n<li><input disabled=\"\" type=\"checkbox\"> <code>mariadb</code> active; <code>sudo mariadb -e &quot;SELECT 1&quot;</code> returns a row.</li>\n<li><input disabled=\"\" type=\"checkbox\"> <code>python3 --version</code> and <code>python3 -m venv -h</code> work.</li>\n<li><input disabled=\"\" type=\"checkbox\"> Snapshot taken (recommended) with a label like <strong>post-global-setup</strong>.</li>\n</ul>\n<hr>\n</div>","labs-kali-h-01":"<p class=\"lh-tg-source\"><strong>Lab H-01 — “Hello, Apache”</strong> · <span class=\"lh-tg-chapter\">Part 1 — HTML (Apache2 on Kali)</span> · Lesson <code>labs-kali-h-01</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab H-01 — “Hello, Apache”</h3>\n<p><strong>Suggested pace:</strong> 45–75 minutes</p>\n<p><strong>Goal:</strong> Prove you control what the browser loads from your VM’s HTTP server.</p>\n<h4>Why this lab exists</h4>\n<p>Before learning markup patterns, you should trust the path from <strong>file on disk → URL → browser</strong>. That path is easy to break with permissions, wrong paths, or wrong IP—this lab makes it concrete.</p>\n<h4>Background</h4>\n<p>On Debian-based systems (including Kali), <strong>Apache</strong> often uses <strong><code>/var/www/html</code></strong> as the default <strong>DocumentRoot</strong>: files there map to URLs on port 80. Configuration lives under <strong><code>/etc/apache2/</code></strong>; logs under <strong><code>/var/log/apache2/</code></strong>. Editing files as root in the web root is normal for class labs; in production you would use deployment users, permissions, and CI instead.</p>\n<p>You will publish a single <code>index.html</code> using a shell pipeline. This is not how production sites are deployed, but it is an excellent mental model for “the server reads a file and sends bytes over HTTP.”</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">echo &#39;&lt;!DOCTYPE html&gt;&lt;html lang=&quot;en&quot;&gt;&lt;head&gt;&lt;meta charset=&quot;utf-8&quot;&gt;&lt;title&gt;Lab H-01&lt;/title&gt;&lt;/head&gt;&lt;body&gt;&lt;h1&gt;Hello from Kali&lt;/h1&gt;&lt;/body&gt;&lt;/html&gt;&#39; | sudo tee /var/www/html/index.html\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> <code>echo</code> prints a string. The <code>|</code> <strong>pipe</strong> sends that output as <strong>stdin</strong> to <code>tee</code>. <code>tee</code> writes stdin to <strong>stdout and</strong> to the given file. <code>sudo</code> runs <code>tee</code> with <strong>root</strong> privileges so it can write under <code>/var/www/html/</code>.</li>\n<li><strong>Why we use it here:</strong> You avoid opening an editor for a one-line sanity check. If this works, Apache can read the file world-readable from DocumentRoot.</li>\n</ul>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">ip -br a\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> <code>ip</code> is the modern replacement for <code>ifconfig</code>. <code>-br</code> is <strong>brief</strong> human output; <code>a</code> shows <strong>all addresses</strong>.</li>\n<li><strong>Why we use it here:</strong> Your host machine needs the VM’s <strong>correct IP</strong> (often bridged or NAT). Wrong interface = wrong IP = browser cannot connect.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/./</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/.</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/./…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>Publish one page and prove the full path</strong></p>\n<ol>\n<li>Re-open what <strong>DocumentRoot</strong> means: Apache maps a URL path to a directory on disk. For <code>/</code>, the server looks for <code>index.html</code> (or another DirectoryIndex) under that root.</li>\n<li>Run the long <code>echo &#39;…html…&#39; | sudo tee /var/www/html/index.html</code> pipeline from the <strong>Commands explained</strong> section. Before Enter, trace data: <code>echo</code> writes to <strong>stdout</strong> → <strong>pipe</strong> → <code>tee</code> reads stdin, prints to your terminal <strong>and</strong> writes the file. <code>sudo</code> only elevates <code>tee</code>, not the string generation.</li>\n<li><code>ls -l /var/www/html/index.html</code> — note owner (often <code>root</code>), group (<code>www-data</code> on Debian), mode (<code>644</code>). Explain why Apache (running as <code>www-data</code>) can still <strong>read</strong> the file.</li>\n<li>On the VM: <code>curl -sS http://127.0.0.1/ | head -n 20</code>. The <code>-sS</code> combo hides progress but still surfaces errors.</li>\n<li><code>ip -br a</code> — pick the address your <strong>host</strong> browser should use (NAT vs bridged). Open <code>http://&lt;that-ip&gt;/</code> on the host.</li>\n<li><strong>Stretch:</strong> edit the same file with <code>sudo nano</code> and add <code>&lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1&quot;&gt;</code>. Reload; in devtools toggle device toolbar and describe what changed visually.</li>\n</ol>\n<ul>\n<li><strong>Checkpoint:</strong> In one sentence: which <strong>absolute path</strong> on disk produced the bytes you saw in the browser?</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<ul>\n<li>Host browser shows <strong>Hello from Kali</strong>.</li>\n<li><code>curl</code> from the VM shows the same HTML source.</li>\n<li>You can explain aloud: <strong>which file</strong> Apache served for <code>/</code>.</li>\n</ul>\n<h4>Common mistakes</h4>\n<ul>\n<li><strong>403 Forbidden:</strong> often permissions or missing <code>index.html</code>.</li>\n<li><strong>Connection refused:</strong> Apache not running or firewall blocking (later labs).</li>\n<li><strong>Wrong page:</strong> you edited a different path than DocumentRoot.</li>\n</ul>\n<h4>Going further</h4>\n<p>Read Apache’s default site config (<code>/etc/apache2/sites-enabled/</code>) and find the <code>DocumentRoot</code> directive—map it to a folder listing in your notes.</p>\n<hr>\n</div>","labs-kali-h-02":"<p class=\"lh-tg-source\"><strong>Lab H-02 — Semantic page skeleton</strong> · <span class=\"lh-tg-chapter\">Part 1 — HTML (Apache2 on Kali)</span> · Lesson <code>labs-kali-h-02</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab H-02 — Semantic page skeleton</h3>\n<p><strong>Suggested pace:</strong> 60–90 minutes</p>\n<p><strong>Goal:</strong> Build a newsletter-style page using semantic regions instead of anonymous <code>&lt;div&gt;</code> soup.</p>\n<h4>Why this lab exists</h4>\n<p>This HTML track lab builds real files under Apache—mirroring how static sites are served before frameworks.</p>\n<h4>Background</h4>\n<p>On Debian-based systems (including Kali), <strong>Apache</strong> often uses <strong><code>/var/www/html</code></strong> as the default <strong>DocumentRoot</strong>: files there map to URLs on port 80. Configuration lives under <strong><code>/etc/apache2/</code></strong>; logs under <strong><code>/var/log/apache2/</code></strong>. Editing files as root in the web root is normal for class labs; in production you would use deployment users, permissions, and CI instead.</p>\n<p><strong>Semantics</strong> help accessibility tools and search engines understand <em>role</em> (navigation vs main content vs aside). Screen readers expose landmarks; keyboard users jump between landmarks in many browsers.</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo nano /var/www/html/semantic.html\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> <code>nano</code> is a simple terminal editor. <code>sudo</code> lets you write under <code>/var/www/html/</code>.</li>\n<li><strong>Why we use it here:</strong> You need a multi-line HTML file; pipelines are awkward here.</li>\n</ul>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo apt install -y tidy &amp;&amp; tidy -e /var/www/html/semantic.html\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> <code>tidy</code> can <strong>parse</strong> HTML and report errors/warnings. <code>-e</code> shows errors only.</li>\n<li><strong>Why we use it here:</strong> Catches unclosed tags and malformed trees early—before you debug “why does my layout look wrong?”</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/semantic.html</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/./</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/.</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/./…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>H-02 — Semantic skeleton</strong></p>\n<ol>\n<li>On paper, label <strong>header</strong>, <strong>nav</strong>, <strong>main</strong> (with one <strong>article</strong> + one <strong>aside</strong>), <strong>footer</strong>. Draw arrows for tab order you want.</li>\n<li><code>sudo nano /var/www/html/semantic.html</code> — implement with a single <code>h1</code> and logical <code>h2</code>/<code>h3</code>. Avoid <code>&lt;div id=&quot;header&quot;&gt;</code> when a native element exists.</li>\n<li><code>sudo apt install -y tidy</code> then <code>tidy -e /var/www/html/semantic.html</code>. Fix <strong>every error</strong>; for each warning, decide “fix now” or “document why ignored.”</li>\n<li>Load <code>http://127.0.0.1/semantic.html</code> → <strong>View Page Source</strong> — confirm reading order matches your sketch.</li>\n</ol>\n<ul>\n<li><strong>Checkpoint:</strong> Name two assistive technologies or browser features that benefit from <code>&lt;main&gt;</code> vs a generic wrapper.</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p>Landmarks exist; no stray content outside <code>body</code>; <code>tidy</code> reports no errors.</p>\n<h4>Common mistakes</h4>\n<p>Using <code>&lt;div id=&quot;header&quot;&gt;</code> instead of <code>&lt;header&gt;</code> misses native semantics—use real elements.</p>\n<hr>\n</div>","labs-kali-h-03":"<p class=\"lh-tg-source\"><strong>Lab H-03 — Forms that POST to a dummy endpoint</strong> · <span class=\"lh-tg-chapter\">Part 1 — HTML (Apache2 on Kali)</span> · Lesson <code>labs-kali-h-03</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab H-03 — Forms that POST to a dummy endpoint</h3>\n<p><strong>Suggested pace:</strong> 60–90 minutes</p>\n<p><strong>Goal:</strong> See how <code>method</code> and <code>action</code> change browser behavior using a safe public echo service.</p>\n<h4>Why this lab exists</h4>\n<p>This HTML track lab builds real files under Apache—mirroring how static sites are served before frameworks.</p>\n<h4>Background</h4>\n<p>HTML forms are how browsers <strong>submit structured data</strong>. <code>method=&quot;get&quot;</code> encodes fields in the <strong>URL query string</strong>; <code>method=&quot;post&quot;</code> typically sends a <strong>body</strong>. <strong>httpbin.org</strong> reflects requests for learning—do not send secrets there.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/form.html</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/./</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/.</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/./…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>H-03 — Forms GET vs POST</strong></p>\n<ol>\n<li>Create <code>/var/www/html/form.html</code> served over <code>http://</code> (not <code>file://</code>) so DevTools Network behaves like a real site.</li>\n<li>Build a form: <code>method=&quot;get&quot;</code>, <code>action=&quot;https://httpbin.org/get&quot;</code>, fields: text, email, number, checkbox. Submit and <strong>read the address bar</strong> — map each <code>name=</code> to its value.</li>\n<li>Switch to <code>method=&quot;post&quot;</code> and <code>action=&quot;https://httpbin.org/post&quot;</code>. Submit again. Open <strong>Network</strong> → select request → compare <strong>Query String</strong> vs <strong>Request payload</strong>.</li>\n<li>In your notebook: one paragraph on why passwords must <strong>never</strong> use GET; mention logs, proxies, and Referer leakage at a high level.</li>\n</ol>\n<ul>\n<li><strong>Checkpoint:</strong> Paste one GET URL and describe which parts are client-controlled vs server-controlled.</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p>You can point to a Network panel entry showing POST body fields.</p>\n<h4>Common mistakes</h4>\n<p>Mixed content or blocked requests if you served the page over HTTPS with mismatched policies—use simple HTTP on localhost if confused.</p>\n<hr>\n</div>","labs-kali-h-04":"<p class=\"lh-tg-source\"><strong>Lab H-04 — Tables for incident timelines</strong> · <span class=\"lh-tg-chapter\">Part 1 — HTML (Apache2 on Kali)</span> · Lesson <code>labs-kali-h-04</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab H-04 — Tables for incident timelines</h3>\n<p><strong>Suggested pace:</strong> 60–90 minutes</p>\n<p><strong>Goal:</strong> Model time-ordered events in an accessible <code>&lt;table&gt;</code>.</p>\n<h4>Why this lab exists</h4>\n<p>This HTML track lab builds real files under Apache—mirroring how static sites are served before frameworks.</p>\n<h4>Background</h4>\n<p>Tables are for <strong>tabular data</strong>, not general layout. <strong><code>&lt;caption&gt;</code></strong>, <strong><code>&lt;thead&gt;</code></strong>, <strong><code>&lt;tbody&gt;</code></strong>, and <strong><code>scope</code></strong> make relationships explicit for assistive tech.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/timeline.html</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/./</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/.</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/./…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>H-04 — Incident timeline table</strong></p>\n<ol>\n<li>Invent <strong>≥4</strong> fictional IR events with timestamps, source system, owner.</li>\n<li>Build <code>/var/www/html/timeline.html</code> with <code>&lt;caption&gt;</code>, <code>&lt;thead&gt;</code>, <code>&lt;tbody&gt;</code>, and <code>scope=&quot;col&quot;</code> on column headers.</li>\n<li>Use <code>&lt;time datetime=&quot;…&quot;&gt;</code> in at least one cell with a human-readable label inside the tag.</li>\n<li>Optional tiny <code>&lt;style&gt;</code>: zebra rows, monospace timestamps — preview of the CSS track.</li>\n</ol>\n<ul>\n<li><strong>Checkpoint:</strong> Explain how <code>scope</code> helps a screen reader associate <code>&lt;th&gt;</code> with body cells.</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p>Table reads correctly with headers associated to columns.</p>\n<h4>Common mistakes</h4>\n<p>Missing <code>scope</code> on <code>th</code> can confuse screen readers on complex tables—start simple and validate.</p>\n<hr>\n</div>","labs-kali-h-05":"<p class=\"lh-tg-source\"><strong>Lab H-05 — Multimedia and fallbacks</strong> · <span class=\"lh-tg-chapter\">Part 1 — HTML (Apache2 on Kali)</span> · Lesson <code>labs-kali-h-05</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab H-05 — Multimedia and fallbacks</h3>\n<p><strong>Suggested pace:</strong> 60–90 minutes</p>\n<p><strong>Goal:</strong> Serve responsive images with <code>&lt;picture&gt;</code> and meaningful <code>alt</code> text.</p>\n<h4>Why this lab exists</h4>\n<p>This HTML track lab builds real files under Apache—mirroring how static sites are served before frameworks.</p>\n<h4>Background</h4>\n<p>Different browsers and widths benefit from different <strong>formats</strong> (WebP vs JPEG) and <strong>resolutions</strong>. <code>&lt;picture&gt;</code> lets the browser pick a <strong>source</strong> while keeping a fallback <code>&lt;img&gt;</code>.</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo mkdir -p /var/www/html/assets &amp;&amp; cd /var/www/html/assets &amp;&amp; wget -O sample.jpg &#39;https://picsum.photos/800/600.jpg&#39;\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> <code>mkdir -p</code> ensures folders exist. <code>wget</code> downloads a remote file to a local name.</li>\n<li><strong>Why we use it here:</strong> Gives you a legal-to-use placeholder image for experiments (respect picsum rate limits; for offline labs, copy a small local JPG instead).</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/media.html</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/./</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/.</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/./…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>H-05 — Responsive images</strong></p>\n<ol>\n<li><code>sudo mkdir -p /var/www/html/assets</code> and download or copy <strong>two</strong> JPEGs (different aspect ratios or widths).</li>\n<li>Build <code>/var/www/html/media.html</code> with <code>&lt;figure&gt;</code>, <code>&lt;figcaption&gt;</code>, and <code>&lt;picture&gt;</code> containing multiple <code>&lt;source&gt;</code> plus fallback <code>&lt;img alt=&quot;…&quot;&gt;</code>.</li>\n<li>Write <code>alt</code> text that describes <strong>purpose</strong> (why the image exists), not the filename.</li>\n<li>Resize the browser; in Network note which asset bytes were transferred at narrow vs wide widths.</li>\n</ol>\n<ul>\n<li><strong>Checkpoint:</strong> When is <code>alt=&quot;&quot;</code> acceptable? Write your answer with an example from this page or “none on this page.”</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p>Image displays; <code>alt</code> describes purpose not filename; <code>&lt;figcaption&gt;</code> adds context.</p>\n<h4>Common mistakes</h4>\n<p>Empty <code>alt</code> is only appropriate for decorative images—don’t use empty <code>alt</code> on informative images.</p>\n<hr>\n</div>","labs-kali-h-06":"<p class=\"lh-tg-source\"><strong>Lab H-06 — Microdata or JSON-LD</strong> · <span class=\"lh-tg-chapter\">Part 1 — HTML (Apache2 on Kali)</span> · Lesson <code>labs-kali-h-06</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab H-06 — Microdata or JSON-LD</h3>\n<p><strong>Suggested pace:</strong> 60–90 minutes</p>\n<p><strong>Goal:</strong> Publish structured data machines can parse (training event example).</p>\n<h4>Why this lab exists</h4>\n<p>This HTML track lab builds real files under Apache—mirroring how static sites are served before frameworks.</p>\n<h4>Background</h4>\n<p>Search engines and tools consume <strong>JSON-LD</strong> in <code>&lt;script type=&quot;application/ld+json&quot;&gt;</code>. It does not change visual layout but improves discoverability and rich results <strong>when eligible</strong>.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/event.html</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/./</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/.</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/./…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>H-06 — JSON-LD event</strong></p>\n<ol>\n<li>Human-visible block: training event title, date, location paragraph.</li>\n<li>Add <code>&lt;script type=&quot;application/ld+json&quot;&gt;</code> with valid JSON: <code>@context</code>, <code>@type</code>, <code>name</code>, <code>startDate</code>, <code>location</code>.</li>\n<li>Copy the JSON to a temp file and run <code>python3 -m json.tool &lt; file</code> — fix until it parses.</li>\n<li>If the VM is reachable from the host, try Google Rich Results Test on the URL; otherwise paste HTML into the tool.</li>\n</ol>\n<ul>\n<li><strong>Checkpoint:</strong> What breaks silently if you leave a trailing comma in JSON?</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p>JSON-LD parses; page still validates as HTML.</p>\n<h4>Common mistakes</h4>\n<p>Invalid JSON silently fails rich parsing—always syntax-check.</p>\n<hr>\n</div>","labs-kali-h-07":"<p class=\"lh-tg-source\"><strong>Lab H-07 — Accessibility audit</strong> · <span class=\"lh-tg-chapter\">Part 1 — HTML (Apache2 on Kali)</span> · Lesson <code>labs-kali-h-07</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab H-07 — Accessibility audit</h3>\n<p><strong>Suggested pace:</strong> 60–90 minutes</p>\n<p><strong>Goal:</strong> Experience keyboard-only navigation and linearize content with a text browser.</p>\n<h4>Why this lab exists</h4>\n<p>This HTML track lab builds real files under Apache—mirroring how static sites are served before frameworks.</p>\n<h4>Background</h4>\n<p>Accessibility is <strong>testable</strong>: keyboard order, visible focus, name/label association, and source order all matter. <strong>Lynx</strong> approximates a linear reading order.</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo apt install -y lynx\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Installs the <strong>Lynx</strong> text-mode browser.</li>\n<li><strong>Why we use it here:</strong> Surfaces problems with tabindex abuse and missing labels.</li>\n</ul>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">lynx http://127.0.0.1/a11y.html\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Opens your page without CSS/JS rendering as a graphical browser would.</li>\n<li><strong>Why we use it here:</strong> If Lynx order is nonsense, your DOM order may be wrong.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/a11y.html</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/./</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/.</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/./…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>H-07 — Accessibility audit</strong></p>\n<ol>\n<li>Build <code>/var/www/html/a11y.html</code>: real <code>&lt;button&gt;</code> (not <code>&lt;div onclick&gt;</code>), labeled file input, several links, one disclosure pattern.</li>\n<li><strong>Unplug the mouse</strong> — Tab / Shift+Tab through everything; fix focus traps and invisible focus.</li>\n<li>Add <code>:focus-visible</code> styles in a <code>&lt;style&gt;</code> block so keyboard users see where they are.</li>\n<li><code>sudo apt install -y lynx</code> → <code>lynx http://127.0.0.1/a11y.html</code> — confirm linear reading order.</li>\n</ol>\n<ul>\n<li><strong>Checkpoint:</strong> List one issue Lynx exposed that your graphical browser hid.</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p>Full keyboard path; focus never disappears; Lynx order is logical.</p>\n<h4>Common mistakes</h4>\n<p><code>div</code> buttons without <code>role</code>/<code>tabindex</code>/<code>keydown</code> handlers break keyboard parity—use real <code>&lt;button&gt;</code>.</p>\n<hr>\n</div>","labs-kali-h-08":"<p class=\"lh-tg-source\"><strong>Lab H-08 — Apache includes or multi-page site</strong> · <span class=\"lh-tg-chapter\">Part 1 — HTML (Apache2 on Kali)</span> · Lesson <code>labs-kali-h-08</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab H-08 — Apache includes or multi-page site</h3>\n<p><strong>Suggested pace:</strong> 60–90 minutes</p>\n<p><strong>Goal:</strong> Organize multiple HTML pages under one folder with shared navigation.</p>\n<h4>Why this lab exists</h4>\n<p>This HTML track lab builds real files under Apache—mirroring how static sites are served before frameworks.</p>\n<h4>Background</h4>\n<p>Small sites are still <strong>information architecture</strong>: predictable URLs, repeated nav, relative links. SSI (<code>mod_include</code>) can deduplicate headers—optional here.</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo mkdir -p /var/www/html/site\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Creates a subdirectory served at <code>/site/</code>.</li>\n<li><strong>Why we use it here:</strong> Keeps experiments out of the web root clutter.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/site/index.html</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/site/</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/site</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/site/…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>H-08 — Multi-page site</strong></p>\n<ol>\n<li><code>sudo mkdir -p /var/www/html/site</code> — add <code>index.html</code> and <code>about.html</code>.</li>\n<li>Use <strong>relative</strong> links (<code>href=&quot;about.html&quot;</code>) so the folder is portable.</li>\n<li>Duplicate a shared nav block by hand first; note the maintenance pain for your report.</li>\n<li>Optional: research <code>mod_include</code> + SSI — <strong>snapshot + backup configs</strong> before enabling.</li>\n</ol>\n<ul>\n<li><strong>Checkpoint:</strong> Why can absolute paths like <code>/missing/</code> break when you move the site folder?</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p>Both pages reachable; nav works in both directions.</p>\n<h4>Common mistakes</h4>\n<p>Absolute paths like <code>/missing/</code> break when you move folders—prefer relative during learning.</p>\n<hr>\n</div>","labs-kali-h-09":"<p class=\"lh-tg-source\"><strong>Lab H-09 — Error pages and custom 404</strong> · <span class=\"lh-tg-chapter\">Part 1 — HTML (Apache2 on Kali)</span> · Lesson <code>labs-kali-h-09</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab H-09 — Error pages and custom 404</h3>\n<p><strong>Suggested pace:</strong> 60–90 minutes</p>\n<p><strong>Goal:</strong> Safely introduce VirtualHost and <code>ErrorDocument</code> patterns.</p>\n<h4>Why this lab exists</h4>\n<p>This HTML track lab builds real files under Apache—mirroring how static sites are served before frameworks.</p>\n<h4>Background</h4>\n<p><strong>Virtual hosts</strong> let Apache map hostnames/paths to different roots. <code>ErrorDocument</code> maps HTTP errors to custom pages—great for learner UX and IR-themed storytelling.</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo cp /etc/apache2/sites-available/000-default.conf /etc/apache2/sites-available/lab-html.conf\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Copies a known template site file.</li>\n<li><strong>Why we use it here:</strong> Never edit only memory—keep a rollback path (<code>sudo cp</code> backup).</li>\n</ul>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo apache2ctl configtest\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Parses Apache configs and reports <strong>Syntax OK</strong> or errors.</li>\n<li><strong>Why we use it here:</strong> Prevents taking down the server with a typo.</li>\n</ul>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo systemctl reload apache2\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> <code>reload</code> asks Apache to <strong>re-read configuration</strong> with minimal downtime.</li>\n<li><strong>Why we use it here:</strong> Safer than restart when only configs changed.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/this-url-should-404-lab-test</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/./</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/.</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/./…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>H-09 — Custom 404</strong></p>\n<ol>\n<li>Create <code>/var/www/html/404.html</code> with a calm message and link home.</li>\n<li><code>sudo cp /etc/apache2/sites-available/000-default.conf /etc/apache2/sites-available/lab-html.conf</code> — edit a <strong>copy</strong>, keep the original as reference.</li>\n<li>Add <code>ErrorDocument 404 /404.html</code> inside the correct <code>VirtualHost</code>. Run <code>sudo apache2ctl configtest</code> before any reload.</li>\n<li><code>sudo apache2ctl -S</code> — resolve duplicate vhost/port conflicts <strong>in writing</strong> before <code>systemctl reload apache2</code>.</li>\n<li>Request a URL that does not exist; confirm status <strong>404</strong> and your HTML body.</li>\n</ol>\n<ul>\n<li><strong>Checkpoint:</strong> Difference between <code>reload</code> and <code>restart</code> for Apache in one sentence.</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p>404 shows your page; Apache still serves normal routes.</p>\n<h4>Common mistakes</h4>\n<p>Multiple default vhosts fighting for port 80—read <code>apache2ctl -S</code> output carefully.</p>\n<hr>\n</div>","labs-kali-h-10":"<p class=\"lh-tg-source\"><strong>Lab H-10 — Security headers (intro level)</strong> · <span class=\"lh-tg-chapter\">Part 1 — HTML (Apache2 on Kali)</span> · Lesson <code>labs-kali-h-10</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab H-10 — Security headers (intro level)</h3>\n<p><strong>Suggested pace:</strong> 60–90 minutes</p>\n<p><strong>Goal:</strong> Set a custom response header and observe it with <code>curl</code>.</p>\n<h4>Why this lab exists</h4>\n<p>This HTML track lab builds real files under Apache—mirroring how static sites are served before frameworks.</p>\n<h4>Background</h4>\n<p>HTTP <strong>headers</strong> carry metadata: caching, security policy hints, server identifiers. <code>mod_headers</code> lets Apache add or remove headers consistently.</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo a2enmod headers &amp;&amp; sudo systemctl reload apache2\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> <code>a2enmod</code> enables an Apache module symlink; <code>reload</code> applies.</li>\n<li><strong>Why we use it here:</strong> <code>Header</code> directives only work when <code>mod_headers</code> is loaded.</li>\n</ul>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">curl -I http://127.0.0.1/\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> <code>curl -I</code> fetches <strong>headers only</strong> (HEAD request behavior for this check).</li>\n<li><strong>Why we use it here:</strong> Fast loop: change config → reload → curl.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/./</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/.</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/./…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>H-10 — Response headers</strong></p>\n<ol>\n<li><code>sudo a2enmod headers</code> then <code>sudo systemctl reload apache2</code>.</li>\n<li>Add <code>Header set X-Lab-Author &quot;YourName&quot;</code> in the appropriate scope (server or directory block).</li>\n<li><code>curl -I http://127.0.0.1/</code> — find your header line and <code>Server:</code> line.</li>\n<li>Read MDN <strong>Content-Security-Policy</strong> overview (concept only) — note how headers shift security left compared to only fixing HTML.</li>\n</ol>\n<ul>\n<li><strong>Checkpoint:</strong> Why is <code>curl -I</code> faster than downloading the full body when iterating configs?</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p>Custom header visible; no Apache syntax errors.</p>\n<h4>Going further</h4>\n<p>Read Mozilla’s MDN pages on <strong>Content-Security-Policy</strong> (concept only)—no need to enable CSP on day one.</p>\n<hr>\n</div>","labs-kali-h-11":"<p class=\"lh-tg-source\"><strong>Lab H-11 — `robots.txt` and `sitemap.xml`</strong> · <span class=\"lh-tg-chapter\">Part 1 — HTML (Apache2 on Kali)</span> · Lesson <code>labs-kali-h-11</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab H-11 — <code>robots.txt</code> and <code>sitemap.xml</code></h3>\n<p><strong>Suggested pace:</strong> 60–90 minutes</p>\n<p><strong>Goal:</strong> Publish crawler hints and a sitemap for your static pages.</p>\n<h4>Why this lab exists</h4>\n<p>This HTML track lab builds real files under Apache—mirroring how static sites are served before frameworks.</p>\n<h4>Background</h4>\n<p><strong>robots.txt</strong> is a voluntary convention: polite crawlers read it before aggressive crawling. <strong>Sitemaps</strong> help discovery of URLs you care about.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/robots.txt</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/./</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/.</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/./…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>H-11 — robots.txt + sitemap</strong></p>\n<ol>\n<li><code>sudo mkdir -p /var/www/html/private</code> (empty placeholder).</li>\n<li><code>/var/www/html/robots.txt</code>: <code>User-agent: *</code>, <code>Disallow: /private/</code>, allow rest; add <code>Sitemap:</code> line with full URL to your <code>sitemap.xml</code>.</li>\n<li><code>sitemap.xml</code>: list your lab URLs with <code>&lt;loc&gt;</code> — validate with <code>xmllint</code> if installed.</li>\n<li><code>curl -s http://127.0.0.1/robots.txt</code> and verify.</li>\n</ol>\n<ul>\n<li><strong>Checkpoint:</strong> Why must secrets never rely on <code>robots.txt</code> alone?</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p><code>curl http://127.0.0.1/robots.txt</code> shows expected rules.</p>\n<h4>Common mistakes</h4>\n<p>robots.txt is <strong>not access control</strong>—sensitive data must not live under the web root.</p>\n<hr>\n</div>","labs-kali-h-12":"<p class=\"lh-tg-source\"><strong>Lab H-12 — Internationalization basics</strong> · <span class=\"lh-tg-chapter\">Part 1 — HTML (Apache2 on Kali)</span> · Lesson <code>labs-kali-h-12</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab H-12 — Internationalization basics</h3>\n<p><strong>Suggested pace:</strong> 60–90 minutes</p>\n<p><strong>Goal:</strong> Use <code>lang</code> attributes and parallel pages for EN/FR (or any pair you know).</p>\n<h4>Why this lab exists</h4>\n<p>This HTML track lab builds real files under Apache—mirroring how static sites are served before frameworks.</p>\n<h4>Background</h4>\n<p>Declaring language helps <strong>screen readers</strong> pick pronunciation rules and helps search engines categorize content.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/index-fr.html</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/./</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/.</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/./…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>H-12 — Internationalization</strong></p>\n<ol>\n<li>Copy a small English page to <code>/var/www/html/index-fr.html</code> with <code>&lt;html lang=&quot;fr&quot;&gt;</code>.</li>\n<li>Translate headings and nav honestly (dictionary ok). Keep parallel structure so CSS can be shared later.</li>\n<li>Add a <code>&lt;nav&gt;</code> language switcher linking EN ↔ FR.</li>\n<li>In devtools Accessibility tree, inspect how <code>lang</code> is exposed.</li>\n</ol>\n<ul>\n<li><strong>Checkpoint:</strong> One sentence on <code>hreflang</code> for multi-domain SEO (research, no implementation required).</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p>Both pages validate; language switcher visible.</p>\n<h4>Going further</h4>\n<p>Research <code>hreflang</code> for multi-language SEO (concept).</p>\n<hr>\n</div>","labs-kali-c-01":"<p class=\"lh-tg-source\"><strong>Lab C-01 — External stylesheet architecture</strong> · <span class=\"lh-tg-chapter\">Part 2 — CSS (Apache2 on Kali)</span> · Lesson <code>labs-kali-c-01</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab C-01 — External stylesheet architecture</h3>\n<p><strong>Suggested pace:</strong> 60–95 minutes</p>\n<p><strong>Goal:</strong> Split CSS into base/layout/theme and link from HTML.</p>\n<h4>Why this lab exists</h4>\n<p>CSS in browser devtools is fast—but <strong>Apache-backed files</strong> train deployment thinking: paths, cache busting during edits, and real multi-file projects.</p>\n<h4>Background</h4>\n<p>On Debian-based systems (including Kali), <strong>Apache</strong> often uses <strong><code>/var/www/html</code></strong> as the default <strong>DocumentRoot</strong>: files there map to URLs on port 80. Configuration lives under <strong><code>/etc/apache2/</code></strong>; logs under <strong><code>/var/log/apache2/</code></strong>. Editing files as root in the web root is normal for class labs; in production you would use deployment users, permissions, and CI instead.</p>\n<p>All CSS labs assume files under <strong><code>/var/www/html/css-lab/</code></strong>. Create that folder once: <code>sudo mkdir -p /var/www/html/css-lab/styles</code>. Use <code>sudo nano</code> or sync from <code>~/labs/css</code> if you prefer editing in your home directory then copying—<strong>document your workflow</strong> in notes.</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo mkdir -p /var/www/html/css-lab/styles\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Ensures lab directory tree exists.</li>\n<li><strong>Why we use it here:</strong> Avoids permission errors mid-lab.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/css-lab/index.html</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/css-lab/</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/css-lab</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/css-lab/…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>External stylesheet architecture</strong></p>\n<ul>\n<li>Create <code>/var/www/html/css-lab/index.html</code> with a minimal semantic shell (<code>&lt;!DOCTYPE html&gt;</code>, <code>&lt;html lang=&quot;en&quot;&gt;</code>, <code>&lt;head&gt;</code>, <code>&lt;body&gt;</code>).</li>\n<li>Add three <code>&lt;link rel=&quot;stylesheet&quot; href=&quot;styles/base.css&quot;&gt;</code> (and <code>layout.css</code>, <code>theme.css</code>) in <strong>dependency order</strong>: variables first, layout second, theme last.</li>\n<li>In <code>base.css</code>, define <code>:root { --fg: …; --bg: …; --space: … }</code>. In <code>layout.css</code>, set <code>body { margin:0; font-family:… }</code> and a simple container. In <code>theme.css</code>, add headings and link colors using <strong>only</strong> variables from <code>:root</code>.</li>\n<li><strong>Checkpoint:</strong> View page source in the browser and confirm <strong>three</strong> separate network requests for CSS (Network tab).</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p>Visual result matches the lab goal; you can explain <strong>which file</strong> controlled each major aspect.</p>\n<h4>Common mistakes</h4>\n<p>Editing the wrong CSS file path in <code>&lt;link href&gt;</code>—always verify with Network tab (200 on CSS).</p>\n<h4>Going further</h4>\n<p>Introduce a <code>?v=1</code> query on your stylesheet link when cache fights you—explain tradeoffs.</p>\n<hr>\n</div>","labs-kali-c-02":"<p class=\"lh-tg-source\"><strong>Lab C-02 — Flexbox dashboard mockup</strong> · <span class=\"lh-tg-chapter\">Part 2 — CSS (Apache2 on Kali)</span> · Lesson <code>labs-kali-c-02</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab C-02 — Flexbox dashboard mockup</h3>\n<p><strong>Suggested pace:</strong> 60–95 minutes</p>\n<p><strong>Goal:</strong> Build a SOC-style dashboard using flexbox only.</p>\n<h4>Why this lab exists</h4>\n<p>CSS in browser devtools is fast—but <strong>Apache-backed files</strong> train deployment thinking: paths, cache busting during edits, and real multi-file projects.</p>\n<h4>Background</h4>\n<p>On Debian-based systems (including Kali), <strong>Apache</strong> often uses <strong><code>/var/www/html</code></strong> as the default <strong>DocumentRoot</strong>: files there map to URLs on port 80. Configuration lives under <strong><code>/etc/apache2/</code></strong>; logs under <strong><code>/var/log/apache2/</code></strong>. Editing files as root in the web root is normal for class labs; in production you would use deployment users, permissions, and CI instead.</p>\n<p>All CSS labs assume files under <strong><code>/var/www/html/css-lab/</code></strong>. Create that folder once: <code>sudo mkdir -p /var/www/html/css-lab/styles</code>. Use <code>sudo nano</code> or sync from <code>~/labs/css</code> if you prefer editing in your home directory then copying—<strong>document your workflow</strong> in notes.</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo mkdir -p /var/www/html/css-lab/styles\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Ensures lab directory tree exists.</li>\n<li><strong>Why we use it here:</strong> Avoids permission errors mid-lab.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/css-lab/index.html</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/css-lab/</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/css-lab</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/css-lab/…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>Flexbox dashboard</strong></p>\n<ul>\n<li>Create <code>flex.html</code> with a <code>&lt;header&gt;</code>, <code>&lt;aside&gt;</code>, <code>&lt;main&gt;</code>, and a <code>&lt;footer&gt;</code>. Wrap main content in a <code>&lt;section&gt;</code> with <strong>cards</strong> (<code>&lt;article&gt;</code>).</li>\n<li>On a top-level flex container (e.g. <code>body</code> or <code>.app</code>), set <code>display:flex; flex-direction:column; min-height:100vh</code>. Put <strong>sidebar + main</strong> in a row flex: <code>display:flex; flex:1; min-height:0</code> on inner wrapper.</li>\n<li>Use <code>gap</code>, <code>flex-wrap</code>, and <code>min-width</code> on cards so the layout <strong>reflows</strong> under ~400px width. Use DevTools <strong>Flexbox overlay</strong> (grid/flex inspector) to see axes.</li>\n<li><strong>Checkpoint:</strong> At three viewport widths, screenshot or describe which element wraps first and why.</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p>Visual result matches the lab goal; you can explain <strong>which file</strong> controlled each major aspect.</p>\n<h4>Common mistakes</h4>\n<p>Editing the wrong CSS file path in <code>&lt;link href&gt;</code>—always verify with Network tab (200 on CSS).</p>\n<h4>Going further</h4>\n<p>Introduce a <code>?v=1</code> query on your stylesheet link when cache fights you—explain tradeoffs.</p>\n<hr>\n</div>","labs-kali-c-03":"<p class=\"lh-tg-source\"><strong>Lab C-03 — CSS Grid magazine layout</strong> · <span class=\"lh-tg-chapter\">Part 2 — CSS (Apache2 on Kali)</span> · Lesson <code>labs-kali-c-03</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab C-03 — CSS Grid magazine layout</h3>\n<p><strong>Suggested pace:</strong> 60–95 minutes</p>\n<p><strong>Goal:</strong> Use named grid areas and responsive breakpoints.</p>\n<h4>Why this lab exists</h4>\n<p>CSS in browser devtools is fast—but <strong>Apache-backed files</strong> train deployment thinking: paths, cache busting during edits, and real multi-file projects.</p>\n<h4>Background</h4>\n<p>On Debian-based systems (including Kali), <strong>Apache</strong> often uses <strong><code>/var/www/html</code></strong> as the default <strong>DocumentRoot</strong>: files there map to URLs on port 80. Configuration lives under <strong><code>/etc/apache2/</code></strong>; logs under <strong><code>/var/log/apache2/</code></strong>. Editing files as root in the web root is normal for class labs; in production you would use deployment users, permissions, and CI instead.</p>\n<p>All CSS labs assume files under <strong><code>/var/www/html/css-lab/</code></strong>. Create that folder once: <code>sudo mkdir -p /var/www/html/css-lab/styles</code>. Use <code>sudo nano</code> or sync from <code>~/labs/css</code> if you prefer editing in your home directory then copying—<strong>document your workflow</strong> in notes.</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo mkdir -p /var/www/html/css-lab/styles\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Ensures lab directory tree exists.</li>\n<li><strong>Why we use it here:</strong> Avoids permission errors mid-lab.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/css-lab/index.html</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/css-lab/</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/css-lab</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/css-lab/…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>CSS Grid magazine layout</strong></p>\n<ul>\n<li>Create <code>grid.html</code> with a <code>.magazine</code> container: <code>display:grid; grid-template-columns: repeat(12, 1fr); gap: …</code>.</li>\n<li>Define <code>grid-template-areas</code> for <strong>at least six regions</strong> (hero, feature, sidebar, quote, footer, etc.) and assign <code>grid-column</code> / <code>grid-row</code> or <code>grid-area</code> to children.</li>\n<li>Add one <code>@media (max-width: …)</code> breakpoint that <strong>changes</strong> <code>grid-template-areas</code> (e.g. sidebar drops below).</li>\n<li><strong>Checkpoint:</strong> Draw your grid on paper with row/column numbers, then compare to DevTools <strong>Layout</strong> pane for grids.</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p>Visual result matches the lab goal; you can explain <strong>which file</strong> controlled each major aspect.</p>\n<h4>Common mistakes</h4>\n<p>Editing the wrong CSS file path in <code>&lt;link href&gt;</code>—always verify with Network tab (200 on CSS).</p>\n<h4>Going further</h4>\n<p>Introduce a <code>?v=1</code> query on your stylesheet link when cache fights you—explain tradeoffs.</p>\n<hr>\n</div>","labs-kali-c-04":"<p class=\"lh-tg-source\"><strong>Lab C-04 — Fluid typography</strong> · <span class=\"lh-tg-chapter\">Part 2 — CSS (Apache2 on Kali)</span> · Lesson <code>labs-kali-c-04</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab C-04 — Fluid typography</h3>\n<p><strong>Suggested pace:</strong> 60–95 minutes</p>\n<p><strong>Goal:</strong> Scale type smoothly with clamp().</p>\n<h4>Why this lab exists</h4>\n<p>CSS in browser devtools is fast—but <strong>Apache-backed files</strong> train deployment thinking: paths, cache busting during edits, and real multi-file projects.</p>\n<h4>Background</h4>\n<p>On Debian-based systems (including Kali), <strong>Apache</strong> often uses <strong><code>/var/www/html</code></strong> as the default <strong>DocumentRoot</strong>: files there map to URLs on port 80. Configuration lives under <strong><code>/etc/apache2/</code></strong>; logs under <strong><code>/var/log/apache2/</code></strong>. Editing files as root in the web root is normal for class labs; in production you would use deployment users, permissions, and CI instead.</p>\n<p>All CSS labs assume files under <strong><code>/var/www/html/css-lab/</code></strong>. Create that folder once: <code>sudo mkdir -p /var/www/html/css-lab/styles</code>. Use <code>sudo nano</code> or sync from <code>~/labs/css</code> if you prefer editing in your home directory then copying—<strong>document your workflow</strong> in notes.</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo mkdir -p /var/www/html/css-lab/styles\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Ensures lab directory tree exists.</li>\n<li><strong>Why we use it here:</strong> Avoids permission errors mid-lab.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/css-lab/index.html</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/css-lab/</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/css-lab</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/css-lab/…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>Fluid typography with clamp()</strong></p>\n<ul>\n<li>In <code>type.html</code> (or reuse an existing page), set <code>html { font-size: clamp(15px, 0.9vw + 12px, 20px); }</code> and style <code>h1</code> with a second <code>clamp()</code> for <code>font-size</code>.</li>\n<li>Resize the window slowly: note when <strong>line length</strong> becomes uncomfortable; adjust <code>max-width</code> on <code>article</code> in <code>ch</code> units.</li>\n<li>Document in your notebook <strong>the three numbers</strong> in <code>clamp(min, preferred, max)</code> and what each controls mathematically.</li>\n<li><strong>Checkpoint:</strong> At 320px and 1280px width, record computed <code>font-size</code> from Computed styles.</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p>Visual result matches the lab goal; you can explain <strong>which file</strong> controlled each major aspect.</p>\n<h4>Common mistakes</h4>\n<p>Editing the wrong CSS file path in <code>&lt;link href&gt;</code>—always verify with Network tab (200 on CSS).</p>\n<h4>Going further</h4>\n<p>Introduce a <code>?v=1</code> query on your stylesheet link when cache fights you—explain tradeoffs.</p>\n<hr>\n</div>","labs-kali-c-05":"<p class=\"lh-tg-source\"><strong>Lab C-05 — Dark mode toggle (prefers + manual)</strong> · <span class=\"lh-tg-chapter\">Part 2 — CSS (Apache2 on Kali)</span> · Lesson <code>labs-kali-c-05</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab C-05 — Dark mode toggle (prefers + manual)</h3>\n<p><strong>Suggested pace:</strong> 60–95 minutes</p>\n<p><strong>Goal:</strong> Respect system theme and allow manual override with persistence.</p>\n<h4>Why this lab exists</h4>\n<p>CSS in browser devtools is fast—but <strong>Apache-backed files</strong> train deployment thinking: paths, cache busting during edits, and real multi-file projects.</p>\n<h4>Background</h4>\n<p>On Debian-based systems (including Kali), <strong>Apache</strong> often uses <strong><code>/var/www/html</code></strong> as the default <strong>DocumentRoot</strong>: files there map to URLs on port 80. Configuration lives under <strong><code>/etc/apache2/</code></strong>; logs under <strong><code>/var/log/apache2/</code></strong>. Editing files as root in the web root is normal for class labs; in production you would use deployment users, permissions, and CI instead.</p>\n<p>All CSS labs assume files under <strong><code>/var/www/html/css-lab/</code></strong>. Create that folder once: <code>sudo mkdir -p /var/www/html/css-lab/styles</code>. Use <code>sudo nano</code> or sync from <code>~/labs/css</code> if you prefer editing in your home directory then copying—<strong>document your workflow</strong> in notes.</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo mkdir -p /var/www/html/css-lab/styles\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Ensures lab directory tree exists.</li>\n<li><strong>Why we use it here:</strong> Avoids permission errors mid-lab.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/css-lab/index.html</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/css-lab/</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/css-lab</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/css-lab/…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>Dark mode: prefers-color-scheme + manual override</strong></p>\n<ul>\n<li>Start from <code>:root</code> light variables. Add <code>@media (prefers-color-scheme: dark)</code> block that swaps variables.</li>\n<li>Add a <strong>toggle button</strong> that sets a class on <code>&lt;html&gt;</code> (e.g. <code>html.force-light</code> / <code>html.force-dark</code>) with CSS that <strong>wins</strong> over media query using slightly higher specificity or order.</li>\n<li>Persist choice in <code>localStorage</code> (you will wire minimal JS if needed—<strong>prefer</strong> a tiny inline script only for this lab, or pair with the JS track).</li>\n<li><strong>Checkpoint:</strong> Toggle while OS theme is dark and light; describe precedence in one paragraph.</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p>Visual result matches the lab goal; you can explain <strong>which file</strong> controlled each major aspect.</p>\n<h4>Common mistakes</h4>\n<p>Editing the wrong CSS file path in <code>&lt;link href&gt;</code>—always verify with Network tab (200 on CSS).</p>\n<h4>Going further</h4>\n<p>Introduce a <code>?v=1</code> query on your stylesheet link when cache fights you—explain tradeoffs.</p>\n<hr>\n</div>","labs-kali-c-06":"<p class=\"lh-tg-source\"><strong>Lab C-06 — Animations and reduced motion</strong> · <span class=\"lh-tg-chapter\">Part 2 — CSS (Apache2 on Kali)</span> · Lesson <code>labs-kali-c-06</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab C-06 — Animations and reduced motion</h3>\n<p><strong>Suggested pace:</strong> 60–95 minutes</p>\n<p><strong>Goal:</strong> Animate respectfully with reduced-motion fallback.</p>\n<h4>Why this lab exists</h4>\n<p>CSS in browser devtools is fast—but <strong>Apache-backed files</strong> train deployment thinking: paths, cache busting during edits, and real multi-file projects.</p>\n<h4>Background</h4>\n<p>On Debian-based systems (including Kali), <strong>Apache</strong> often uses <strong><code>/var/www/html</code></strong> as the default <strong>DocumentRoot</strong>: files there map to URLs on port 80. Configuration lives under <strong><code>/etc/apache2/</code></strong>; logs under <strong><code>/var/log/apache2/</code></strong>. Editing files as root in the web root is normal for class labs; in production you would use deployment users, permissions, and CI instead.</p>\n<p>All CSS labs assume files under <strong><code>/var/www/html/css-lab/</code></strong>. Create that folder once: <code>sudo mkdir -p /var/www/html/css-lab/styles</code>. Use <code>sudo nano</code> or sync from <code>~/labs/css</code> if you prefer editing in your home directory then copying—<strong>document your workflow</strong> in notes.</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo mkdir -p /var/www/html/css-lab/styles\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Ensures lab directory tree exists.</li>\n<li><strong>Why we use it here:</strong> Avoids permission errors mid-lab.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/css-lab/index.html</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/css-lab/</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/css-lab</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/css-lab/…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>Animation + reduced motion</strong></p>\n<ul>\n<li>Create a <strong>badge</strong> with a subtle pulse (<code>@keyframes</code>). Keep animation duration long and amplitude small (professional, not flashy).</li>\n<li>Add <code>@media (prefers-reduced-motion: reduce)</code> that sets <code>animation: none</code> (or equivalent) for animated elements.</li>\n<li>In Firefox, test <strong>Settings → Accessibility → Reduce motion</strong> (or OS-level setting) and confirm animation stops.</li>\n<li><strong>Checkpoint:</strong> List who benefits from reduced-motion (vestibular disorders, migraine triggers, etc.) in your report.</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p>Visual result matches the lab goal; you can explain <strong>which file</strong> controlled each major aspect.</p>\n<h4>Common mistakes</h4>\n<p>Editing the wrong CSS file path in <code>&lt;link href&gt;</code>—always verify with Network tab (200 on CSS).</p>\n<h4>Going further</h4>\n<p>Introduce a <code>?v=1</code> query on your stylesheet link when cache fights you—explain tradeoffs.</p>\n<hr>\n</div>","labs-kali-c-07":"<p class=\"lh-tg-source\"><strong>Lab C-07 — Pure CSS interactive components</strong> · <span class=\"lh-tg-chapter\">Part 2 — CSS (Apache2 on Kali)</span> · Lesson <code>labs-kali-c-07</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab C-07 — Pure CSS interactive components</h3>\n<p><strong>Suggested pace:</strong> 60–95 minutes</p>\n<p><strong>Goal:</strong> Style interactive patterns without JavaScript.</p>\n<h4>Why this lab exists</h4>\n<p>CSS in browser devtools is fast—but <strong>Apache-backed files</strong> train deployment thinking: paths, cache busting during edits, and real multi-file projects.</p>\n<h4>Background</h4>\n<p>On Debian-based systems (including Kali), <strong>Apache</strong> often uses <strong><code>/var/www/html</code></strong> as the default <strong>DocumentRoot</strong>: files there map to URLs on port 80. Configuration lives under <strong><code>/etc/apache2/</code></strong>; logs under <strong><code>/var/log/apache2/</code></strong>. Editing files as root in the web root is normal for class labs; in production you would use deployment users, permissions, and CI instead.</p>\n<p>All CSS labs assume files under <strong><code>/var/www/html/css-lab/</code></strong>. Create that folder once: <code>sudo mkdir -p /var/www/html/css-lab/styles</code>. Use <code>sudo nano</code> or sync from <code>~/labs/css</code> if you prefer editing in your home directory then copying—<strong>document your workflow</strong> in notes.</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo mkdir -p /var/www/html/css-lab/styles\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Ensures lab directory tree exists.</li>\n<li><strong>Why we use it here:</strong> Avoids permission errors mid-lab.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/css-lab/index.html</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/css-lab/</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/css-lab</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/css-lab/…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>Pure CSS interactivity</strong></p>\n<ul>\n<li>Build an FAQ with <code>&lt;details&gt;&lt;summary&gt;…&lt;/summary&gt;…&lt;/details&gt;</code>. Style <code>summary</code> with <code>:focus-visible</code> outline and hover state.</li>\n<li>Add a <strong>no-JS</strong> hover card using nested rules (border, shadow). Keep contrast AA where possible.</li>\n<li><strong>Checkpoint:</strong> Tab to each summary; ensure visible focus and logical order.</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p>Visual result matches the lab goal; you can explain <strong>which file</strong> controlled each major aspect.</p>\n<h4>Common mistakes</h4>\n<p>Editing the wrong CSS file path in <code>&lt;link href&gt;</code>—always verify with Network tab (200 on CSS).</p>\n<h4>Going further</h4>\n<p>Introduce a <code>?v=1</code> query on your stylesheet link when cache fights you—explain tradeoffs.</p>\n<hr>\n</div>","labs-kali-c-08":"<p class=\"lh-tg-source\"><strong>Lab C-08 — Positioning and stacking contexts</strong> · <span class=\"lh-tg-chapter\">Part 2 — CSS (Apache2 on Kali)</span> · Lesson <code>labs-kali-c-08</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab C-08 — Positioning and stacking contexts</h3>\n<p><strong>Suggested pace:</strong> 60–95 minutes</p>\n<p><strong>Goal:</strong> Use sticky positioning and debug stacking contexts.</p>\n<h4>Why this lab exists</h4>\n<p>CSS in browser devtools is fast—but <strong>Apache-backed files</strong> train deployment thinking: paths, cache busting during edits, and real multi-file projects.</p>\n<h4>Background</h4>\n<p>On Debian-based systems (including Kali), <strong>Apache</strong> often uses <strong><code>/var/www/html</code></strong> as the default <strong>DocumentRoot</strong>: files there map to URLs on port 80. Configuration lives under <strong><code>/etc/apache2/</code></strong>; logs under <strong><code>/var/log/apache2/</code></strong>. Editing files as root in the web root is normal for class labs; in production you would use deployment users, permissions, and CI instead.</p>\n<p>All CSS labs assume files under <strong><code>/var/www/html/css-lab/</code></strong>. Create that folder once: <code>sudo mkdir -p /var/www/html/css-lab/styles</code>. Use <code>sudo nano</code> or sync from <code>~/labs/css</code> if you prefer editing in your home directory then copying—<strong>document your workflow</strong> in notes.</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo mkdir -p /var/www/html/css-lab/styles\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Ensures lab directory tree exists.</li>\n<li><strong>Why we use it here:</strong> Avoids permission errors mid-lab.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/css-lab/index.html</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/css-lab/</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/css-lab</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/css-lab/…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>Sticky header + stacking</strong></p>\n<ul>\n<li>Create overlapping <code>.card</code> elements. Intentionally set <strong>wrong</strong> <code>z-index</code> once, observe occlusion, then fix by creating a deliberate <strong>stacking context</strong> (<code>isolation: isolate</code> on a parent or <code>position: relative; z-index: 0</code>).</li>\n<li>Add <code>position: sticky; top: 0</code> on a header inside a scrollable <code>&lt;main&gt;</code>—note <strong>which ancestor</strong> must not have <code>overflow: hidden</code> or sticky breaks.</li>\n<li><strong>Checkpoint:</strong> In DevTools <strong>3D view</strong> (if available) or Layers panel, describe what changed after the fix.</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p>Visual result matches the lab goal; you can explain <strong>which file</strong> controlled each major aspect.</p>\n<h4>Common mistakes</h4>\n<p>Editing the wrong CSS file path in <code>&lt;link href&gt;</code>—always verify with Network tab (200 on CSS).</p>\n<h4>Going further</h4>\n<p>Introduce a <code>?v=1</code> query on your stylesheet link when cache fights you—explain tradeoffs.</p>\n<hr>\n</div>","labs-kali-c-09":"<p class=\"lh-tg-source\"><strong>Lab C-09 — Print stylesheet</strong> · <span class=\"lh-tg-chapter\">Part 2 — CSS (Apache2 on Kali)</span> · Lesson <code>labs-kali-c-09</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab C-09 — Print stylesheet</h3>\n<p><strong>Suggested pace:</strong> 60–95 minutes</p>\n<p><strong>Goal:</strong> Craft a print-specific stylesheet.</p>\n<h4>Why this lab exists</h4>\n<p>CSS in browser devtools is fast—but <strong>Apache-backed files</strong> train deployment thinking: paths, cache busting during edits, and real multi-file projects.</p>\n<h4>Background</h4>\n<p>On Debian-based systems (including Kali), <strong>Apache</strong> often uses <strong><code>/var/www/html</code></strong> as the default <strong>DocumentRoot</strong>: files there map to URLs on port 80. Configuration lives under <strong><code>/etc/apache2/</code></strong>; logs under <strong><code>/var/log/apache2/</code></strong>. Editing files as root in the web root is normal for class labs; in production you would use deployment users, permissions, and CI instead.</p>\n<p>All CSS labs assume files under <strong><code>/var/www/html/css-lab/</code></strong>. Create that folder once: <code>sudo mkdir -p /var/www/html/css-lab/styles</code>. Use <code>sudo nano</code> or sync from <code>~/labs/css</code> if you prefer editing in your home directory then copying—<strong>document your workflow</strong> in notes.</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo mkdir -p /var/www/html/css-lab/styles\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Ensures lab directory tree exists.</li>\n<li><strong>Why we use it here:</strong> Avoids permission errors mid-lab.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/css-lab/index.html</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/css-lab/</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/css-lab</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/css-lab/…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>Print stylesheet</strong></p>\n<ul>\n<li>Add <code>@media print</code> rules: hide nav, remove background colors that waste ink, set <code>body { color: #000; background: #fff }</code>, widen content.</li>\n<li>Use <code>break-inside: avoid</code> on headings paired with following paragraphs where needed.</li>\n<li><strong>Checkpoint:</strong> Print to PDF; open PDF and verify page breaks are acceptable.</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p>Visual result matches the lab goal; you can explain <strong>which file</strong> controlled each major aspect.</p>\n<h4>Common mistakes</h4>\n<p>Editing the wrong CSS file path in <code>&lt;link href&gt;</code>—always verify with Network tab (200 on CSS).</p>\n<h4>Going further</h4>\n<p>Introduce a <code>?v=1</code> query on your stylesheet link when cache fights you—explain tradeoffs.</p>\n<hr>\n</div>","labs-kali-c-10":"<p class=\"lh-tg-source\"><strong>Lab C-10 — BEM-style naming refactor</strong> · <span class=\"lh-tg-chapter\">Part 2 — CSS (Apache2 on Kali)</span> · Lesson <code>labs-kali-c-10</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab C-10 — BEM-style naming refactor</h3>\n<p><strong>Suggested pace:</strong> 60–95 minutes</p>\n<p><strong>Goal:</strong> Refactor messy class names to BEM-like clarity without visual change.</p>\n<h4>Why this lab exists</h4>\n<p>CSS in browser devtools is fast—but <strong>Apache-backed files</strong> train deployment thinking: paths, cache busting during edits, and real multi-file projects.</p>\n<h4>Background</h4>\n<p>On Debian-based systems (including Kali), <strong>Apache</strong> often uses <strong><code>/var/www/html</code></strong> as the default <strong>DocumentRoot</strong>: files there map to URLs on port 80. Configuration lives under <strong><code>/etc/apache2/</code></strong>; logs under <strong><code>/var/log/apache2/</code></strong>. Editing files as root in the web root is normal for class labs; in production you would use deployment users, permissions, and CI instead.</p>\n<p>All CSS labs assume files under <strong><code>/var/www/html/css-lab/</code></strong>. Create that folder once: <code>sudo mkdir -p /var/www/html/css-lab/styles</code>. Use <code>sudo nano</code> or sync from <code>~/labs/css</code> if you prefer editing in your home directory then copying—<strong>document your workflow</strong> in notes.</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo mkdir -p /var/www/html/css-lab/styles\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Ensures lab directory tree exists.</li>\n<li><strong>Why we use it here:</strong> Avoids permission errors mid-lab.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/css-lab/index.html</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/css-lab/</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/css-lab</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/css-lab/…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>BEM-style refactor</strong></p>\n<ul>\n<li>Start from a deliberately messy <code>messy.html</code> + <code>messy.css</code> (create them with vague class names like <code>box1</code>, <code>red</code>).</li>\n<li>Refactor to <strong>block__element--modifier</strong> style names <strong>without</strong> changing the visual result (pixel-approximate is fine).</li>\n<li><strong>Checkpoint:</strong> Side-by-side before/after screenshot or description of naming only.</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p>Visual result matches the lab goal; you can explain <strong>which file</strong> controlled each major aspect.</p>\n<h4>Common mistakes</h4>\n<p>Editing the wrong CSS file path in <code>&lt;link href&gt;</code>—always verify with Network tab (200 on CSS).</p>\n<h4>Going further</h4>\n<p>Introduce a <code>?v=1</code> query on your stylesheet link when cache fights you—explain tradeoffs.</p>\n<hr>\n</div>","labs-kali-c-11":"<p class=\"lh-tg-source\"><strong>Lab C-11 — Responsive images with `srcset` / sizes</strong> · <span class=\"lh-tg-chapter\">Part 2 — CSS (Apache2 on Kali)</span> · Lesson <code>labs-kali-c-11</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab C-11 — Responsive images with <code>srcset</code> / sizes</h3>\n<p><strong>Suggested pace:</strong> 60–95 minutes</p>\n<p><strong>Goal:</strong> Pair HTML responsive images with CSS that supports layout.</p>\n<h4>Why this lab exists</h4>\n<p>CSS in browser devtools is fast—but <strong>Apache-backed files</strong> train deployment thinking: paths, cache busting during edits, and real multi-file projects.</p>\n<h4>Background</h4>\n<p>On Debian-based systems (including Kali), <strong>Apache</strong> often uses <strong><code>/var/www/html</code></strong> as the default <strong>DocumentRoot</strong>: files there map to URLs on port 80. Configuration lives under <strong><code>/etc/apache2/</code></strong>; logs under <strong><code>/var/log/apache2/</code></strong>. Editing files as root in the web root is normal for class labs; in production you would use deployment users, permissions, and CI instead.</p>\n<p>All CSS labs assume files under <strong><code>/var/www/html/css-lab/</code></strong>. Create that folder once: <code>sudo mkdir -p /var/www/html/css-lab/styles</code>. Use <code>sudo nano</code> or sync from <code>~/labs/css</code> if you prefer editing in your home directory then copying—<strong>document your workflow</strong> in notes.</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo mkdir -p /var/www/html/css-lab/styles\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Ensures lab directory tree exists.</li>\n<li><strong>Why we use it here:</strong> Avoids permission errors mid-lab.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/css-lab/index.html</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/css-lab/</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/css-lab</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/css-lab/…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>srcset / sizes with layout CSS</strong></p>\n<ul>\n<li>Pair with prior HTML image assets. In HTML, add <code>srcset</code> widths and a thoughtful <code>sizes</code> attribute describing layout breakpoints.</li>\n<li>In CSS, constrain the <code>&lt;img&gt;</code> with <code>max-width:100%; height:auto</code> and a subtle <code>object-fit</code> if cropping applies.</li>\n<li><strong>Checkpoint:</strong> In Network, show which downloaded width you got at two viewport sizes.</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p>Visual result matches the lab goal; you can explain <strong>which file</strong> controlled each major aspect.</p>\n<h4>Common mistakes</h4>\n<p>Editing the wrong CSS file path in <code>&lt;link href&gt;</code>—always verify with Network tab (200 on CSS).</p>\n<h4>Going further</h4>\n<p>Introduce a <code>?v=1</code> query on your stylesheet link when cache fights you—explain tradeoffs.</p>\n<hr>\n</div>","labs-kali-c-12":"<p class=\"lh-tg-source\"><strong>Lab C-12 — CSS layers (`@layer`) experiment</strong> · <span class=\"lh-tg-chapter\">Part 2 — CSS (Apache2 on Kali)</span> · Lesson <code>labs-kali-c-12</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab C-12 — CSS layers (<code>@layer</code>) experiment</h3>\n<p><strong>Suggested pace:</strong> 60–95 minutes</p>\n<p><strong>Goal:</strong> Demonstrate cascade control with @layer.</p>\n<h4>Why this lab exists</h4>\n<p>CSS in browser devtools is fast—but <strong>Apache-backed files</strong> train deployment thinking: paths, cache busting during edits, and real multi-file projects.</p>\n<h4>Background</h4>\n<p>On Debian-based systems (including Kali), <strong>Apache</strong> often uses <strong><code>/var/www/html</code></strong> as the default <strong>DocumentRoot</strong>: files there map to URLs on port 80. Configuration lives under <strong><code>/etc/apache2/</code></strong>; logs under <strong><code>/var/log/apache2/</code></strong>. Editing files as root in the web root is normal for class labs; in production you would use deployment users, permissions, and CI instead.</p>\n<p>All CSS labs assume files under <strong><code>/var/www/html/css-lab/</code></strong>. Create that folder once: <code>sudo mkdir -p /var/www/html/css-lab/styles</code>. Use <code>sudo nano</code> or sync from <code>~/labs/css</code> if you prefer editing in your home directory then copying—<strong>document your workflow</strong> in notes.</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo mkdir -p /var/www/html/css-lab/styles\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Ensures lab directory tree exists.</li>\n<li><strong>Why we use it here:</strong> Avoids permission errors mid-lab.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/css-lab/index.html</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/css-lab/</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/css-lab</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/css-lab/…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong><code>@layer</code> cascade experiment</strong></p>\n<ul>\n<li>Declare <code>@layer base, utilities;</code> then put conflicting rules in different layers—demonstrate that <strong>layer order</strong> beats source order.</li>\n<li>Add one unlayered rule and explain how it interacts (spec reading: unlayered vs layered).</li>\n<li><strong>Checkpoint:</strong> In DevTools Styles, show which layer name appears next to the winning rule.</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p>Visual result matches the lab goal; you can explain <strong>which file</strong> controlled each major aspect.</p>\n<h4>Common mistakes</h4>\n<p>Editing the wrong CSS file path in <code>&lt;link href&gt;</code>—always verify with Network tab (200 on CSS).</p>\n<h4>Going further</h4>\n<p>Introduce a <code>?v=1</code> query on your stylesheet link when cache fights you—explain tradeoffs.</p>\n<hr>\n</div>","labs-kali-j-01":"<p class=\"lh-tg-source\"><strong>Lab J-01 — Strict mode and modules</strong> · <span class=\"lh-tg-chapter\">Part 3 — JavaScript (Apache2 on Kali)</span> · Lesson <code>labs-kali-j-01</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab J-01 — Strict mode and modules</h3>\n<p><strong>Suggested pace:</strong> 70–110 minutes</p>\n<p><strong>Goal:</strong> Use ES modules with <code>import</code>/<code>export</code> behind Apache.</p>\n<h4>Why this lab exists</h4>\n<p>Browser JS labs on a real server highlight <strong>module paths</strong>, <strong>MIME types</strong>, and caching—issues you rarely see in a single-file playground.</p>\n<h4>Background</h4>\n<p>On Debian-based systems (including Kali), <strong>Apache</strong> often uses <strong><code>/var/www/html</code></strong> as the default <strong>DocumentRoot</strong>: files there map to URLs on port 80. Configuration lives under <strong><code>/etc/apache2/</code></strong>; logs under <strong><code>/var/log/apache2/</code></strong>. Editing files as root in the web root is normal for class labs; in production you would use deployment users, permissions, and CI instead.</p>\n<p>JavaScript runs in the browser under <strong>origin rules</strong> (scheme/host/port). Serving from <code>http://127.0.0.1</code> keeps examples predictable. Use <strong>DevTools Console</strong> for errors—read stack traces slowly.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/js-lab/index.html</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/js-lab/</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/js-lab</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/js-lab/…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>ES modules under Apache</strong></p>\n<ul>\n<li>Create <code>/var/www/html/js-lab/index.html</code> with <code>&lt;script type=&quot;module&quot; src=&quot;./app.js&quot;&gt;&lt;/script&gt;</code> (note <code>type=&quot;module&quot;</code>).</li>\n<li>In <code>app.js</code>, <code>import { helper } from &#39;./utils.js&#39;</code> and call it. In <code>utils.js</code>, <code>export function helper(){…}</code>.</li>\n<li>Open DevTools <strong>Network</strong>: confirm <code>app.js</code> loads with MIME type <strong>JavaScript</strong> and that <strong>utils.js</strong> is a second request.</li>\n<li><strong>Checkpoint:</strong> Change a string in <code>utils.js</code>, hard refresh, confirm the change—explain <strong>caching</strong> if it does not appear.</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p>Behavior matches goal; console clean; you can narrate the <strong>data flow</strong>.</p>\n<h4>Common mistakes</h4>\n<p>MIME/type errors for <code>.js</code> modules—ensure Apache serves JavaScript as <code>application/javascript</code> (default on Kali).</p>\n<h4>Going further</h4>\n<p>Try splitting modules further and observe caching headers (conceptual).</p>\n<hr>\n</div>","labs-kali-j-02":"<p class=\"lh-tg-source\"><strong>Lab J-02 — DOM events and delegation</strong> · <span class=\"lh-tg-chapter\">Part 3 — JavaScript (Apache2 on Kali)</span> · Lesson <code>labs-kali-j-02</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab J-02 — DOM events and delegation</h3>\n<p><strong>Suggested pace:</strong> 70–110 minutes</p>\n<p><strong>Goal:</strong> Handle dynamic rows with event delegation.</p>\n<h4>Why this lab exists</h4>\n<p>Browser JS labs on a real server highlight <strong>module paths</strong>, <strong>MIME types</strong>, and caching—issues you rarely see in a single-file playground.</p>\n<h4>Background</h4>\n<p>On Debian-based systems (including Kali), <strong>Apache</strong> often uses <strong><code>/var/www/html</code></strong> as the default <strong>DocumentRoot</strong>: files there map to URLs on port 80. Configuration lives under <strong><code>/etc/apache2/</code></strong>; logs under <strong><code>/var/log/apache2/</code></strong>. Editing files as root in the web root is normal for class labs; in production you would use deployment users, permissions, and CI instead.</p>\n<p>JavaScript runs in the browser under <strong>origin rules</strong> (scheme/host/port). Serving from <code>http://127.0.0.1</code> keeps examples predictable. Use <strong>DevTools Console</strong> for errors—read stack traces slowly.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/js-lab/index.html</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/js-lab/</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/js-lab</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/js-lab/…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>Event delegation</strong></p>\n<ul>\n<li>Build a small table of “tickets.” Add <strong>one</strong> click listener on <code>&lt;tbody&gt;</code> (or the table) and use <code>event.target.closest(&#39;button&#39;)</code> to detect which row’s button fired.</li>\n<li>Add a row dynamically with <code>insertRow</code> and confirm <strong>no new listeners</strong> were required.</li>\n<li><strong>Checkpoint:</strong> Explain memory and maintenance benefits in 3–5 sentences.</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p>Behavior matches goal; console clean; you can narrate the <strong>data flow</strong>.</p>\n<h4>Common mistakes</h4>\n<p>MIME/type errors for <code>.js</code> modules—ensure Apache serves JavaScript as <code>application/javascript</code> (default on Kali).</p>\n<h4>Going further</h4>\n<p>Try splitting modules further and observe caching headers (conceptual).</p>\n<hr>\n</div>","labs-kali-j-03":"<p class=\"lh-tg-source\"><strong>Lab J-03 — Form validation (client-side)</strong> · <span class=\"lh-tg-chapter\">Part 3 — JavaScript (Apache2 on Kali)</span> · Lesson <code>labs-kali-j-03</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab J-03 — Form validation (client-side)</h3>\n<p><strong>Suggested pace:</strong> 70–110 minutes</p>\n<p><strong>Goal:</strong> Use the Constraint Validation API with custom messages.</p>\n<h4>Why this lab exists</h4>\n<p>Browser JS labs on a real server highlight <strong>module paths</strong>, <strong>MIME types</strong>, and caching—issues you rarely see in a single-file playground.</p>\n<h4>Background</h4>\n<p>On Debian-based systems (including Kali), <strong>Apache</strong> often uses <strong><code>/var/www/html</code></strong> as the default <strong>DocumentRoot</strong>: files there map to URLs on port 80. Configuration lives under <strong><code>/etc/apache2/</code></strong>; logs under <strong><code>/var/log/apache2/</code></strong>. Editing files as root in the web root is normal for class labs; in production you would use deployment users, permissions, and CI instead.</p>\n<p>JavaScript runs in the browser under <strong>origin rules</strong> (scheme/host/port). Serving from <code>http://127.0.0.1</code> keeps examples predictable. Use <strong>DevTools Console</strong> for errors—read stack traces slowly.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/js-lab/index.html</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/js-lab/</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/js-lab</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/js-lab/…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>Constraint Validation API</strong></p>\n<ul>\n<li>Build a form with <code>required</code>, <code>pattern</code>, and <code>type=&quot;email&quot;</code>. On <code>submit</code>, call <code>reportValidity()</code> and use <code>setCustomValidity</code> for cross-field rules (e.g. password confirmation).</li>\n<li>Style <code>:invalid</code> / <code>:user-invalid</code> carefully so errors are visible but not screaming.</li>\n<li><strong>Checkpoint:</strong> Show both a passing and failing submit in screenshots or console logs.</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p>Behavior matches goal; console clean; you can narrate the <strong>data flow</strong>.</p>\n<h4>Common mistakes</h4>\n<p>MIME/type errors for <code>.js</code> modules—ensure Apache serves JavaScript as <code>application/javascript</code> (default on Kali).</p>\n<h4>Going further</h4>\n<p>Try splitting modules further and observe caching headers (conceptual).</p>\n<hr>\n</div>","labs-kali-j-04":"<p class=\"lh-tg-source\"><strong>Lab J-04 — Fetch + JSON</strong> · <span class=\"lh-tg-chapter\">Part 3 — JavaScript (Apache2 on Kali)</span> · Lesson <code>labs-kali-j-04</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab J-04 — Fetch + JSON</h3>\n<p><strong>Suggested pace:</strong> 70–110 minutes</p>\n<p><strong>Goal:</strong> Fetch remote JSON and render cards with error UI.</p>\n<h4>Why this lab exists</h4>\n<p>Browser JS labs on a real server highlight <strong>module paths</strong>, <strong>MIME types</strong>, and caching—issues you rarely see in a single-file playground.</p>\n<h4>Background</h4>\n<p>On Debian-based systems (including Kali), <strong>Apache</strong> often uses <strong><code>/var/www/html</code></strong> as the default <strong>DocumentRoot</strong>: files there map to URLs on port 80. Configuration lives under <strong><code>/etc/apache2/</code></strong>; logs under <strong><code>/var/log/apache2/</code></strong>. Editing files as root in the web root is normal for class labs; in production you would use deployment users, permissions, and CI instead.</p>\n<p>JavaScript runs in the browser under <strong>origin rules</strong> (scheme/host/port). Serving from <code>http://127.0.0.1</code> keeps examples predictable. Use <strong>DevTools Console</strong> for errors—read stack traces slowly.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/js-lab/index.html</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/js-lab/</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/js-lab</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/js-lab/…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>fetch + JSON</strong></p>\n<ul>\n<li>Use <code>fetch(&#39;https://jsonplaceholder.typicode.com/posts?_limit=6&#39;)</code> (read-only). Parse JSON, render cards in the DOM.</li>\n<li>Simulate failure: temporarily break the URL and show a <strong>friendly</strong> in-page error with retry button.</li>\n<li><strong>Checkpoint:</strong> Capture Network tab for success vs failure.</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p>Behavior matches goal; console clean; you can narrate the <strong>data flow</strong>.</p>\n<h4>Common mistakes</h4>\n<p>MIME/type errors for <code>.js</code> modules—ensure Apache serves JavaScript as <code>application/javascript</code> (default on Kali).</p>\n<h4>Going further</h4>\n<p>Try splitting modules further and observe caching headers (conceptual).</p>\n<hr>\n</div>","labs-kali-j-05":"<p class=\"lh-tg-source\"><strong>Lab J-05 — Async patterns</strong> · <span class=\"lh-tg-chapter\">Part 3 — JavaScript (Apache2 on Kali)</span> · Lesson <code>labs-kali-j-05</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab J-05 — Async patterns</h3>\n<p><strong>Suggested pace:</strong> 70–110 minutes</p>\n<p><strong>Goal:</strong> Combine async/await, Promise.all, and AbortController timeout.</p>\n<h4>Why this lab exists</h4>\n<p>Browser JS labs on a real server highlight <strong>module paths</strong>, <strong>MIME types</strong>, and caching—issues you rarely see in a single-file playground.</p>\n<h4>Background</h4>\n<p>On Debian-based systems (including Kali), <strong>Apache</strong> often uses <strong><code>/var/www/html</code></strong> as the default <strong>DocumentRoot</strong>: files there map to URLs on port 80. Configuration lives under <strong><code>/etc/apache2/</code></strong>; logs under <strong><code>/var/log/apache2/</code></strong>. Editing files as root in the web root is normal for class labs; in production you would use deployment users, permissions, and CI instead.</p>\n<p>JavaScript runs in the browser under <strong>origin rules</strong> (scheme/host/port). Serving from <code>http://127.0.0.1</code> keeps examples predictable. Use <strong>DevTools Console</strong> for errors—read stack traces slowly.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/js-lab/index.html</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/js-lab/</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/js-lab</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/js-lab/…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>async/await, Promise.all, AbortController</strong></p>\n<ul>\n<li>Fetch two endpoints in parallel with <code>Promise.all</code>. Wrap in <code>try/catch</code>.</li>\n<li>Implement a <strong>timeout</strong> using <code>AbortController</code> + <code>setTimeout</code> that aborts the request and surfaces “took too long.”</li>\n<li><strong>Checkpoint:</strong> Paste the <code>finally</code> block you used to clear timers.</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p>Behavior matches goal; console clean; you can narrate the <strong>data flow</strong>.</p>\n<h4>Common mistakes</h4>\n<p>MIME/type errors for <code>.js</code> modules—ensure Apache serves JavaScript as <code>application/javascript</code> (default on Kali).</p>\n<h4>Going further</h4>\n<p>Try splitting modules further and observe caching headers (conceptual).</p>\n<hr>\n</div>","labs-kali-j-06":"<p class=\"lh-tg-source\"><strong>Lab J-06 — Local storage “study flashcards”</strong> · <span class=\"lh-tg-chapter\">Part 3 — JavaScript (Apache2 on Kali)</span> · Lesson <code>labs-kali-j-06</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab J-06 — Local storage “study flashcards”</h3>\n<p><strong>Suggested pace:</strong> 70–110 minutes</p>\n<p><strong>Goal:</strong> Persist flashcards in localStorage with import/export.</p>\n<h4>Why this lab exists</h4>\n<p>Browser JS labs on a real server highlight <strong>module paths</strong>, <strong>MIME types</strong>, and caching—issues you rarely see in a single-file playground.</p>\n<h4>Background</h4>\n<p>On Debian-based systems (including Kali), <strong>Apache</strong> often uses <strong><code>/var/www/html</code></strong> as the default <strong>DocumentRoot</strong>: files there map to URLs on port 80. Configuration lives under <strong><code>/etc/apache2/</code></strong>; logs under <strong><code>/var/log/apache2/</code></strong>. Editing files as root in the web root is normal for class labs; in production you would use deployment users, permissions, and CI instead.</p>\n<p>JavaScript runs in the browser under <strong>origin rules</strong> (scheme/host/port). Serving from <code>http://127.0.0.1</code> keeps examples predictable. Use <strong>DevTools Console</strong> for errors—read stack traces slowly.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/js-lab/index.html</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/js-lab/</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/js-lab</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/js-lab/…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>localStorage flashcards</strong></p>\n<ul>\n<li>Model cards as an array of objects <code>{ front, back, id }</code>. Serialize with <code>JSON.stringify</code>.</li>\n<li>Implement add, edit, delete, and <strong>Export JSON</strong> / <strong>Import JSON</strong> (with <code>try/catch</code> on parse).</li>\n<li><strong>Checkpoint:</strong> Show your exported JSON file snippet in the report.</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p>Behavior matches goal; console clean; you can narrate the <strong>data flow</strong>.</p>\n<h4>Common mistakes</h4>\n<p>MIME/type errors for <code>.js</code> modules—ensure Apache serves JavaScript as <code>application/javascript</code> (default on Kali).</p>\n<h4>Going further</h4>\n<p>Try splitting modules further and observe caching headers (conceptual).</p>\n<hr>\n</div>","labs-kali-j-07":"<p class=\"lh-tg-source\"><strong>Lab J-07 — Simple router (hash-based)</strong> · <span class=\"lh-tg-chapter\">Part 3 — JavaScript (Apache2 on Kali)</span> · Lesson <code>labs-kali-j-07</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab J-07 — Simple router (hash-based)</h3>\n<p><strong>Suggested pace:</strong> 70–110 minutes</p>\n<p><strong>Goal:</strong> Build hash routing with back/forward behavior.</p>\n<h4>Why this lab exists</h4>\n<p>Browser JS labs on a real server highlight <strong>module paths</strong>, <strong>MIME types</strong>, and caching—issues you rarely see in a single-file playground.</p>\n<h4>Background</h4>\n<p>On Debian-based systems (including Kali), <strong>Apache</strong> often uses <strong><code>/var/www/html</code></strong> as the default <strong>DocumentRoot</strong>: files there map to URLs on port 80. Configuration lives under <strong><code>/etc/apache2/</code></strong>; logs under <strong><code>/var/log/apache2/</code></strong>. Editing files as root in the web root is normal for class labs; in production you would use deployment users, permissions, and CI instead.</p>\n<p>JavaScript runs in the browser under <strong>origin rules</strong> (scheme/host/port). Serving from <code>http://127.0.0.1</code> keeps examples predictable. Use <strong>DevTools Console</strong> for errors—read stack traces slowly.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/js-lab/index.html</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/js-lab/</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/js-lab</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/js-lab/…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>Hash router</strong></p>\n<ul>\n<li>Listen to <code>hashchange</code> and <code>load</code>. Parse <code>location.hash</code> (e.g. <code>#/about</code>) and swap sections with <code>hidden</code> or class toggles.</li>\n<li>Ensure <strong>back/forward</strong> updates the visible section without full reload.</li>\n<li><strong>Checkpoint:</strong> List two limitations of hash routing vs History API.</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p>Behavior matches goal; console clean; you can narrate the <strong>data flow</strong>.</p>\n<h4>Common mistakes</h4>\n<p>MIME/type errors for <code>.js</code> modules—ensure Apache serves JavaScript as <code>application/javascript</code> (default on Kali).</p>\n<h4>Going further</h4>\n<p>Try splitting modules further and observe caching headers (conceptual).</p>\n<hr>\n</div>","labs-kali-j-08":"<p class=\"lh-tg-source\"><strong>Lab J-08 — Web Crypto (non-sensitive demo)</strong> · <span class=\"lh-tg-chapter\">Part 3 — JavaScript (Apache2 on Kali)</span> · Lesson <code>labs-kali-j-08</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab J-08 — Web Crypto (non-sensitive demo)</h3>\n<p><strong>Suggested pace:</strong> 70–110 minutes</p>\n<p><strong>Goal:</strong> Compute SHA-256 of a string in the browser—conceptual only.</p>\n<h4>Why this lab exists</h4>\n<p>Browser JS labs on a real server highlight <strong>module paths</strong>, <strong>MIME types</strong>, and caching—issues you rarely see in a single-file playground.</p>\n<h4>Background</h4>\n<p>On Debian-based systems (including Kali), <strong>Apache</strong> often uses <strong><code>/var/www/html</code></strong> as the default <strong>DocumentRoot</strong>: files there map to URLs on port 80. Configuration lives under <strong><code>/etc/apache2/</code></strong>; logs under <strong><code>/var/log/apache2/</code></strong>. Editing files as root in the web root is normal for class labs; in production you would use deployment users, permissions, and CI instead.</p>\n<p>JavaScript runs in the browser under <strong>origin rules</strong> (scheme/host/port). Serving from <code>http://127.0.0.1</code> keeps examples predictable. Use <strong>DevTools Console</strong> for errors—read stack traces slowly.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/js-lab/index.html</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/js-lab/</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/js-lab</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/js-lab/…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>Web Crypto SHA-256 demo</strong></p>\n<ul>\n<li>Take user text, encode as <code>TextEncoder</code>, digest with <code>crypto.subtle.digest(&#39;SHA-256&#39;, …)</code>, convert ArrayBuffer to <strong>hex</strong>.</li>\n<li><strong>Never</strong> present this as password storage—write a paragraph on why salted KDFs (Argon2/bcrypt) are required offline.</li>\n<li><strong>Checkpoint:</strong> Hash the string <code>test</code> and compare to a known vector from docs or <code>openssl dgst -sha256</code>.</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p>Behavior matches goal; console clean; you can narrate the <strong>data flow</strong>.</p>\n<h4>Common mistakes</h4>\n<p>MIME/type errors for <code>.js</code> modules—ensure Apache serves JavaScript as <code>application/javascript</code> (default on Kali).</p>\n<h4>Going further</h4>\n<p>Try splitting modules further and observe caching headers (conceptual).</p>\n<hr>\n</div>","labs-kali-j-09":"<p class=\"lh-tg-source\"><strong>Lab J-09 — Performance: debounce search</strong> · <span class=\"lh-tg-chapter\">Part 3 — JavaScript (Apache2 on Kali)</span> · Lesson <code>labs-kali-j-09</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab J-09 — Performance: debounce search</h3>\n<p><strong>Suggested pace:</strong> 70–110 minutes</p>\n<p><strong>Goal:</strong> Debounce rapid input against a large in-memory list.</p>\n<h4>Why this lab exists</h4>\n<p>Browser JS labs on a real server highlight <strong>module paths</strong>, <strong>MIME types</strong>, and caching—issues you rarely see in a single-file playground.</p>\n<h4>Background</h4>\n<p>On Debian-based systems (including Kali), <strong>Apache</strong> often uses <strong><code>/var/www/html</code></strong> as the default <strong>DocumentRoot</strong>: files there map to URLs on port 80. Configuration lives under <strong><code>/etc/apache2/</code></strong>; logs under <strong><code>/var/log/apache2/</code></strong>. Editing files as root in the web root is normal for class labs; in production you would use deployment users, permissions, and CI instead.</p>\n<p>JavaScript runs in the browser under <strong>origin rules</strong> (scheme/host/port). Serving from <code>http://127.0.0.1</code> keeps examples predictable. Use <strong>DevTools Console</strong> for errors—read stack traces slowly.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/js-lab/index.html</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/js-lab/</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/js-lab</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/js-lab/…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>Debounced search</strong></p>\n<ul>\n<li>Generate ~500 rows in memory. On <code>input</code>, debounce (e.g. 200ms) filtering.</li>\n<li>Log <strong>how many</strong> filter passes ran vs keystrokes to prove debounce works.</li>\n<li><strong>Checkpoint:</strong> Describe Big-O of naive filter vs indexed approach (conceptual).</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p>Behavior matches goal; console clean; you can narrate the <strong>data flow</strong>.</p>\n<h4>Common mistakes</h4>\n<p>MIME/type errors for <code>.js</code> modules—ensure Apache serves JavaScript as <code>application/javascript</code> (default on Kali).</p>\n<h4>Going further</h4>\n<p>Try splitting modules further and observe caching headers (conceptual).</p>\n<hr>\n</div>","labs-kali-j-10":"<p class=\"lh-tg-source\"><strong>Lab J-10 — Unit tests (optional Node) or console harness</strong> · <span class=\"lh-tg-chapter\">Part 3 — JavaScript (Apache2 on Kali)</span> · Lesson <code>labs-kali-j-10</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab J-10 — Unit tests (optional Node) or console harness</h3>\n<p><strong>Suggested pace:</strong> 70–110 minutes</p>\n<p><strong>Goal:</strong> Add automated or manual tests for pure functions.</p>\n<h4>Why this lab exists</h4>\n<p>Browser JS labs on a real server highlight <strong>module paths</strong>, <strong>MIME types</strong>, and caching—issues you rarely see in a single-file playground.</p>\n<h4>Background</h4>\n<p>On Debian-based systems (including Kali), <strong>Apache</strong> often uses <strong><code>/var/www/html</code></strong> as the default <strong>DocumentRoot</strong>: files there map to URLs on port 80. Configuration lives under <strong><code>/etc/apache2/</code></strong>; logs under <strong><code>/var/log/apache2/</code></strong>. Editing files as root in the web root is normal for class labs; in production you would use deployment users, permissions, and CI instead.</p>\n<p>JavaScript runs in the browser under <strong>origin rules</strong> (scheme/host/port). Serving from <code>http://127.0.0.1</code> keeps examples predictable. Use <strong>DevTools Console</strong> for errors—read stack traces slowly.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/js-lab/index.html</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/js-lab/</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/js-lab</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/js-lab/…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>Tests: Vitest/Jest or console harness</strong></p>\n<ul>\n<li>If Node is available, add one tiny <code>*.test.js</code> for a pure function. Otherwise, write <code>console.assert</code> checks in a <code>tests.html</code> page.</li>\n<li><strong>Checkpoint:</strong> Paste green output or explain one failing assertion you fixed.</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p>Behavior matches goal; console clean; you can narrate the <strong>data flow</strong>.</p>\n<h4>Common mistakes</h4>\n<p>MIME/type errors for <code>.js</code> modules—ensure Apache serves JavaScript as <code>application/javascript</code> (default on Kali).</p>\n<h4>Going further</h4>\n<p>Try splitting modules further and observe caching headers (conceptual).</p>\n<hr>\n</div>","labs-kali-j-11":"<p class=\"lh-tg-source\"><strong>Lab J-11 — Web Workers</strong> · <span class=\"lh-tg-chapter\">Part 3 — JavaScript (Apache2 on Kali)</span> · Lesson <code>labs-kali-j-11</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab J-11 — Web Workers</h3>\n<p><strong>Suggested pace:</strong> 70–110 minutes</p>\n<p><strong>Goal:</strong> Move heavy work off the main thread.</p>\n<h4>Why this lab exists</h4>\n<p>Browser JS labs on a real server highlight <strong>module paths</strong>, <strong>MIME types</strong>, and caching—issues you rarely see in a single-file playground.</p>\n<h4>Background</h4>\n<p>On Debian-based systems (including Kali), <strong>Apache</strong> often uses <strong><code>/var/www/html</code></strong> as the default <strong>DocumentRoot</strong>: files there map to URLs on port 80. Configuration lives under <strong><code>/etc/apache2/</code></strong>; logs under <strong><code>/var/log/apache2/</code></strong>. Editing files as root in the web root is normal for class labs; in production you would use deployment users, permissions, and CI instead.</p>\n<p>JavaScript runs in the browser under <strong>origin rules</strong> (scheme/host/port). Serving from <code>http://127.0.0.1</code> keeps examples predictable. Use <strong>DevTools Console</strong> for errors—read stack traces slowly.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/js-lab/index.html</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/js-lab/</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/js-lab</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/js-lab/…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>Web Worker</strong></p>\n<ul>\n<li>Offload a silly CPU loop (sum 1..5e7) to <code>worker.js</code>; postMessage the result. Keep UI responsive (spinner or status text).</li>\n<li><strong>Checkpoint:</strong> Compare time with worker vs blocking main thread (warn user briefly if blocking).</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p>Behavior matches goal; console clean; you can narrate the <strong>data flow</strong>.</p>\n<h4>Common mistakes</h4>\n<p>MIME/type errors for <code>.js</code> modules—ensure Apache serves JavaScript as <code>application/javascript</code> (default on Kali).</p>\n<h4>Going further</h4>\n<p>Try splitting modules further and observe caching headers (conceptual).</p>\n<hr>\n</div>","labs-kali-j-12":"<p class=\"lh-tg-source\"><strong>Lab J-12 — Progressive enhancement</strong> · <span class=\"lh-tg-chapter\">Part 3 — JavaScript (Apache2 on Kali)</span> · Lesson <code>labs-kali-j-12</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab J-12 — Progressive enhancement</h3>\n<p><strong>Suggested pace:</strong> 70–110 minutes</p>\n<p><strong>Goal:</strong> Keep HTML usable without JS; enhance with sorting.</p>\n<h4>Why this lab exists</h4>\n<p>Browser JS labs on a real server highlight <strong>module paths</strong>, <strong>MIME types</strong>, and caching—issues you rarely see in a single-file playground.</p>\n<h4>Background</h4>\n<p>On Debian-based systems (including Kali), <strong>Apache</strong> often uses <strong><code>/var/www/html</code></strong> as the default <strong>DocumentRoot</strong>: files there map to URLs on port 80. Configuration lives under <strong><code>/etc/apache2/</code></strong>; logs under <strong><code>/var/log/apache2/</code></strong>. Editing files as root in the web root is normal for class labs; in production you would use deployment users, permissions, and CI instead.</p>\n<p>JavaScript runs in the browser under <strong>origin rules</strong> (scheme/host/port). Serving from <code>http://127.0.0.1</code> keeps examples predictable. Use <strong>DevTools Console</strong> for errors—read stack traces slowly.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Map the server to the filesystem (15–20 min)</h3>\n<ol>\n<li>Run <code>systemctl status apache2 --no-pager | head -n 20</code>. In your notebook, copy the <strong>Active:</strong> line and note <strong>Main PID</strong>.</li>\n<li>Run <code>sudo apache2ctl -S 2&gt;&amp;1 | head -n 50</code>. Find which vhost answers on <strong>:80</strong> and which <strong>DocumentRoot</strong> applies. Write one sentence: “A request to <code>http://127.0.0.1/js-lab/index.html</code> should read from ___ on disk.”</li>\n<li>Open <code>/etc/apache2/sites-enabled/000-default.conf</code> with <code>sudo nano</code> or <code>sudo less</code> (read-only with <code>less</code>). Locate <code>DocumentRoot</code> and <code>&lt;Directory ...&gt;</code> blocks—<strong>do not save changes</strong> until a lab explicitly asks.</li>\n<li>List the lab folder: <code>sudo ls -la /var/www/html/js-lab/</code>. If it is missing, create it: <code>sudo mkdir -p /var/www/html/js-lab</code>.</li>\n</ol>\n<h3>Phase 2 — Edit → save → reload habit (10 min)</h3>\n<ul>\n<li>Use <code>sudo nano /var/www/html/js-lab/…</code> for small edits. After save, <strong>one wrong character</strong> can break CSS or HTML—re-open the file and confirm your last line is intact.</li>\n<li>In the browser, use <strong>hard refresh</strong> (<code>Ctrl+F5</code>) when styles look “stuck.” Optionally add a <strong>cache-bust</strong> query on <code>&lt;link href=&quot;…?v=2&quot;&gt;</code> and explain why that works.</li>\n</ul>\n<h3>Phase 3 — Core lab work (see focus below)</h3>\n<p><strong>Progressive enhancement</strong></p>\n<ul>\n<li>Build a sortable table: <strong>without JS</strong>, the table reads fine. With JS, add header click handlers that sort rows.</li>\n<li>Ensure sort <strong>announces</strong> visually which column is active (aria-sort optional stretch).</li>\n<li><strong>Checkpoint:</strong> Disable JS in devtools and confirm table still usable.</li>\n</ul>\n<h3>Phase 4 — Verify like someone on-call (15–20 min)</h3>\n<ol>\n<li>In <strong>Firefox or Chromium on the VM</strong>, open your page. Open <strong>DevTools → Network</strong>, disable cache, reload. Confirm <strong>200</strong> on the document and on each CSS/JS asset you expect.</li>\n<li>In <strong>Console</strong>, confirm there are <strong>no red errors</strong>. If there are, read the <strong>file name and line</strong>; fix, reload, repeat.</li>\n<li>From the shell, run <code>curl -sS -D- -o /tmp/lh-body.html http://127.0.0.1/</code> <strong>against your lab URL path</strong> (adjust the URL). Inspect the <strong>first few response headers</strong> (<code>Content-Type</code>, <code>Content-Length</code>).</li>\n<li>Tail the access log while you click: <code>sudo tail -n 20 /var/log/apache2/access.log</code>. Match log lines to your requests.</li>\n</ol>\n<h3>Phase 5 — Lab report (10 min)</h3>\n<ul>\n<li>Write: objective, files touched, <strong>one mistake</strong> you made and how you proved the fix, and <strong>one security or maintenance thought</strong> (e.g. why <code>sudo</code> on web root is OK in class but risky in production).</li>\n</ul>\n<h4>Verify</h4>\n<p>Behavior matches goal; console clean; you can narrate the <strong>data flow</strong>.</p>\n<h4>Common mistakes</h4>\n<p>MIME/type errors for <code>.js</code> modules—ensure Apache serves JavaScript as <code>application/javascript</code> (default on Kali).</p>\n<h4>Going further</h4>\n<p>Try splitting modules further and observe caching headers (conceptual).</p>\n<hr>\n</div>","labs-kali-p-01":"<p class=\"lh-tg-source\"><strong>Lab P-01 — Virtual environments and pip</strong> · <span class=\"lh-tg-chapter\">Part 4 — Python (on Kali)</span> · Lesson <code>labs-kali-p-01</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab P-01 — Virtual environments and pip</h3>\n<p><strong>Suggested pace:</strong> 75–120 minutes</p>\n<p><strong>Goal:</strong> Create a venv and call a safe public HTTP API with requests.</p>\n<h4>Why this lab exists</h4>\n<p>Python on Kali is how automators glue systems together—<strong>read docs</strong>, <strong>type code</strong>, <strong>run tests</strong>, not copy-paste alone.</p>\n<h4>Background</h4>\n<p>Use <strong><code>~/labs/python/</code></strong> for sources. Always activate your venv (<code>source .venv/bin/activate</code>) before <code>pip install</code>. Prefer <strong>absolute imports within small projects</strong> for clarity.</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">cd ~/labs/python &amp;&amp; python3 -m venv .venv &amp;&amp; source .venv/bin/activate\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> <code>python3 -m venv</code> creates an isolated interpreter + site-packages. <code>source</code> applies env vars in the current shell.</li>\n<li><strong>Why we use it here:</strong> Avoids polluting system Python—critical habit.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Isolate Python (15 min)</h3>\n<ol>\n<li><code>cd ~/labs/python</code> — if the folder is missing, <code>mkdir -p ~/labs/python</code>.</li>\n<li>Create or reuse a venv: <code>python3 -m venv .venv &amp;&amp; source .venv/bin/activate</code>. Run <code>which python</code> and confirm it points inside <code>.venv</code>.</li>\n<li>Run <code>python -m pip install --upgrade pip</code> (note <code>python</code> vs <code>python3</code> inside venv).</li>\n</ol>\n<h3>Phase 2 — Project hygiene (10 min)</h3>\n<ul>\n<li>Create today’s folder or branch naming pattern (for example <code>lab-</code> plus the output of <code>date +%F</code> in bash).</li>\n<li>Open <code>README.txt</code> in the lab folder listing: objective, commands you expect to run, rollback (delete venv? restore snapshot?).</li>\n</ul>\n<h3>Phase 3 — Core lab work</h3>\n<p><strong>venv + pip + requests</strong></p>\n<ol>\n<li><code>cd ~/labs/python &amp;&amp; python3 -m venv .venv &amp;&amp; source .venv/bin/activate</code> — confirm prompt shows <code>(.venv)</code>.</li>\n<li><code>python -m pip install --upgrade pip</code> then <code>pip install requests</code>.</li>\n<li>Create <code>hello_requests.py</code> that GETs a <strong>public</strong> JSON endpoint (e.g. ipify) and prints <strong>only</strong> non-sensitive fields.</li>\n<li>Run <code>python -I hello_requests.py</code> once to see isolated flags (<code>-I</code> ignores env—optional exploration).</li>\n</ol>\n<ul>\n<li><strong>Checkpoint:</strong> <code>pip freeze | head</code> in your notes.</li>\n</ul>\n<h3>Phase 4 — Evidence (15 min)</h3>\n<ul>\n<li>Capture <strong>stdout/stderr</strong> for at least two runs (success + intentional failure path).</li>\n<li>If you added files, <code>tree -L 2 ~/labs/python</code> or <code>ls -la</code> in your report.</li>\n</ul>\n<h3>Phase 5 — Retro (10 min)</h3>\n<ul>\n<li>What broke if <code>PATH</code> contained spaces? What broke if you forgot <code>source .venv/bin/activate</code>?</li>\n</ul>\n<h4>Verify</h4>\n<p>Acceptance checklist satisfied; you can explain <strong>each import</strong> you used.</p>\n<h4>Common mistakes</h4>\n<p>Forgetting to activate venv → packages install globally or commands fail mysteriously.</p>\n<h4>Going further</h4>\n<p>Add <code>ruff</code> or <code>black</code> later for style—optional.</p>\n<hr>\n</div>","labs-kali-p-02":"<p class=\"lh-tg-source\"><strong>Lab P-02 — argparse CLI tool</strong> · <span class=\"lh-tg-chapter\">Part 4 — Python (on Kali)</span> · Lesson <code>labs-kali-p-02</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab P-02 — argparse CLI tool</h3>\n<p><strong>Suggested pace:</strong> 75–120 minutes</p>\n<p><strong>Goal:</strong> Build a small CLI that filters log lines.</p>\n<h4>Why this lab exists</h4>\n<p>Python on Kali is how automators glue systems together—<strong>read docs</strong>, <strong>type code</strong>, <strong>run tests</strong>, not copy-paste alone.</p>\n<h4>Background</h4>\n<p>Use <strong><code>~/labs/python/</code></strong> for sources. Always activate your venv (<code>source .venv/bin/activate</code>) before <code>pip install</code>. Prefer <strong>absolute imports within small projects</strong> for clarity.</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">cd ~/labs/python &amp;&amp; python3 -m venv .venv &amp;&amp; source .venv/bin/activate\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> <code>python3 -m venv</code> creates an isolated interpreter + site-packages. <code>source</code> applies env vars in the current shell.</li>\n<li><strong>Why we use it here:</strong> Avoids polluting system Python—critical habit.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Isolate Python (15 min)</h3>\n<ol>\n<li><code>cd ~/labs/python</code> — if the folder is missing, <code>mkdir -p ~/labs/python</code>.</li>\n<li>Create or reuse a venv: <code>python3 -m venv .venv &amp;&amp; source .venv/bin/activate</code>. Run <code>which python</code> and confirm it points inside <code>.venv</code>.</li>\n<li>Run <code>python -m pip install --upgrade pip</code> (note <code>python</code> vs <code>python3</code> inside venv).</li>\n</ol>\n<h3>Phase 2 — Project hygiene (10 min)</h3>\n<ul>\n<li>Create today’s folder or branch naming pattern (for example <code>lab-</code> plus the output of <code>date +%F</code> in bash).</li>\n<li>Open <code>README.txt</code> in the lab folder listing: objective, commands you expect to run, rollback (delete venv? restore snapshot?).</li>\n</ul>\n<h3>Phase 3 — Core lab work</h3>\n<p><strong>argparse log filter</strong></p>\n<ul>\n<li>Scaffold <code>logfmt.py</code> with subcommands or flags: input path, regex pattern, optional <code>--invert-match</code>.</li>\n<li>Read file line-by-line (generator) instead of slurping whole file—explain memory benefit.</li>\n<li><strong>Checkpoint:</strong> Run against a 10k-line synthetic log you create with a loop in shell.</li>\n</ul>\n<h3>Phase 4 — Evidence (15 min)</h3>\n<ul>\n<li>Capture <strong>stdout/stderr</strong> for at least two runs (success + intentional failure path).</li>\n<li>If you added files, <code>tree -L 2 ~/labs/python</code> or <code>ls -la</code> in your report.</li>\n</ul>\n<h3>Phase 5 — Retro (10 min)</h3>\n<ul>\n<li>What broke if <code>PATH</code> contained spaces? What broke if you forgot <code>source .venv/bin/activate</code>?</li>\n</ul>\n<h4>Verify</h4>\n<p>Acceptance checklist satisfied; you can explain <strong>each import</strong> you used.</p>\n<h4>Common mistakes</h4>\n<p>Forgetting to activate venv → packages install globally or commands fail mysteriously.</p>\n<h4>Going further</h4>\n<p>Add <code>ruff</code> or <code>black</code> later for style—optional.</p>\n<hr>\n</div>","labs-kali-p-03":"<p class=\"lh-tg-source\"><strong>Lab P-03 — CSV / JSON conversion</strong> · <span class=\"lh-tg-chapter\">Part 4 — Python (on Kali)</span> · Lesson <code>labs-kali-p-03</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab P-03 — CSV / JSON conversion</h3>\n<p><strong>Suggested pace:</strong> 75–120 minutes</p>\n<p><strong>Goal:</strong> Convert CSV rows to typed JSON objects.</p>\n<h4>Why this lab exists</h4>\n<p>Python on Kali is how automators glue systems together—<strong>read docs</strong>, <strong>type code</strong>, <strong>run tests</strong>, not copy-paste alone.</p>\n<h4>Background</h4>\n<p>Use <strong><code>~/labs/python/</code></strong> for sources. Always activate your venv (<code>source .venv/bin/activate</code>) before <code>pip install</code>. Prefer <strong>absolute imports within small projects</strong> for clarity.</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">cd ~/labs/python &amp;&amp; python3 -m venv .venv &amp;&amp; source .venv/bin/activate\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> <code>python3 -m venv</code> creates an isolated interpreter + site-packages. <code>source</code> applies env vars in the current shell.</li>\n<li><strong>Why we use it here:</strong> Avoids polluting system Python—critical habit.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Isolate Python (15 min)</h3>\n<ol>\n<li><code>cd ~/labs/python</code> — if the folder is missing, <code>mkdir -p ~/labs/python</code>.</li>\n<li>Create or reuse a venv: <code>python3 -m venv .venv &amp;&amp; source .venv/bin/activate</code>. Run <code>which python</code> and confirm it points inside <code>.venv</code>.</li>\n<li>Run <code>python -m pip install --upgrade pip</code> (note <code>python</code> vs <code>python3</code> inside venv).</li>\n</ol>\n<h3>Phase 2 — Project hygiene (10 min)</h3>\n<ul>\n<li>Create today’s folder or branch naming pattern (for example <code>lab-</code> plus the output of <code>date +%F</code> in bash).</li>\n<li>Open <code>README.txt</code> in the lab folder listing: objective, commands you expect to run, rollback (delete venv? restore snapshot?).</li>\n</ul>\n<h3>Phase 3 — Core lab work</h3>\n<p><strong>CSV → JSON with types</strong></p>\n<ul>\n<li>Create <code>inventory.csv</code> with a numeric column. Parse with <code>csv.DictReader</code>.</li>\n<li>Coerce integers safely (<code>try/except ValueError</code> per row); collect bad rows in a list and print a summary at end.</li>\n<li><strong>Checkpoint:</strong> Output <code>inventory.json</code> pretty-printed.</li>\n</ul>\n<h3>Phase 4 — Evidence (15 min)</h3>\n<ul>\n<li>Capture <strong>stdout/stderr</strong> for at least two runs (success + intentional failure path).</li>\n<li>If you added files, <code>tree -L 2 ~/labs/python</code> or <code>ls -la</code> in your report.</li>\n</ul>\n<h3>Phase 5 — Retro (10 min)</h3>\n<ul>\n<li>What broke if <code>PATH</code> contained spaces? What broke if you forgot <code>source .venv/bin/activate</code>?</li>\n</ul>\n<h4>Verify</h4>\n<p>Acceptance checklist satisfied; you can explain <strong>each import</strong> you used.</p>\n<h4>Common mistakes</h4>\n<p>Forgetting to activate venv → packages install globally or commands fail mysteriously.</p>\n<h4>Going further</h4>\n<p>Add <code>ruff</code> or <code>black</code> later for style—optional.</p>\n<hr>\n</div>","labs-kali-p-04":"<p class=\"lh-tg-source\"><strong>Lab P-04 — pathlib batch rename (dry-run)</strong> · <span class=\"lh-tg-chapter\">Part 4 — Python (on Kali)</span> · Lesson <code>labs-kali-p-04</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab P-04 — pathlib batch rename (dry-run)</h3>\n<p><strong>Suggested pace:</strong> 75–120 minutes</p>\n<p><strong>Goal:</strong> Rename files with dry-run default and explicit --apply.</p>\n<h4>Why this lab exists</h4>\n<p>Python on Kali is how automators glue systems together—<strong>read docs</strong>, <strong>type code</strong>, <strong>run tests</strong>, not copy-paste alone.</p>\n<h4>Background</h4>\n<p>Use <strong><code>~/labs/python/</code></strong> for sources. Always activate your venv (<code>source .venv/bin/activate</code>) before <code>pip install</code>. Prefer <strong>absolute imports within small projects</strong> for clarity.</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">cd ~/labs/python &amp;&amp; python3 -m venv .venv &amp;&amp; source .venv/bin/activate\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> <code>python3 -m venv</code> creates an isolated interpreter + site-packages. <code>source</code> applies env vars in the current shell.</li>\n<li><strong>Why we use it here:</strong> Avoids polluting system Python—critical habit.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Isolate Python (15 min)</h3>\n<ol>\n<li><code>cd ~/labs/python</code> — if the folder is missing, <code>mkdir -p ~/labs/python</code>.</li>\n<li>Create or reuse a venv: <code>python3 -m venv .venv &amp;&amp; source .venv/bin/activate</code>. Run <code>which python</code> and confirm it points inside <code>.venv</code>.</li>\n<li>Run <code>python -m pip install --upgrade pip</code> (note <code>python</code> vs <code>python3</code> inside venv).</li>\n</ol>\n<h3>Phase 2 — Project hygiene (10 min)</h3>\n<ul>\n<li>Create today’s folder or branch naming pattern (for example <code>lab-</code> plus the output of <code>date +%F</code> in bash).</li>\n<li>Open <code>README.txt</code> in the lab folder listing: objective, commands you expect to run, rollback (delete venv? restore snapshot?).</li>\n</ul>\n<h3>Phase 3 — Core lab work</h3>\n<p><strong>pathlib rename dry-run</strong></p>\n<ul>\n<li>Write <code>rename_media.py</code> using <code>Path.glob</code>. Default <code>--dry-run</code> prints planned renames; <code>--apply</code> performs them.</li>\n<li>Use <code>Path.rename</code> and catch <code>OSError</code>—log failures without crashing whole batch.</li>\n<li><strong>Checkpoint:</strong> Show dry-run vs apply diff in your report.</li>\n</ul>\n<h3>Phase 4 — Evidence (15 min)</h3>\n<ul>\n<li>Capture <strong>stdout/stderr</strong> for at least two runs (success + intentional failure path).</li>\n<li>If you added files, <code>tree -L 2 ~/labs/python</code> or <code>ls -la</code> in your report.</li>\n</ul>\n<h3>Phase 5 — Retro (10 min)</h3>\n<ul>\n<li>What broke if <code>PATH</code> contained spaces? What broke if you forgot <code>source .venv/bin/activate</code>?</li>\n</ul>\n<h4>Verify</h4>\n<p>Acceptance checklist satisfied; you can explain <strong>each import</strong> you used.</p>\n<h4>Common mistakes</h4>\n<p>Forgetting to activate venv → packages install globally or commands fail mysteriously.</p>\n<h4>Going further</h4>\n<p>Add <code>ruff</code> or <code>black</code> later for style—optional.</p>\n<hr>\n</div>","labs-kali-p-05":"<p class=\"lh-tg-source\"><strong>Lab P-05 — HTTP server (stdlib) vs Apache</strong> · <span class=\"lh-tg-chapter\">Part 4 — Python (on Kali)</span> · Lesson <code>labs-kali-p-05</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab P-05 — HTTP server (stdlib) vs Apache</h3>\n<p><strong>Suggested pace:</strong> 75–120 minutes</p>\n<p><strong>Goal:</strong> Compare Python’s http.server headers to Apache.</p>\n<h4>Why this lab exists</h4>\n<p>Python on Kali is how automators glue systems together—<strong>read docs</strong>, <strong>type code</strong>, <strong>run tests</strong>, not copy-paste alone.</p>\n<h4>Background</h4>\n<p>Use <strong><code>~/labs/python/</code></strong> for sources. Always activate your venv (<code>source .venv/bin/activate</code>) before <code>pip install</code>. Prefer <strong>absolute imports within small projects</strong> for clarity.</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">cd ~/labs/python &amp;&amp; python3 -m venv .venv &amp;&amp; source .venv/bin/activate\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> <code>python3 -m venv</code> creates an isolated interpreter + site-packages. <code>source</code> applies env vars in the current shell.</li>\n<li><strong>Why we use it here:</strong> Avoids polluting system Python—critical habit.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Isolate Python (15 min)</h3>\n<ol>\n<li><code>cd ~/labs/python</code> — if the folder is missing, <code>mkdir -p ~/labs/python</code>.</li>\n<li>Create or reuse a venv: <code>python3 -m venv .venv &amp;&amp; source .venv/bin/activate</code>. Run <code>which python</code> and confirm it points inside <code>.venv</code>.</li>\n<li>Run <code>python -m pip install --upgrade pip</code> (note <code>python</code> vs <code>python3</code> inside venv).</li>\n</ol>\n<h3>Phase 2 — Project hygiene (10 min)</h3>\n<ul>\n<li>Create today’s folder or branch naming pattern (for example <code>lab-</code> plus the output of <code>date +%F</code> in bash).</li>\n<li>Open <code>README.txt</code> in the lab folder listing: objective, commands you expect to run, rollback (delete venv? restore snapshot?).</li>\n</ul>\n<h3>Phase 3 — Core lab work</h3>\n<p><strong>http.server vs Apache</strong></p>\n<ul>\n<li>Run <code>python3 -m http.server 8765 --bind 127.0.0.1 --directory /var/www/html</code> in one terminal.</li>\n<li><code>curl -I http://127.0.0.1:8765/</code> vs <code>curl -I http://127.0.0.1:80/</code>. Compare <strong>Server</strong> and <strong>headers</strong>.</li>\n<li><strong>Checkpoint:</strong> Explain one capability Apache gives that stdlib server does not (vhosts, TLS termination, etc.).</li>\n</ul>\n<h3>Phase 4 — Evidence (15 min)</h3>\n<ul>\n<li>Capture <strong>stdout/stderr</strong> for at least two runs (success + intentional failure path).</li>\n<li>If you added files, <code>tree -L 2 ~/labs/python</code> or <code>ls -la</code> in your report.</li>\n</ul>\n<h3>Phase 5 — Retro (10 min)</h3>\n<ul>\n<li>What broke if <code>PATH</code> contained spaces? What broke if you forgot <code>source .venv/bin/activate</code>?</li>\n</ul>\n<h4>Verify</h4>\n<p>Acceptance checklist satisfied; you can explain <strong>each import</strong> you used.</p>\n<h4>Common mistakes</h4>\n<p>Forgetting to activate venv → packages install globally or commands fail mysteriously.</p>\n<h4>Going further</h4>\n<p>Add <code>ruff</code> or <code>black</code> later for style—optional.</p>\n<hr>\n</div>","labs-kali-p-06":"<p class=\"lh-tg-source\"><strong>Lab P-06 — Socket programming intro</strong> · <span class=\"lh-tg-chapter\">Part 4 — Python (on Kali)</span> · Lesson <code>labs-kali-p-06</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab P-06 — Socket programming intro</h3>\n<p><strong>Suggested pace:</strong> 75–120 minutes</p>\n<p><strong>Goal:</strong> TCP echo on loopback only.</p>\n<h4>Why this lab exists</h4>\n<p>Python on Kali is how automators glue systems together—<strong>read docs</strong>, <strong>type code</strong>, <strong>run tests</strong>, not copy-paste alone.</p>\n<h4>Background</h4>\n<p>Use <strong><code>~/labs/python/</code></strong> for sources. Always activate your venv (<code>source .venv/bin/activate</code>) before <code>pip install</code>. Prefer <strong>absolute imports within small projects</strong> for clarity.</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">cd ~/labs/python &amp;&amp; python3 -m venv .venv &amp;&amp; source .venv/bin/activate\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> <code>python3 -m venv</code> creates an isolated interpreter + site-packages. <code>source</code> applies env vars in the current shell.</li>\n<li><strong>Why we use it here:</strong> Avoids polluting system Python—critical habit.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Isolate Python (15 min)</h3>\n<ol>\n<li><code>cd ~/labs/python</code> — if the folder is missing, <code>mkdir -p ~/labs/python</code>.</li>\n<li>Create or reuse a venv: <code>python3 -m venv .venv &amp;&amp; source .venv/bin/activate</code>. Run <code>which python</code> and confirm it points inside <code>.venv</code>.</li>\n<li>Run <code>python -m pip install --upgrade pip</code> (note <code>python</code> vs <code>python3</code> inside venv).</li>\n</ol>\n<h3>Phase 2 — Project hygiene (10 min)</h3>\n<ul>\n<li>Create today’s folder or branch naming pattern (for example <code>lab-</code> plus the output of <code>date +%F</code> in bash).</li>\n<li>Open <code>README.txt</code> in the lab folder listing: objective, commands you expect to run, rollback (delete venv? restore snapshot?).</li>\n</ul>\n<h3>Phase 3 — Core lab work</h3>\n<p><strong>TCP echo on loopback</strong></p>\n<ul>\n<li>Implement <code>echo_server.py</code> binding <code>127.0.0.1:9090</code> and <code>echo_client.py</code> sending one line.</li>\n<li>Use <code>with conn:</code> context managers; close sockets explicitly in <code>finally</code> if not using contexts.</li>\n<li><strong>Checkpoint:</strong> Show <code>ss -lntp | grep 9090</code> while server runs.</li>\n</ul>\n<h3>Phase 4 — Evidence (15 min)</h3>\n<ul>\n<li>Capture <strong>stdout/stderr</strong> for at least two runs (success + intentional failure path).</li>\n<li>If you added files, <code>tree -L 2 ~/labs/python</code> or <code>ls -la</code> in your report.</li>\n</ul>\n<h3>Phase 5 — Retro (10 min)</h3>\n<ul>\n<li>What broke if <code>PATH</code> contained spaces? What broke if you forgot <code>source .venv/bin/activate</code>?</li>\n</ul>\n<h4>Verify</h4>\n<p>Acceptance checklist satisfied; you can explain <strong>each import</strong> you used.</p>\n<h4>Common mistakes</h4>\n<p>Forgetting to activate venv → packages install globally or commands fail mysteriously.</p>\n<h4>Going further</h4>\n<p>Add <code>ruff</code> or <code>black</code> later for style—optional.</p>\n<hr>\n</div>","labs-kali-p-07":"<p class=\"lh-tg-source\"><strong>Lab P-07 — subprocess safety</strong> · <span class=\"lh-tg-chapter\">Part 4 — Python (on Kali)</span> · Lesson <code>labs-kali-p-07</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab P-07 — subprocess safety</h3>\n<p><strong>Suggested pace:</strong> 75–120 minutes</p>\n<p><strong>Goal:</strong> Run ping via subprocess without shell injection.</p>\n<h4>Why this lab exists</h4>\n<p>Python on Kali is how automators glue systems together—<strong>read docs</strong>, <strong>type code</strong>, <strong>run tests</strong>, not copy-paste alone.</p>\n<h4>Background</h4>\n<p>Use <strong><code>~/labs/python/</code></strong> for sources. Always activate your venv (<code>source .venv/bin/activate</code>) before <code>pip install</code>. Prefer <strong>absolute imports within small projects</strong> for clarity.</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">cd ~/labs/python &amp;&amp; python3 -m venv .venv &amp;&amp; source .venv/bin/activate\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> <code>python3 -m venv</code> creates an isolated interpreter + site-packages. <code>source</code> applies env vars in the current shell.</li>\n<li><strong>Why we use it here:</strong> Avoids polluting system Python—critical habit.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Isolate Python (15 min)</h3>\n<ol>\n<li><code>cd ~/labs/python</code> — if the folder is missing, <code>mkdir -p ~/labs/python</code>.</li>\n<li>Create or reuse a venv: <code>python3 -m venv .venv &amp;&amp; source .venv/bin/activate</code>. Run <code>which python</code> and confirm it points inside <code>.venv</code>.</li>\n<li>Run <code>python -m pip install --upgrade pip</code> (note <code>python</code> vs <code>python3</code> inside venv).</li>\n</ol>\n<h3>Phase 2 — Project hygiene (10 min)</h3>\n<ul>\n<li>Create today’s folder or branch naming pattern (for example <code>lab-</code> plus the output of <code>date +%F</code> in bash).</li>\n<li>Open <code>README.txt</code> in the lab folder listing: objective, commands you expect to run, rollback (delete venv? restore snapshot?).</li>\n</ul>\n<h3>Phase 3 — Core lab work</h3>\n<p><strong>subprocess without shell=True</strong></p>\n<ul>\n<li>Call <code>ping -c 1 127.0.0.1</code> with argument list form. Capture <code>stdout</code>, <code>stderr</code>, <code>returncode</code>.</li>\n<li>Demonstrate <strong>why</strong> <code>shell=True</code> is dangerous with a user-controlled string (theory only—do not execute untrusted input).</li>\n<li><strong>Checkpoint:</strong> Map return code 0 vs non-zero to success/failure in your code.</li>\n</ul>\n<h3>Phase 4 — Evidence (15 min)</h3>\n<ul>\n<li>Capture <strong>stdout/stderr</strong> for at least two runs (success + intentional failure path).</li>\n<li>If you added files, <code>tree -L 2 ~/labs/python</code> or <code>ls -la</code> in your report.</li>\n</ul>\n<h3>Phase 5 — Retro (10 min)</h3>\n<ul>\n<li>What broke if <code>PATH</code> contained spaces? What broke if you forgot <code>source .venv/bin/activate</code>?</li>\n</ul>\n<h4>Verify</h4>\n<p>Acceptance checklist satisfied; you can explain <strong>each import</strong> you used.</p>\n<h4>Common mistakes</h4>\n<p>Forgetting to activate venv → packages install globally or commands fail mysteriously.</p>\n<h4>Going further</h4>\n<p>Add <code>ruff</code> or <code>black</code> later for style—optional.</p>\n<hr>\n</div>","labs-kali-p-08":"<p class=\"lh-tg-source\"><strong>Lab P-08 — Simple log parser</strong> · <span class=\"lh-tg-chapter\">Part 4 — Python (on Kali)</span> · Lesson <code>labs-kali-p-08</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab P-08 — Simple log parser</h3>\n<p><strong>Suggested pace:</strong> 75–120 minutes</p>\n<p><strong>Goal:</strong> Aggregate status codes from synthetic access logs.</p>\n<h4>Why this lab exists</h4>\n<p>Python on Kali is how automators glue systems together—<strong>read docs</strong>, <strong>type code</strong>, <strong>run tests</strong>, not copy-paste alone.</p>\n<h4>Background</h4>\n<p>Use <strong><code>~/labs/python/</code></strong> for sources. Always activate your venv (<code>source .venv/bin/activate</code>) before <code>pip install</code>. Prefer <strong>absolute imports within small projects</strong> for clarity.</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">cd ~/labs/python &amp;&amp; python3 -m venv .venv &amp;&amp; source .venv/bin/activate\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> <code>python3 -m venv</code> creates an isolated interpreter + site-packages. <code>source</code> applies env vars in the current shell.</li>\n<li><strong>Why we use it here:</strong> Avoids polluting system Python—critical habit.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Isolate Python (15 min)</h3>\n<ol>\n<li><code>cd ~/labs/python</code> — if the folder is missing, <code>mkdir -p ~/labs/python</code>.</li>\n<li>Create or reuse a venv: <code>python3 -m venv .venv &amp;&amp; source .venv/bin/activate</code>. Run <code>which python</code> and confirm it points inside <code>.venv</code>.</li>\n<li>Run <code>python -m pip install --upgrade pip</code> (note <code>python</code> vs <code>python3</code> inside venv).</li>\n</ol>\n<h3>Phase 2 — Project hygiene (10 min)</h3>\n<ul>\n<li>Create today’s folder or branch naming pattern (for example <code>lab-</code> plus the output of <code>date +%F</code> in bash).</li>\n<li>Open <code>README.txt</code> in the lab folder listing: objective, commands you expect to run, rollback (delete venv? restore snapshot?).</li>\n</ul>\n<h3>Phase 3 — Core lab work</h3>\n<p><strong>Apache log counter</strong></p>\n<ul>\n<li>Generate synthetic <code>access.log</code> lines with <code>for i in {1..200}; do echo &quot;… $((i%5)) …&quot;; done &gt; ~/labs/python/fake.log</code>.</li>\n<li>Parse and count HTTP status “codes” you embedded; output histogram dict sorted by count.</li>\n<li><strong>Checkpoint:</strong> Compare your counts to <code>grep -c</code> sanity checks.</li>\n</ul>\n<h3>Phase 4 — Evidence (15 min)</h3>\n<ul>\n<li>Capture <strong>stdout/stderr</strong> for at least two runs (success + intentional failure path).</li>\n<li>If you added files, <code>tree -L 2 ~/labs/python</code> or <code>ls -la</code> in your report.</li>\n</ul>\n<h3>Phase 5 — Retro (10 min)</h3>\n<ul>\n<li>What broke if <code>PATH</code> contained spaces? What broke if you forgot <code>source .venv/bin/activate</code>?</li>\n</ul>\n<h4>Verify</h4>\n<p>Acceptance checklist satisfied; you can explain <strong>each import</strong> you used.</p>\n<h4>Common mistakes</h4>\n<p>Forgetting to activate venv → packages install globally or commands fail mysteriously.</p>\n<h4>Going further</h4>\n<p>Add <code>ruff</code> or <code>black</code> later for style—optional.</p>\n<hr>\n</div>","labs-kali-p-09":"<p class=\"lh-tg-source\"><strong>Lab P-09 — dataclasses and dict validation</strong> · <span class=\"lh-tg-chapter\">Part 4 — Python (on Kali)</span> · Lesson <code>labs-kali-p-09</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab P-09 — dataclasses and dict validation</h3>\n<p><strong>Suggested pace:</strong> 75–120 minutes</p>\n<p><strong>Goal:</strong> Model records safely before use.</p>\n<h4>Why this lab exists</h4>\n<p>Python on Kali is how automators glue systems together—<strong>read docs</strong>, <strong>type code</strong>, <strong>run tests</strong>, not copy-paste alone.</p>\n<h4>Background</h4>\n<p>Use <strong><code>~/labs/python/</code></strong> for sources. Always activate your venv (<code>source .venv/bin/activate</code>) before <code>pip install</code>. Prefer <strong>absolute imports within small projects</strong> for clarity.</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">cd ~/labs/python &amp;&amp; python3 -m venv .venv &amp;&amp; source .venv/bin/activate\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> <code>python3 -m venv</code> creates an isolated interpreter + site-packages. <code>source</code> applies env vars in the current shell.</li>\n<li><strong>Why we use it here:</strong> Avoids polluting system Python—critical habit.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Isolate Python (15 min)</h3>\n<ol>\n<li><code>cd ~/labs/python</code> — if the folder is missing, <code>mkdir -p ~/labs/python</code>.</li>\n<li>Create or reuse a venv: <code>python3 -m venv .venv &amp;&amp; source .venv/bin/activate</code>. Run <code>which python</code> and confirm it points inside <code>.venv</code>.</li>\n<li>Run <code>python -m pip install --upgrade pip</code> (note <code>python</code> vs <code>python3</code> inside venv).</li>\n</ol>\n<h3>Phase 2 — Project hygiene (10 min)</h3>\n<ul>\n<li>Create today’s folder or branch naming pattern (for example <code>lab-</code> plus the output of <code>date +%F</code> in bash).</li>\n<li>Open <code>README.txt</code> in the lab folder listing: objective, commands you expect to run, rollback (delete venv? restore snapshot?).</li>\n</ul>\n<h3>Phase 3 — Core lab work</h3>\n<p><strong>dataclass validation</strong></p>\n<ul>\n<li>Define <code>@dataclass</code> User with fields. Write <code>User.from_dict(d)</code> that rejects unknown keys and missing required keys with clear exceptions.</li>\n<li><strong>Checkpoint:</strong> Show one rejected dict example in notes.</li>\n</ul>\n<h3>Phase 4 — Evidence (15 min)</h3>\n<ul>\n<li>Capture <strong>stdout/stderr</strong> for at least two runs (success + intentional failure path).</li>\n<li>If you added files, <code>tree -L 2 ~/labs/python</code> or <code>ls -la</code> in your report.</li>\n</ul>\n<h3>Phase 5 — Retro (10 min)</h3>\n<ul>\n<li>What broke if <code>PATH</code> contained spaces? What broke if you forgot <code>source .venv/bin/activate</code>?</li>\n</ul>\n<h4>Verify</h4>\n<p>Acceptance checklist satisfied; you can explain <strong>each import</strong> you used.</p>\n<h4>Common mistakes</h4>\n<p>Forgetting to activate venv → packages install globally or commands fail mysteriously.</p>\n<h4>Going further</h4>\n<p>Add <code>ruff</code> or <code>black</code> later for style—optional.</p>\n<hr>\n</div>","labs-kali-p-10":"<p class=\"lh-tg-source\"><strong>Lab P-10 — pytest basics</strong> · <span class=\"lh-tg-chapter\">Part 4 — Python (on Kali)</span> · Lesson <code>labs-kali-p-10</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab P-10 — pytest basics</h3>\n<p><strong>Suggested pace:</strong> 75–120 minutes</p>\n<p><strong>Goal:</strong> Write five meaningful tests.</p>\n<h4>Why this lab exists</h4>\n<p>Python on Kali is how automators glue systems together—<strong>read docs</strong>, <strong>type code</strong>, <strong>run tests</strong>, not copy-paste alone.</p>\n<h4>Background</h4>\n<p>Use <strong><code>~/labs/python/</code></strong> for sources. Always activate your venv (<code>source .venv/bin/activate</code>) before <code>pip install</code>. Prefer <strong>absolute imports within small projects</strong> for clarity.</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">cd ~/labs/python &amp;&amp; python3 -m venv .venv &amp;&amp; source .venv/bin/activate\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> <code>python3 -m venv</code> creates an isolated interpreter + site-packages. <code>source</code> applies env vars in the current shell.</li>\n<li><strong>Why we use it here:</strong> Avoids polluting system Python—critical habit.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Isolate Python (15 min)</h3>\n<ol>\n<li><code>cd ~/labs/python</code> — if the folder is missing, <code>mkdir -p ~/labs/python</code>.</li>\n<li>Create or reuse a venv: <code>python3 -m venv .venv &amp;&amp; source .venv/bin/activate</code>. Run <code>which python</code> and confirm it points inside <code>.venv</code>.</li>\n<li>Run <code>python -m pip install --upgrade pip</code> (note <code>python</code> vs <code>python3</code> inside venv).</li>\n</ol>\n<h3>Phase 2 — Project hygiene (10 min)</h3>\n<ul>\n<li>Create today’s folder or branch naming pattern (for example <code>lab-</code> plus the output of <code>date +%F</code> in bash).</li>\n<li>Open <code>README.txt</code> in the lab folder listing: objective, commands you expect to run, rollback (delete venv? restore snapshot?).</li>\n</ul>\n<h3>Phase 3 — Core lab work</h3>\n<p><strong>pytest five tests</strong></p>\n<ul>\n<li>Add <code>tests/test_csv_tool.py</code> with five tests covering happy path, bad int row, empty file, etc.</li>\n<li>Run <code>pytest -q</code>; paste summary line.</li>\n<li><strong>Checkpoint:</strong> Explain fixture vs plain function setup.</li>\n</ul>\n<h3>Phase 4 — Evidence (15 min)</h3>\n<ul>\n<li>Capture <strong>stdout/stderr</strong> for at least two runs (success + intentional failure path).</li>\n<li>If you added files, <code>tree -L 2 ~/labs/python</code> or <code>ls -la</code> in your report.</li>\n</ul>\n<h3>Phase 5 — Retro (10 min)</h3>\n<ul>\n<li>What broke if <code>PATH</code> contained spaces? What broke if you forgot <code>source .venv/bin/activate</code>?</li>\n</ul>\n<h4>Verify</h4>\n<p>Acceptance checklist satisfied; you can explain <strong>each import</strong> you used.</p>\n<h4>Common mistakes</h4>\n<p>Forgetting to activate venv → packages install globally or commands fail mysteriously.</p>\n<h4>Going further</h4>\n<p>Add <code>ruff</code> or <code>black</code> later for style—optional.</p>\n<hr>\n</div>","labs-kali-p-11":"<p class=\"lh-tg-source\"><strong>Lab P-11 — File hashing duplicate finder</strong> · <span class=\"lh-tg-chapter\">Part 4 — Python (on Kali)</span> · Lesson <code>labs-kali-p-11</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab P-11 — File hashing duplicate finder</h3>\n<p><strong>Suggested pace:</strong> 75–120 minutes</p>\n<p><strong>Goal:</strong> Find duplicate files by hash.</p>\n<h4>Why this lab exists</h4>\n<p>Python on Kali is how automators glue systems together—<strong>read docs</strong>, <strong>type code</strong>, <strong>run tests</strong>, not copy-paste alone.</p>\n<h4>Background</h4>\n<p>Use <strong><code>~/labs/python/</code></strong> for sources. Always activate your venv (<code>source .venv/bin/activate</code>) before <code>pip install</code>. Prefer <strong>absolute imports within small projects</strong> for clarity.</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">cd ~/labs/python &amp;&amp; python3 -m venv .venv &amp;&amp; source .venv/bin/activate\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> <code>python3 -m venv</code> creates an isolated interpreter + site-packages. <code>source</code> applies env vars in the current shell.</li>\n<li><strong>Why we use it here:</strong> Avoids polluting system Python—critical habit.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Isolate Python (15 min)</h3>\n<ol>\n<li><code>cd ~/labs/python</code> — if the folder is missing, <code>mkdir -p ~/labs/python</code>.</li>\n<li>Create or reuse a venv: <code>python3 -m venv .venv &amp;&amp; source .venv/bin/activate</code>. Run <code>which python</code> and confirm it points inside <code>.venv</code>.</li>\n<li>Run <code>python -m pip install --upgrade pip</code> (note <code>python</code> vs <code>python3</code> inside venv).</li>\n</ol>\n<h3>Phase 2 — Project hygiene (10 min)</h3>\n<ul>\n<li>Create today’s folder or branch naming pattern (for example <code>lab-</code> plus the output of <code>date +%F</code> in bash).</li>\n<li>Open <code>README.txt</code> in the lab folder listing: objective, commands you expect to run, rollback (delete venv? restore snapshot?).</li>\n</ul>\n<h3>Phase 3 — Core lab work</h3>\n<p><strong>hash duplicate finder</strong></p>\n<ul>\n<li>Walk <code>~/labs/python/tree</code> (create nested duplicate files). Hash with <code>hashlib.sha256</code> in binary mode.</li>\n<li>Print groups of paths sharing a digest.</li>\n<li><strong>Checkpoint:</strong> Note collision probability argument (SHA-256) in one sentence.</li>\n</ul>\n<h3>Phase 4 — Evidence (15 min)</h3>\n<ul>\n<li>Capture <strong>stdout/stderr</strong> for at least two runs (success + intentional failure path).</li>\n<li>If you added files, <code>tree -L 2 ~/labs/python</code> or <code>ls -la</code> in your report.</li>\n</ul>\n<h3>Phase 5 — Retro (10 min)</h3>\n<ul>\n<li>What broke if <code>PATH</code> contained spaces? What broke if you forgot <code>source .venv/bin/activate</code>?</li>\n</ul>\n<h4>Verify</h4>\n<p>Acceptance checklist satisfied; you can explain <strong>each import</strong> you used.</p>\n<h4>Common mistakes</h4>\n<p>Forgetting to activate venv → packages install globally or commands fail mysteriously.</p>\n<h4>Going further</h4>\n<p>Add <code>ruff</code> or <code>black</code> later for style—optional.</p>\n<hr>\n</div>","labs-kali-p-12":"<p class=\"lh-tg-source\"><strong>Lab P-12 — Tiny API client with retries</strong> · <span class=\"lh-tg-chapter\">Part 4 — Python (on Kali)</span> · Lesson <code>labs-kali-p-12</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab P-12 — Tiny API client with retries</h3>\n<p><strong>Suggested pace:</strong> 75–120 minutes</p>\n<p><strong>Goal:</strong> Retry flaky HTTP with backoff.</p>\n<h4>Why this lab exists</h4>\n<p>Python on Kali is how automators glue systems together—<strong>read docs</strong>, <strong>type code</strong>, <strong>run tests</strong>, not copy-paste alone.</p>\n<h4>Background</h4>\n<p>Use <strong><code>~/labs/python/</code></strong> for sources. Always activate your venv (<code>source .venv/bin/activate</code>) before <code>pip install</code>. Prefer <strong>absolute imports within small projects</strong> for clarity.</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">cd ~/labs/python &amp;&amp; python3 -m venv .venv &amp;&amp; source .venv/bin/activate\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> <code>python3 -m venv</code> creates an isolated interpreter + site-packages. <code>source</code> applies env vars in the current shell.</li>\n<li><strong>Why we use it here:</strong> Avoids polluting system Python—critical habit.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Isolate Python (15 min)</h3>\n<ol>\n<li><code>cd ~/labs/python</code> — if the folder is missing, <code>mkdir -p ~/labs/python</code>.</li>\n<li>Create or reuse a venv: <code>python3 -m venv .venv &amp;&amp; source .venv/bin/activate</code>. Run <code>which python</code> and confirm it points inside <code>.venv</code>.</li>\n<li>Run <code>python -m pip install --upgrade pip</code> (note <code>python</code> vs <code>python3</code> inside venv).</li>\n</ol>\n<h3>Phase 2 — Project hygiene (10 min)</h3>\n<ul>\n<li>Create today’s folder or branch naming pattern (for example <code>lab-</code> plus the output of <code>date +%F</code> in bash).</li>\n<li>Open <code>README.txt</code> in the lab folder listing: objective, commands you expect to run, rollback (delete venv? restore snapshot?).</li>\n</ul>\n<h3>Phase 3 — Core lab work</h3>\n<p><strong>requests + exponential backoff</strong></p>\n<ul>\n<li>Write a tiny Flask app on 127.0.0.1 that randomly returns 500 (or use a stub). Client retries with <strong>backoff + jitter</strong>.</li>\n<li>Cap max retries; log each attempt with timestamp.</li>\n<li><strong>Checkpoint:</strong> Plot or list attempt timings in your notebook.</li>\n</ul>\n<h3>Phase 4 — Evidence (15 min)</h3>\n<ul>\n<li>Capture <strong>stdout/stderr</strong> for at least two runs (success + intentional failure path).</li>\n<li>If you added files, <code>tree -L 2 ~/labs/python</code> or <code>ls -la</code> in your report.</li>\n</ul>\n<h3>Phase 5 — Retro (10 min)</h3>\n<ul>\n<li>What broke if <code>PATH</code> contained spaces? What broke if you forgot <code>source .venv/bin/activate</code>?</li>\n</ul>\n<h4>Verify</h4>\n<p>Acceptance checklist satisfied; you can explain <strong>each import</strong> you used.</p>\n<h4>Common mistakes</h4>\n<p>Forgetting to activate venv → packages install globally or commands fail mysteriously.</p>\n<h4>Going further</h4>\n<p>Add <code>ruff</code> or <code>black</code> later for style—optional.</p>\n<hr>\n</div>","labs-kali-s-01":"<p class=\"lh-tg-source\"><strong>Lab S-01 — Create database and role</strong> · <span class=\"lh-tg-chapter\">Part 5 — SQL / MariaDB (on Kali)</span> · Lesson <code>labs-kali-s-01</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab S-01 — Create database and role</h3>\n<p><strong>Suggested pace:</strong> 70–110 minutes</p>\n<p><strong>Goal:</strong> Execute the relational modeling and SQL skills for this step—see procedure.</p>\n<h4>Why this lab exists</h4>\n<p>Databases reward <strong>slow thinking</strong>: schema first, constraints second, queries third.</p>\n<h4>Background</h4>\n<p><strong>MariaDB</strong> speaks the same client protocol as MySQL for most learner tasks. The <strong><code>mariadb</code></strong> client connects to the server; <strong><code>sudo mariadb</code></strong> typically opens a local <strong>root</strong> administrative session on fresh installs. Always prefer <strong>parameterized</strong> queries in application code (covered in a later lab).</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo mariadb\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Opens the MariaDB client as the privileged local admin (typical Kali install).</li>\n<li><strong>Why we use it here:</strong> You need DDL privileges to create databases and users.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Session + safety (10 min)</h3>\n<ol>\n<li>Open <strong>two</strong> terminals: one <code>sudo mariadb</code> (admin), one normal shell for <code>mysqldump</code> / editors.</li>\n<li>Before destructive DDL, run <code>SHOW CREATE TABLE …</code> or copy schema with <code>mysqldump --no-data</code> when appropriate.</li>\n</ol>\n<h3>Phase 2 — Vocabulary (10 min)</h3>\n<ul>\n<li>Classify each statement you will run as <strong>DDL</strong> (schema), <strong>DML</strong> (data), or <strong>DCL</strong> (permissions).</li>\n<li>Write one sentence on why <code>FLUSH PRIVILEGES;</code> exists after <code>GRANT</code>.</li>\n</ul>\n<h3>Phase 3 — Execute the lab</h3>\n<p><strong>S-01 — Database + role (DDL walkthrough)</strong></p>\n<p>Run in <code>sudo mariadb</code> (paste one statement at a time, read responses):</p>\n<pre><code class=\"language-sql\">CREATE DATABASE IF NOT EXISTS soc_training;\nCREATE USER IF NOT EXISTS &#39;analyst&#39;@&#39;localhost&#39; IDENTIFIED BY &#39;replace-with-strong-password&#39;;\nGRANT SELECT, INSERT, UPDATE ON soc_training.* TO &#39;analyst&#39;@&#39;localhost&#39;;\nFLUSH PRIVILEGES;\n</code></pre>\n<p>Then <strong>exit</strong> root session and test: <code>mariadb -u analyst -p soc_training -e &quot;SELECT DATABASE();&quot;</code>.</p>\n<ul>\n<li><strong>Explain:</strong> what <code>GRANT … ON db.*</code> means vs table-level grants.</li>\n</ul>\n<h3>Phase 4 — Integrity checks (15 min)</h3>\n<ul>\n<li>After inserts, run <code>SELECT … COUNT(*)</code> sanity queries. Use <code>EXPLAIN</code> on at least one non-trivial <code>SELECT</code>.</li>\n<li>If you changed users, log out and verify <strong>least privilege</strong> by connecting as <code>analyst</code>.</li>\n</ul>\n<h3>Phase 5 — Diagram + notes (10 min)</h3>\n<ul>\n<li>Sketch ER diagram on paper; photograph or transcribe key entities and cardinalities.</li>\n</ul>\n<h4>Verify</h4>\n<p>Schema and queries match lab intent; you can draw the <strong>ER diagram</strong> on paper.</p>\n<h4>Common mistakes</h4>\n<p>Mixing quoting for user@host; forgetting <code>FLUSH PRIVILEGES</code> after GRANT changes in some workflows.</p>\n<h4>Going further</h4>\n<p>Read MariaDB explain JSON output once (<code>EXPLAIN FORMAT=JSON</code>).</p>\n<hr>\n</div>","labs-kali-s-02":"<p class=\"lh-tg-source\"><strong>Lab S-02 — Incidents and assets (1NF)</strong> · <span class=\"lh-tg-chapter\">Part 5 — SQL / MariaDB (on Kali)</span> · Lesson <code>labs-kali-s-02</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab S-02 — Incidents and assets (1NF)</h3>\n<p><strong>Suggested pace:</strong> 70–110 minutes</p>\n<p><strong>Goal:</strong> Execute the relational modeling and SQL skills for this step—see procedure.</p>\n<h4>Why this lab exists</h4>\n<p>Databases reward <strong>slow thinking</strong>: schema first, constraints second, queries third.</p>\n<h4>Background</h4>\n<p><strong>MariaDB</strong> speaks the same client protocol as MySQL for most learner tasks. The <strong><code>mariadb</code></strong> client connects to the server; <strong><code>sudo mariadb</code></strong> typically opens a local <strong>root</strong> administrative session on fresh installs. Always prefer <strong>parameterized</strong> queries in application code (covered in a later lab).</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo mariadb\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Opens the MariaDB client as the privileged local admin (typical Kali install).</li>\n<li><strong>Why we use it here:</strong> You need DDL privileges to create databases and users.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Session + safety (10 min)</h3>\n<ol>\n<li>Open <strong>two</strong> terminals: one <code>sudo mariadb</code> (admin), one normal shell for <code>mysqldump</code> / editors.</li>\n<li>Before destructive DDL, run <code>SHOW CREATE TABLE …</code> or copy schema with <code>mysqldump --no-data</code> when appropriate.</li>\n</ol>\n<h3>Phase 2 — Vocabulary (10 min)</h3>\n<ul>\n<li>Classify each statement you will run as <strong>DDL</strong> (schema), <strong>DML</strong> (data), or <strong>DCL</strong> (permissions).</li>\n<li>Write one sentence on why <code>FLUSH PRIVILEGES;</code> exists after <code>GRANT</code>.</li>\n</ul>\n<h3>Phase 3 — Execute the lab</h3>\n<p><strong>S-02 — assets + incidents tables</strong></p>\n<pre><code class=\"language-sql\">USE soc_training;\nCREATE TABLE assets (\n  id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,\n  hostname VARCHAR(128) NOT NULL,\n  ip VARCHAR(45) NOT NULL,\n  owner VARCHAR(128) NOT NULL\n);\nCREATE TABLE incidents (\n  id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,\n  title VARCHAR(255) NOT NULL,\n  opened_at DATETIME NOT NULL,\n  severity ENUM(&#39;low&#39;,&#39;med&#39;,&#39;high&#39;,&#39;crit&#39;) NOT NULL\n);\n</code></pre>\n<p>Insert <strong>≥10</strong> realistic rows (<code>INSERT … VALUES</code> multi-row is fine). Run <code>SELECT COUNT(*) FROM assets;</code>.</p>\n<h3>Phase 4 — Integrity checks (15 min)</h3>\n<ul>\n<li>After inserts, run <code>SELECT … COUNT(*)</code> sanity queries. Use <code>EXPLAIN</code> on at least one non-trivial <code>SELECT</code>.</li>\n<li>If you changed users, log out and verify <strong>least privilege</strong> by connecting as <code>analyst</code>.</li>\n</ul>\n<h3>Phase 5 — Diagram + notes (10 min)</h3>\n<ul>\n<li>Sketch ER diagram on paper; photograph or transcribe key entities and cardinalities.</li>\n</ul>\n<h4>Verify</h4>\n<p>Schema and queries match lab intent; you can draw the <strong>ER diagram</strong> on paper.</p>\n<h4>Common mistakes</h4>\n<p>Mixing quoting for user@host; forgetting <code>FLUSH PRIVILEGES</code> after GRANT changes in some workflows.</p>\n<h4>Going further</h4>\n<p>Read MariaDB explain JSON output once (<code>EXPLAIN FORMAT=JSON</code>).</p>\n<hr>\n</div>","labs-kali-s-03":"<p class=\"lh-tg-source\"><strong>Lab S-03 — Foreign keys and cascades</strong> · <span class=\"lh-tg-chapter\">Part 5 — SQL / MariaDB (on Kali)</span> · Lesson <code>labs-kali-s-03</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab S-03 — Foreign keys and cascades</h3>\n<p><strong>Suggested pace:</strong> 70–110 minutes</p>\n<p><strong>Goal:</strong> Execute the relational modeling and SQL skills for this step—see procedure.</p>\n<h4>Why this lab exists</h4>\n<p>Databases reward <strong>slow thinking</strong>: schema first, constraints second, queries third.</p>\n<h4>Background</h4>\n<p><strong>MariaDB</strong> speaks the same client protocol as MySQL for most learner tasks. The <strong><code>mariadb</code></strong> client connects to the server; <strong><code>sudo mariadb</code></strong> typically opens a local <strong>root</strong> administrative session on fresh installs. Always prefer <strong>parameterized</strong> queries in application code (covered in a later lab).</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo mariadb\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Opens the MariaDB client as the privileged local admin (typical Kali install).</li>\n<li><strong>Why we use it here:</strong> You need DDL privileges to create databases and users.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Session + safety (10 min)</h3>\n<ol>\n<li>Open <strong>two</strong> terminals: one <code>sudo mariadb</code> (admin), one normal shell for <code>mysqldump</code> / editors.</li>\n<li>Before destructive DDL, run <code>SHOW CREATE TABLE …</code> or copy schema with <code>mysqldump --no-data</code> when appropriate.</li>\n</ol>\n<h3>Phase 2 — Vocabulary (10 min)</h3>\n<ul>\n<li>Classify each statement you will run as <strong>DDL</strong> (schema), <strong>DML</strong> (data), or <strong>DCL</strong> (permissions).</li>\n<li>Write one sentence on why <code>FLUSH PRIVILEGES;</code> exists after <code>GRANT</code>.</li>\n</ul>\n<h3>Phase 3 — Execute the lab</h3>\n<p><strong>S-03 — Foreign keys + cascades</strong></p>\n<p>Create <code>incident_findings</code> with FK to <code>incidents</code> and <code>assets</code>. Test <strong>ON DELETE RESTRICT</strong> by trying to delete an incident that still has findings—note error code.\nThen test <strong>ON UPDATE CASCADE</strong> on a child column if you modeled one (or document why not).</p>\n<h3>Phase 4 — Integrity checks (15 min)</h3>\n<ul>\n<li>After inserts, run <code>SELECT … COUNT(*)</code> sanity queries. Use <code>EXPLAIN</code> on at least one non-trivial <code>SELECT</code>.</li>\n<li>If you changed users, log out and verify <strong>least privilege</strong> by connecting as <code>analyst</code>.</li>\n</ul>\n<h3>Phase 5 — Diagram + notes (10 min)</h3>\n<ul>\n<li>Sketch ER diagram on paper; photograph or transcribe key entities and cardinalities.</li>\n</ul>\n<h4>Verify</h4>\n<p>Schema and queries match lab intent; you can draw the <strong>ER diagram</strong> on paper.</p>\n<h4>Common mistakes</h4>\n<p>Mixing quoting for user@host; forgetting <code>FLUSH PRIVILEGES</code> after GRANT changes in some workflows.</p>\n<h4>Going further</h4>\n<p>Read MariaDB explain JSON output once (<code>EXPLAIN FORMAT=JSON</code>).</p>\n<hr>\n</div>","labs-kali-s-04":"<p class=\"lh-tg-source\"><strong>Lab S-04 — Join practice</strong> · <span class=\"lh-tg-chapter\">Part 5 — SQL / MariaDB (on Kali)</span> · Lesson <code>labs-kali-s-04</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab S-04 — Join practice</h3>\n<p><strong>Suggested pace:</strong> 70–110 minutes</p>\n<p><strong>Goal:</strong> Execute the relational modeling and SQL skills for this step—see procedure.</p>\n<h4>Why this lab exists</h4>\n<p>Databases reward <strong>slow thinking</strong>: schema first, constraints second, queries third.</p>\n<h4>Background</h4>\n<p><strong>MariaDB</strong> speaks the same client protocol as MySQL for most learner tasks. The <strong><code>mariadb</code></strong> client connects to the server; <strong><code>sudo mariadb</code></strong> typically opens a local <strong>root</strong> administrative session on fresh installs. Always prefer <strong>parameterized</strong> queries in application code (covered in a later lab).</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo mariadb\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Opens the MariaDB client as the privileged local admin (typical Kali install).</li>\n<li><strong>Why we use it here:</strong> You need DDL privileges to create databases and users.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Session + safety (10 min)</h3>\n<ol>\n<li>Open <strong>two</strong> terminals: one <code>sudo mariadb</code> (admin), one normal shell for <code>mysqldump</code> / editors.</li>\n<li>Before destructive DDL, run <code>SHOW CREATE TABLE …</code> or copy schema with <code>mysqldump --no-data</code> when appropriate.</li>\n</ol>\n<h3>Phase 2 — Vocabulary (10 min)</h3>\n<ul>\n<li>Classify each statement you will run as <strong>DDL</strong> (schema), <strong>DML</strong> (data), or <strong>DCL</strong> (permissions).</li>\n<li>Write one sentence on why <code>FLUSH PRIVILEGES;</code> exists after <code>GRANT</code>.</li>\n</ul>\n<h3>Phase 3 — Execute the lab</h3>\n<p><strong>S-04 — Join practice</strong></p>\n<p>Write queries (save in <code>~/labs/sql/joins.sql</code>) for:</p>\n<ol>\n<li>assets touched by &gt;1 incident; 2) open incidents with zero findings. Use <strong>both</strong> inner join and left join styles where possible.\n<code>EXPLAIN</code> one heavy join before/after you add data.</li>\n</ol>\n<h3>Phase 4 — Integrity checks (15 min)</h3>\n<ul>\n<li>After inserts, run <code>SELECT … COUNT(*)</code> sanity queries. Use <code>EXPLAIN</code> on at least one non-trivial <code>SELECT</code>.</li>\n<li>If you changed users, log out and verify <strong>least privilege</strong> by connecting as <code>analyst</code>.</li>\n</ul>\n<h3>Phase 5 — Diagram + notes (10 min)</h3>\n<ul>\n<li>Sketch ER diagram on paper; photograph or transcribe key entities and cardinalities.</li>\n</ul>\n<h4>Verify</h4>\n<p>Schema and queries match lab intent; you can draw the <strong>ER diagram</strong> on paper.</p>\n<h4>Common mistakes</h4>\n<p>Mixing quoting for user@host; forgetting <code>FLUSH PRIVILEGES</code> after GRANT changes in some workflows.</p>\n<h4>Going further</h4>\n<p>Read MariaDB explain JSON output once (<code>EXPLAIN FORMAT=JSON</code>).</p>\n<hr>\n</div>","labs-kali-s-05":"<p class=\"lh-tg-source\"><strong>Lab S-05 — Aggregation and HAVING</strong> · <span class=\"lh-tg-chapter\">Part 5 — SQL / MariaDB (on Kali)</span> · Lesson <code>labs-kali-s-05</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab S-05 — Aggregation and HAVING</h3>\n<p><strong>Suggested pace:</strong> 70–110 minutes</p>\n<p><strong>Goal:</strong> Execute the relational modeling and SQL skills for this step—see procedure.</p>\n<h4>Why this lab exists</h4>\n<p>Databases reward <strong>slow thinking</strong>: schema first, constraints second, queries third.</p>\n<h4>Background</h4>\n<p><strong>MariaDB</strong> speaks the same client protocol as MySQL for most learner tasks. The <strong><code>mariadb</code></strong> client connects to the server; <strong><code>sudo mariadb</code></strong> typically opens a local <strong>root</strong> administrative session on fresh installs. Always prefer <strong>parameterized</strong> queries in application code (covered in a later lab).</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo mariadb\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Opens the MariaDB client as the privileged local admin (typical Kali install).</li>\n<li><strong>Why we use it here:</strong> You need DDL privileges to create databases and users.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Session + safety (10 min)</h3>\n<ol>\n<li>Open <strong>two</strong> terminals: one <code>sudo mariadb</code> (admin), one normal shell for <code>mysqldump</code> / editors.</li>\n<li>Before destructive DDL, run <code>SHOW CREATE TABLE …</code> or copy schema with <code>mysqldump --no-data</code> when appropriate.</li>\n</ol>\n<h3>Phase 2 — Vocabulary (10 min)</h3>\n<ul>\n<li>Classify each statement you will run as <strong>DDL</strong> (schema), <strong>DML</strong> (data), or <strong>DCL</strong> (permissions).</li>\n<li>Write one sentence on why <code>FLUSH PRIVILEGES;</code> exists after <code>GRANT</code>.</li>\n</ul>\n<h3>Phase 3 — Execute the lab</h3>\n<p><strong>S-05 — Aggregation + HAVING</strong></p>\n<p>Compute average <strong>hours between</strong> incidents grouped by <code>severity</code> (use <code>TIMESTAMPDIFF</code> or precomputed durations in a column you add).\nUse <code>HAVING</code> to filter groups with count ≥ 3.</p>\n<h3>Phase 4 — Integrity checks (15 min)</h3>\n<ul>\n<li>After inserts, run <code>SELECT … COUNT(*)</code> sanity queries. Use <code>EXPLAIN</code> on at least one non-trivial <code>SELECT</code>.</li>\n<li>If you changed users, log out and verify <strong>least privilege</strong> by connecting as <code>analyst</code>.</li>\n</ul>\n<h3>Phase 5 — Diagram + notes (10 min)</h3>\n<ul>\n<li>Sketch ER diagram on paper; photograph or transcribe key entities and cardinalities.</li>\n</ul>\n<h4>Verify</h4>\n<p>Schema and queries match lab intent; you can draw the <strong>ER diagram</strong> on paper.</p>\n<h4>Common mistakes</h4>\n<p>Mixing quoting for user@host; forgetting <code>FLUSH PRIVILEGES</code> after GRANT changes in some workflows.</p>\n<h4>Going further</h4>\n<p>Read MariaDB explain JSON output once (<code>EXPLAIN FORMAT=JSON</code>).</p>\n<hr>\n</div>","labs-kali-s-06":"<p class=\"lh-tg-source\"><strong>Lab S-06 — Transactions</strong> · <span class=\"lh-tg-chapter\">Part 5 — SQL / MariaDB (on Kali)</span> · Lesson <code>labs-kali-s-06</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab S-06 — Transactions</h3>\n<p><strong>Suggested pace:</strong> 70–110 minutes</p>\n<p><strong>Goal:</strong> Execute the relational modeling and SQL skills for this step—see procedure.</p>\n<h4>Why this lab exists</h4>\n<p>Databases reward <strong>slow thinking</strong>: schema first, constraints second, queries third.</p>\n<h4>Background</h4>\n<p><strong>MariaDB</strong> speaks the same client protocol as MySQL for most learner tasks. The <strong><code>mariadb</code></strong> client connects to the server; <strong><code>sudo mariadb</code></strong> typically opens a local <strong>root</strong> administrative session on fresh installs. Always prefer <strong>parameterized</strong> queries in application code (covered in a later lab).</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo mariadb\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Opens the MariaDB client as the privileged local admin (typical Kali install).</li>\n<li><strong>Why we use it here:</strong> You need DDL privileges to create databases and users.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Session + safety (10 min)</h3>\n<ol>\n<li>Open <strong>two</strong> terminals: one <code>sudo mariadb</code> (admin), one normal shell for <code>mysqldump</code> / editors.</li>\n<li>Before destructive DDL, run <code>SHOW CREATE TABLE …</code> or copy schema with <code>mysqldump --no-data</code> when appropriate.</li>\n</ol>\n<h3>Phase 2 — Vocabulary (10 min)</h3>\n<ul>\n<li>Classify each statement you will run as <strong>DDL</strong> (schema), <strong>DML</strong> (data), or <strong>DCL</strong> (permissions).</li>\n<li>Write one sentence on why <code>FLUSH PRIVILEGES;</code> exists after <code>GRANT</code>.</li>\n</ul>\n<h3>Phase 3 — Execute the lab</h3>\n<p><strong>S-06 — Transactions</strong></p>\n<pre><code class=\"language-sql\">START TRANSACTION;\nINSERT INTO incidents (…) VALUES (…);\nINSERT INTO incident_findings (…) VALUES (…); -- second stmt violates FK on purpose\nROLLBACK;\n</code></pre>\n<p>Repeat with a valid pair and <code>COMMIT;</code>. Show <code>SELECT</code> proof rows appear only after commit.</p>\n<h3>Phase 4 — Integrity checks (15 min)</h3>\n<ul>\n<li>After inserts, run <code>SELECT … COUNT(*)</code> sanity queries. Use <code>EXPLAIN</code> on at least one non-trivial <code>SELECT</code>.</li>\n<li>If you changed users, log out and verify <strong>least privilege</strong> by connecting as <code>analyst</code>.</li>\n</ul>\n<h3>Phase 5 — Diagram + notes (10 min)</h3>\n<ul>\n<li>Sketch ER diagram on paper; photograph or transcribe key entities and cardinalities.</li>\n</ul>\n<h4>Verify</h4>\n<p>Schema and queries match lab intent; you can draw the <strong>ER diagram</strong> on paper.</p>\n<h4>Common mistakes</h4>\n<p>Mixing quoting for user@host; forgetting <code>FLUSH PRIVILEGES</code> after GRANT changes in some workflows.</p>\n<h4>Going further</h4>\n<p>Read MariaDB explain JSON output once (<code>EXPLAIN FORMAT=JSON</code>).</p>\n<hr>\n</div>","labs-kali-s-07":"<p class=\"lh-tg-source\"><strong>Lab S-07 — Indexes and EXPLAIN</strong> · <span class=\"lh-tg-chapter\">Part 5 — SQL / MariaDB (on Kali)</span> · Lesson <code>labs-kali-s-07</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab S-07 — Indexes and EXPLAIN</h3>\n<p><strong>Suggested pace:</strong> 70–110 minutes</p>\n<p><strong>Goal:</strong> Execute the relational modeling and SQL skills for this step—see procedure.</p>\n<h4>Why this lab exists</h4>\n<p>Databases reward <strong>slow thinking</strong>: schema first, constraints second, queries third.</p>\n<h4>Background</h4>\n<p><strong>MariaDB</strong> speaks the same client protocol as MySQL for most learner tasks. The <strong><code>mariadb</code></strong> client connects to the server; <strong><code>sudo mariadb</code></strong> typically opens a local <strong>root</strong> administrative session on fresh installs. Always prefer <strong>parameterized</strong> queries in application code (covered in a later lab).</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo mariadb\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Opens the MariaDB client as the privileged local admin (typical Kali install).</li>\n<li><strong>Why we use it here:</strong> You need DDL privileges to create databases and users.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Session + safety (10 min)</h3>\n<ol>\n<li>Open <strong>two</strong> terminals: one <code>sudo mariadb</code> (admin), one normal shell for <code>mysqldump</code> / editors.</li>\n<li>Before destructive DDL, run <code>SHOW CREATE TABLE …</code> or copy schema with <code>mysqldump --no-data</code> when appropriate.</li>\n</ol>\n<h3>Phase 2 — Vocabulary (10 min)</h3>\n<ul>\n<li>Classify each statement you will run as <strong>DDL</strong> (schema), <strong>DML</strong> (data), or <strong>DCL</strong> (permissions).</li>\n<li>Write one sentence on why <code>FLUSH PRIVILEGES;</code> exists after <code>GRANT</code>.</li>\n</ul>\n<h3>Phase 3 — Execute the lab</h3>\n<p><strong>S-07 — Index + EXPLAIN</strong></p>\n<p>Pick a slow query on <code>(severity, opened_at)</code>. Show <code>EXPLAIN</code> before index; <code>CREATE INDEX idx_sev_opened ON incidents (severity, opened_at);</code>; show <code>EXPLAIN</code> after.\nDescribe <strong>rows</strong> / <strong>type</strong> changes in prose.</p>\n<h3>Phase 4 — Integrity checks (15 min)</h3>\n<ul>\n<li>After inserts, run <code>SELECT … COUNT(*)</code> sanity queries. Use <code>EXPLAIN</code> on at least one non-trivial <code>SELECT</code>.</li>\n<li>If you changed users, log out and verify <strong>least privilege</strong> by connecting as <code>analyst</code>.</li>\n</ul>\n<h3>Phase 5 — Diagram + notes (10 min)</h3>\n<ul>\n<li>Sketch ER diagram on paper; photograph or transcribe key entities and cardinalities.</li>\n</ul>\n<h4>Verify</h4>\n<p>Schema and queries match lab intent; you can draw the <strong>ER diagram</strong> on paper.</p>\n<h4>Common mistakes</h4>\n<p>Mixing quoting for user@host; forgetting <code>FLUSH PRIVILEGES</code> after GRANT changes in some workflows.</p>\n<h4>Going further</h4>\n<p>Read MariaDB explain JSON output once (<code>EXPLAIN FORMAT=JSON</code>).</p>\n<hr>\n</div>","labs-kali-s-08":"<p class=\"lh-tg-source\"><strong>Lab S-08 — Views for analysts</strong> · <span class=\"lh-tg-chapter\">Part 5 — SQL / MariaDB (on Kali)</span> · Lesson <code>labs-kali-s-08</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab S-08 — Views for analysts</h3>\n<p><strong>Suggested pace:</strong> 70–110 minutes</p>\n<p><strong>Goal:</strong> Execute the relational modeling and SQL skills for this step—see procedure.</p>\n<h4>Why this lab exists</h4>\n<p>Databases reward <strong>slow thinking</strong>: schema first, constraints second, queries third.</p>\n<h4>Background</h4>\n<p><strong>MariaDB</strong> speaks the same client protocol as MySQL for most learner tasks. The <strong><code>mariadb</code></strong> client connects to the server; <strong><code>sudo mariadb</code></strong> typically opens a local <strong>root</strong> administrative session on fresh installs. Always prefer <strong>parameterized</strong> queries in application code (covered in a later lab).</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo mariadb\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Opens the MariaDB client as the privileged local admin (typical Kali install).</li>\n<li><strong>Why we use it here:</strong> You need DDL privileges to create databases and users.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Session + safety (10 min)</h3>\n<ol>\n<li>Open <strong>two</strong> terminals: one <code>sudo mariadb</code> (admin), one normal shell for <code>mysqldump</code> / editors.</li>\n<li>Before destructive DDL, run <code>SHOW CREATE TABLE …</code> or copy schema with <code>mysqldump --no-data</code> when appropriate.</li>\n</ol>\n<h3>Phase 2 — Vocabulary (10 min)</h3>\n<ul>\n<li>Classify each statement you will run as <strong>DDL</strong> (schema), <strong>DML</strong> (data), or <strong>DCL</strong> (permissions).</li>\n<li>Write one sentence on why <code>FLUSH PRIVILEGES;</code> exists after <code>GRANT</code>.</li>\n</ul>\n<h3>Phase 3 — Execute the lab</h3>\n<p><strong>S-08 — View + analyst user</strong></p>\n<p><code>CREATE VIEW open_incidents AS SELECT …</code>. Grant <strong>only</strong> <code>SELECT</code> on the view to <code>analyst</code> (revoke base table perms if you granted them earlier—plan this carefully in notes).\nLogin as analyst and confirm <code>SELECT * FROM open_incidents;</code> works while raw tables may not.</p>\n<h3>Phase 4 — Integrity checks (15 min)</h3>\n<ul>\n<li>After inserts, run <code>SELECT … COUNT(*)</code> sanity queries. Use <code>EXPLAIN</code> on at least one non-trivial <code>SELECT</code>.</li>\n<li>If you changed users, log out and verify <strong>least privilege</strong> by connecting as <code>analyst</code>.</li>\n</ul>\n<h3>Phase 5 — Diagram + notes (10 min)</h3>\n<ul>\n<li>Sketch ER diagram on paper; photograph or transcribe key entities and cardinalities.</li>\n</ul>\n<h4>Verify</h4>\n<p>Schema and queries match lab intent; you can draw the <strong>ER diagram</strong> on paper.</p>\n<h4>Common mistakes</h4>\n<p>Mixing quoting for user@host; forgetting <code>FLUSH PRIVILEGES</code> after GRANT changes in some workflows.</p>\n<h4>Going further</h4>\n<p>Read MariaDB explain JSON output once (<code>EXPLAIN FORMAT=JSON</code>).</p>\n<hr>\n</div>","labs-kali-s-09":"<p class=\"lh-tg-source\"><strong>Lab S-09 — Stored procedure (safe)</strong> · <span class=\"lh-tg-chapter\">Part 5 — SQL / MariaDB (on Kali)</span> · Lesson <code>labs-kali-s-09</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab S-09 — Stored procedure (safe)</h3>\n<p><strong>Suggested pace:</strong> 70–110 minutes</p>\n<p><strong>Goal:</strong> Execute the relational modeling and SQL skills for this step—see procedure.</p>\n<h4>Why this lab exists</h4>\n<p>Databases reward <strong>slow thinking</strong>: schema first, constraints second, queries third.</p>\n<h4>Background</h4>\n<p><strong>MariaDB</strong> speaks the same client protocol as MySQL for most learner tasks. The <strong><code>mariadb</code></strong> client connects to the server; <strong><code>sudo mariadb</code></strong> typically opens a local <strong>root</strong> administrative session on fresh installs. Always prefer <strong>parameterized</strong> queries in application code (covered in a later lab).</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo mariadb\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Opens the MariaDB client as the privileged local admin (typical Kali install).</li>\n<li><strong>Why we use it here:</strong> You need DDL privileges to create databases and users.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Session + safety (10 min)</h3>\n<ol>\n<li>Open <strong>two</strong> terminals: one <code>sudo mariadb</code> (admin), one normal shell for <code>mysqldump</code> / editors.</li>\n<li>Before destructive DDL, run <code>SHOW CREATE TABLE …</code> or copy schema with <code>mysqldump --no-data</code> when appropriate.</li>\n</ol>\n<h3>Phase 2 — Vocabulary (10 min)</h3>\n<ul>\n<li>Classify each statement you will run as <strong>DDL</strong> (schema), <strong>DML</strong> (data), or <strong>DCL</strong> (permissions).</li>\n<li>Write one sentence on why <code>FLUSH PRIVILEGES;</code> exists after <code>GRANT</code>.</li>\n</ul>\n<h3>Phase 3 — Execute the lab</h3>\n<p><strong>S-09 — Stored procedure</strong></p>\n<p><code>DELIMITER //</code> style procedure <code>close_incident(IN p_id INT)</code> that checks status, sets <code>closed_at</code>, and signals error on invalid transition.\nCall with <code>CALL close_incident(1);</code> and show before/after row.</p>\n<h3>Phase 4 — Integrity checks (15 min)</h3>\n<ul>\n<li>After inserts, run <code>SELECT … COUNT(*)</code> sanity queries. Use <code>EXPLAIN</code> on at least one non-trivial <code>SELECT</code>.</li>\n<li>If you changed users, log out and verify <strong>least privilege</strong> by connecting as <code>analyst</code>.</li>\n</ul>\n<h3>Phase 5 — Diagram + notes (10 min)</h3>\n<ul>\n<li>Sketch ER diagram on paper; photograph or transcribe key entities and cardinalities.</li>\n</ul>\n<h4>Verify</h4>\n<p>Schema and queries match lab intent; you can draw the <strong>ER diagram</strong> on paper.</p>\n<h4>Common mistakes</h4>\n<p>Mixing quoting for user@host; forgetting <code>FLUSH PRIVILEGES</code> after GRANT changes in some workflows.</p>\n<h4>Going further</h4>\n<p>Read MariaDB explain JSON output once (<code>EXPLAIN FORMAT=JSON</code>).</p>\n<hr>\n</div>","labs-kali-s-10":"<p class=\"lh-tg-source\"><strong>Lab S-10 — Triggers audit log</strong> · <span class=\"lh-tg-chapter\">Part 5 — SQL / MariaDB (on Kali)</span> · Lesson <code>labs-kali-s-10</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab S-10 — Triggers audit log</h3>\n<p><strong>Suggested pace:</strong> 70–110 minutes</p>\n<p><strong>Goal:</strong> Execute the relational modeling and SQL skills for this step—see procedure.</p>\n<h4>Why this lab exists</h4>\n<p>Databases reward <strong>slow thinking</strong>: schema first, constraints second, queries third.</p>\n<h4>Background</h4>\n<p><strong>MariaDB</strong> speaks the same client protocol as MySQL for most learner tasks. The <strong><code>mariadb</code></strong> client connects to the server; <strong><code>sudo mariadb</code></strong> typically opens a local <strong>root</strong> administrative session on fresh installs. Always prefer <strong>parameterized</strong> queries in application code (covered in a later lab).</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo mariadb\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Opens the MariaDB client as the privileged local admin (typical Kali install).</li>\n<li><strong>Why we use it here:</strong> You need DDL privileges to create databases and users.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Session + safety (10 min)</h3>\n<ol>\n<li>Open <strong>two</strong> terminals: one <code>sudo mariadb</code> (admin), one normal shell for <code>mysqldump</code> / editors.</li>\n<li>Before destructive DDL, run <code>SHOW CREATE TABLE …</code> or copy schema with <code>mysqldump --no-data</code> when appropriate.</li>\n</ol>\n<h3>Phase 2 — Vocabulary (10 min)</h3>\n<ul>\n<li>Classify each statement you will run as <strong>DDL</strong> (schema), <strong>DML</strong> (data), or <strong>DCL</strong> (permissions).</li>\n<li>Write one sentence on why <code>FLUSH PRIVILEGES;</code> exists after <code>GRANT</code>.</li>\n</ul>\n<h3>Phase 3 — Execute the lab</h3>\n<p><strong>S-10 — Audit trigger</strong></p>\n<p>Create <code>audit_log</code> table. <code>AFTER UPDATE</code> on <code>incidents</code> inserts old/new severity with <code>NOW()</code>.\nFire an update and <code>SELECT * FROM audit_log ORDER BY id DESC LIMIT 5;</code>.</p>\n<h3>Phase 4 — Integrity checks (15 min)</h3>\n<ul>\n<li>After inserts, run <code>SELECT … COUNT(*)</code> sanity queries. Use <code>EXPLAIN</code> on at least one non-trivial <code>SELECT</code>.</li>\n<li>If you changed users, log out and verify <strong>least privilege</strong> by connecting as <code>analyst</code>.</li>\n</ul>\n<h3>Phase 5 — Diagram + notes (10 min)</h3>\n<ul>\n<li>Sketch ER diagram on paper; photograph or transcribe key entities and cardinalities.</li>\n</ul>\n<h4>Verify</h4>\n<p>Schema and queries match lab intent; you can draw the <strong>ER diagram</strong> on paper.</p>\n<h4>Common mistakes</h4>\n<p>Mixing quoting for user@host; forgetting <code>FLUSH PRIVILEGES</code> after GRANT changes in some workflows.</p>\n<h4>Going further</h4>\n<p>Read MariaDB explain JSON output once (<code>EXPLAIN FORMAT=JSON</code>).</p>\n<hr>\n</div>","labs-kali-s-11":"<p class=\"lh-tg-source\"><strong>Lab S-11 — Backup and restore</strong> · <span class=\"lh-tg-chapter\">Part 5 — SQL / MariaDB (on Kali)</span> · Lesson <code>labs-kali-s-11</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab S-11 — Backup and restore</h3>\n<p><strong>Suggested pace:</strong> 70–110 minutes</p>\n<p><strong>Goal:</strong> Execute the relational modeling and SQL skills for this step—see procedure.</p>\n<h4>Why this lab exists</h4>\n<p>Databases reward <strong>slow thinking</strong>: schema first, constraints second, queries third.</p>\n<h4>Background</h4>\n<p><strong>MariaDB</strong> speaks the same client protocol as MySQL for most learner tasks. The <strong><code>mariadb</code></strong> client connects to the server; <strong><code>sudo mariadb</code></strong> typically opens a local <strong>root</strong> administrative session on fresh installs. Always prefer <strong>parameterized</strong> queries in application code (covered in a later lab).</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo mariadb\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Opens the MariaDB client as the privileged local admin (typical Kali install).</li>\n<li><strong>Why we use it here:</strong> You need DDL privileges to create databases and users.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Session + safety (10 min)</h3>\n<ol>\n<li>Open <strong>two</strong> terminals: one <code>sudo mariadb</code> (admin), one normal shell for <code>mysqldump</code> / editors.</li>\n<li>Before destructive DDL, run <code>SHOW CREATE TABLE …</code> or copy schema with <code>mysqldump --no-data</code> when appropriate.</li>\n</ol>\n<h3>Phase 2 — Vocabulary (10 min)</h3>\n<ul>\n<li>Classify each statement you will run as <strong>DDL</strong> (schema), <strong>DML</strong> (data), or <strong>DCL</strong> (permissions).</li>\n<li>Write one sentence on why <code>FLUSH PRIVILEGES;</code> exists after <code>GRANT</code>.</li>\n</ul>\n<h3>Phase 3 — Execute the lab</h3>\n<p><strong>S-11 — mysqldump restore</strong></p>\n<p>Shell: <code>mysqldump soc_training &gt; /tmp/soc.sql</code> then create <code>soc_training_restore</code> and import.\nVerify row counts match with <code>CHECKSUM TABLE</code> or <code>COUNT(*)</code> per table.</p>\n<h3>Phase 4 — Integrity checks (15 min)</h3>\n<ul>\n<li>After inserts, run <code>SELECT … COUNT(*)</code> sanity queries. Use <code>EXPLAIN</code> on at least one non-trivial <code>SELECT</code>.</li>\n<li>If you changed users, log out and verify <strong>least privilege</strong> by connecting as <code>analyst</code>.</li>\n</ul>\n<h3>Phase 5 — Diagram + notes (10 min)</h3>\n<ul>\n<li>Sketch ER diagram on paper; photograph or transcribe key entities and cardinalities.</li>\n</ul>\n<h4>Verify</h4>\n<p>Schema and queries match lab intent; you can draw the <strong>ER diagram</strong> on paper.</p>\n<h4>Common mistakes</h4>\n<p>Mixing quoting for user@host; forgetting <code>FLUSH PRIVILEGES</code> after GRANT changes in some workflows.</p>\n<h4>Going further</h4>\n<p>Read MariaDB explain JSON output once (<code>EXPLAIN FORMAT=JSON</code>).</p>\n<hr>\n</div>","labs-kali-s-12":"<p class=\"lh-tg-source\"><strong>Lab S-12 — Parameterized queries from Python</strong> · <span class=\"lh-tg-chapter\">Part 5 — SQL / MariaDB (on Kali)</span> · Lesson <code>labs-kali-s-12</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab S-12 — Parameterized queries from Python</h3>\n<p><strong>Suggested pace:</strong> 70–110 minutes</p>\n<p><strong>Goal:</strong> Execute the relational modeling and SQL skills for this step—see procedure.</p>\n<h4>Why this lab exists</h4>\n<p>Databases reward <strong>slow thinking</strong>: schema first, constraints second, queries third.</p>\n<h4>Background</h4>\n<p><strong>MariaDB</strong> speaks the same client protocol as MySQL for most learner tasks. The <strong><code>mariadb</code></strong> client connects to the server; <strong><code>sudo mariadb</code></strong> typically opens a local <strong>root</strong> administrative session on fresh installs. Always prefer <strong>parameterized</strong> queries in application code (covered in a later lab).</p>\n<h4>Commands explained (read before you type)</h4>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo mariadb\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Opens the MariaDB client as the privileged local admin (typical Kali install).</li>\n<li><strong>Why we use it here:</strong> You need DDL privileges to create databases and users.</li>\n</ul>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Session + safety (10 min)</h3>\n<ol>\n<li>Open <strong>two</strong> terminals: one <code>sudo mariadb</code> (admin), one normal shell for <code>mysqldump</code> / editors.</li>\n<li>Before destructive DDL, run <code>SHOW CREATE TABLE …</code> or copy schema with <code>mysqldump --no-data</code> when appropriate.</li>\n</ol>\n<h3>Phase 2 — Vocabulary (10 min)</h3>\n<ul>\n<li>Classify each statement you will run as <strong>DDL</strong> (schema), <strong>DML</strong> (data), or <strong>DCL</strong> (permissions).</li>\n<li>Write one sentence on why <code>FLUSH PRIVILEGES;</code> exists after <code>GRANT</code>.</li>\n</ul>\n<h3>Phase 3 — Execute the lab</h3>\n<p><strong>S-12 — Python parameterized inserts</strong></p>\n<p>Install <code>PyMySQL</code> in venv. Script inserts a row using <strong>tuple parameters</strong>—show the query string uses <code>%s</code> placeholders only.\nDemonstrate that string concatenation with user input would be unsafe (theory + tiny safe example).</p>\n<h3>Phase 4 — Integrity checks (15 min)</h3>\n<ul>\n<li>After inserts, run <code>SELECT … COUNT(*)</code> sanity queries. Use <code>EXPLAIN</code> on at least one non-trivial <code>SELECT</code>.</li>\n<li>If you changed users, log out and verify <strong>least privilege</strong> by connecting as <code>analyst</code>.</li>\n</ul>\n<h3>Phase 5 — Diagram + notes (10 min)</h3>\n<ul>\n<li>Sketch ER diagram on paper; photograph or transcribe key entities and cardinalities.</li>\n</ul>\n<h4>Verify</h4>\n<p>Schema and queries match lab intent; you can draw the <strong>ER diagram</strong> on paper.</p>\n<h4>Common mistakes</h4>\n<p>Mixing quoting for user@host; forgetting <code>FLUSH PRIVILEGES</code> after GRANT changes in some workflows.</p>\n<h4>Going further</h4>\n<p>Read MariaDB explain JSON output once (<code>EXPLAIN FORMAT=JSON</code>).</p>\n<hr>\n</div>","labs-kali-sec-01":"<p class=\"lh-tg-source\"><strong>Lab SEC-01 — Threat modeling STRIDE on a fake web app</strong> · <span class=\"lh-tg-chapter\">Part 6 — Security+ aligned (on Kali)</span> · Lesson <code>labs-kali-sec-01</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab SEC-01 — Threat modeling STRIDE on a fake web app</h3>\n<p><strong>Suggested pace:</strong> 75–130 minutes</p>\n<p><strong>Goal:</strong> Build Security+ aligned instincts: evidence, least privilege, and safe lab boundaries.</p>\n<h4>Why this lab exists</h4>\n<p><strong>Ethics first:</strong> only targets you own or have <strong>written authorization</strong> to test. These labs default to <strong>localhost</strong> and learning exercises.</p>\n<h4>Background</h4>\n<p>Security work is <strong>evidence-based</strong>. When a command could affect remote systems, <strong>stop</strong> and validate scope. Prefer snapshots before firewall or SSH changes.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Ethics + scope (10 min)</h3>\n<ul>\n<li>Write <strong>scope</strong> in your notebook: “I will only touch 127.0.0.1 / my VM / files I own.” Sign it with the date.</li>\n<li>If a command could touch remote systems, <strong>stop</strong> and re-read the man page SYNOPSIS.</li>\n</ul>\n<h3>Phase 2 — Evidence discipline (10 min)</h3>\n<ul>\n<li>Decide filename convention for transcripts (<code>~/labs/securityplus/SEC-NN-notes.txt</code>).</li>\n<li>For every tool, save <strong>command + first 20 lines of output</strong>.</li>\n</ul>\n<h3>Phase 3 — Hands-on work</h3>\n<p><strong>STRIDE table for <code>/var/www/html/site</code></strong></p>\n<p>Build a six-row table (S,T,R,I,D,E) with <strong>one realistic threat</strong> and <strong>one mitigation</strong> each tied to your actual files (forms, static assets, nav links).\nReference OWASP or NIST language loosely—accuracy over buzzwords.</p>\n<h3>Phase 4 — Control mapping (15 min)</h3>\n<ul>\n<li>For each major action, label it <strong>prevent / detect / respond</strong> in NIST CSF terms (high level).</li>\n</ul>\n<h3>Phase 5 — Lessons learned (10 min)</h3>\n<ul>\n<li>What would you automate next time? What would you <strong>never</strong> do on production without CAB approval?</li>\n</ul>\n<h4>Verify</h4>\n<p>Artifacts in notes: commands, outputs, and a short risk statement.</p>\n<h4>Common mistakes</h4>\n<p>Running scanners against networks you do not own—<strong>illegal</strong> and out of scope.</p>\n<h4>Going further</h4>\n<p>Map this lab to a Security+ objective domain in your own words.</p>\n<hr>\n</div>","labs-kali-sec-02":"<p class=\"lh-tg-source\"><strong>Lab SEC-02 — Hashing and password storage concepts</strong> · <span class=\"lh-tg-chapter\">Part 6 — Security+ aligned (on Kali)</span> · Lesson <code>labs-kali-sec-02</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab SEC-02 — Hashing and password storage concepts</h3>\n<p><strong>Suggested pace:</strong> 75–130 minutes</p>\n<p><strong>Goal:</strong> Build Security+ aligned instincts: evidence, least privilege, and safe lab boundaries.</p>\n<h4>Why this lab exists</h4>\n<p><strong>Ethics first:</strong> only targets you own or have <strong>written authorization</strong> to test. These labs default to <strong>localhost</strong> and learning exercises.</p>\n<h4>Background</h4>\n<p>Security work is <strong>evidence-based</strong>. When a command could affect remote systems, <strong>stop</strong> and validate scope. Prefer snapshots before firewall or SSH changes.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Ethics + scope (10 min)</h3>\n<ul>\n<li>Write <strong>scope</strong> in your notebook: “I will only touch 127.0.0.1 / my VM / files I own.” Sign it with the date.</li>\n<li>If a command could touch remote systems, <strong>stop</strong> and re-read the man page SYNOPSIS.</li>\n</ul>\n<h3>Phase 2 — Evidence discipline (10 min)</h3>\n<ul>\n<li>Decide filename convention for transcripts (<code>~/labs/securityplus/SEC-NN-notes.txt</code>).</li>\n<li>For every tool, save <strong>command + first 20 lines of output</strong>.</li>\n</ul>\n<h3>Phase 3 — Hands-on work</h3>\n<p><strong>Hash identification (safe)</strong></p>\n<p>Use <code>hashid</code> (if installed) or a short Python script to <strong>identify</strong> algorithm family from sample strings your instructor provides.\nWrite why <strong>rainbow tables</strong> matter for unsalted MD5 but not for modern password hashes.</p>\n<h3>Phase 4 — Control mapping (15 min)</h3>\n<ul>\n<li>For each major action, label it <strong>prevent / detect / respond</strong> in NIST CSF terms (high level).</li>\n</ul>\n<h3>Phase 5 — Lessons learned (10 min)</h3>\n<ul>\n<li>What would you automate next time? What would you <strong>never</strong> do on production without CAB approval?</li>\n</ul>\n<h4>Verify</h4>\n<p>Artifacts in notes: commands, outputs, and a short risk statement.</p>\n<h4>Common mistakes</h4>\n<p>Running scanners against networks you do not own—<strong>illegal</strong> and out of scope.</p>\n<h4>Going further</h4>\n<p>Map this lab to a Security+ objective domain in your own words.</p>\n<hr>\n</div>","labs-kali-sec-03":"<p class=\"lh-tg-source\"><strong>Lab SEC-03 — Symmetric encryption with OpenSSL</strong> · <span class=\"lh-tg-chapter\">Part 6 — Security+ aligned (on Kali)</span> · Lesson <code>labs-kali-sec-03</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab SEC-03 — Symmetric encryption with OpenSSL</h3>\n<p><strong>Suggested pace:</strong> 75–130 minutes</p>\n<p><strong>Goal:</strong> Build Security+ aligned instincts: evidence, least privilege, and safe lab boundaries.</p>\n<h4>Why this lab exists</h4>\n<p><strong>Ethics first:</strong> only targets you own or have <strong>written authorization</strong> to test. These labs default to <strong>localhost</strong> and learning exercises.</p>\n<h4>Background</h4>\n<p>Security work is <strong>evidence-based</strong>. When a command could affect remote systems, <strong>stop</strong> and validate scope. Prefer snapshots before firewall or SSH changes.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Ethics + scope (10 min)</h3>\n<ul>\n<li>Write <strong>scope</strong> in your notebook: “I will only touch 127.0.0.1 / my VM / files I own.” Sign it with the date.</li>\n<li>If a command could touch remote systems, <strong>stop</strong> and re-read the man page SYNOPSIS.</li>\n</ul>\n<h3>Phase 2 — Evidence discipline (10 min)</h3>\n<ul>\n<li>Decide filename convention for transcripts (<code>~/labs/securityplus/SEC-NN-notes.txt</code>).</li>\n<li>For every tool, save <strong>command + first 20 lines of output</strong>.</li>\n</ul>\n<h3>Phase 3 — Hands-on work</h3>\n<p><strong>OpenSSL symmetric encrypt/decrypt</strong></p>\n<p>Create <code>/tmp/secret.txt</code>. Encrypt with <code>openssl enc -aes-256-gcm</code> (read <code>man enc</code> for Kali’s supported syntax).\nDecrypt to a new file; <code>cmp</code> or <code>sha256sum</code> both files. Document <strong>where the key lived</strong> and why stdin keys are risky.</p>\n<h3>Phase 4 — Control mapping (15 min)</h3>\n<ul>\n<li>For each major action, label it <strong>prevent / detect / respond</strong> in NIST CSF terms (high level).</li>\n</ul>\n<h3>Phase 5 — Lessons learned (10 min)</h3>\n<ul>\n<li>What would you automate next time? What would you <strong>never</strong> do on production without CAB approval?</li>\n</ul>\n<h4>Verify</h4>\n<p>Artifacts in notes: commands, outputs, and a short risk statement.</p>\n<h4>Common mistakes</h4>\n<p>Running scanners against networks you do not own—<strong>illegal</strong> and out of scope.</p>\n<h4>Going further</h4>\n<p>Map this lab to a Security+ objective domain in your own words.</p>\n<hr>\n</div>","labs-kali-sec-04":"<p class=\"lh-tg-source\"><strong>Lab SEC-04 — TLS inspection with openssl s_client</strong> · <span class=\"lh-tg-chapter\">Part 6 — Security+ aligned (on Kali)</span> · Lesson <code>labs-kali-sec-04</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab SEC-04 — TLS inspection with openssl s_client</h3>\n<p><strong>Suggested pace:</strong> 75–130 minutes</p>\n<p><strong>Goal:</strong> Build Security+ aligned instincts: evidence, least privilege, and safe lab boundaries.</p>\n<h4>Why this lab exists</h4>\n<p><strong>Ethics first:</strong> only targets you own or have <strong>written authorization</strong> to test. These labs default to <strong>localhost</strong> and learning exercises.</p>\n<h4>Background</h4>\n<p>Security work is <strong>evidence-based</strong>. When a command could affect remote systems, <strong>stop</strong> and validate scope. Prefer snapshots before firewall or SSH changes.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Ethics + scope (10 min)</h3>\n<ul>\n<li>Write <strong>scope</strong> in your notebook: “I will only touch 127.0.0.1 / my VM / files I own.” Sign it with the date.</li>\n<li>If a command could touch remote systems, <strong>stop</strong> and re-read the man page SYNOPSIS.</li>\n</ul>\n<h3>Phase 2 — Evidence discipline (10 min)</h3>\n<ul>\n<li>Decide filename convention for transcripts (<code>~/labs/securityplus/SEC-NN-notes.txt</code>).</li>\n<li>For every tool, save <strong>command + first 20 lines of output</strong>.</li>\n</ul>\n<h3>Phase 3 — Hands-on work</h3>\n<p><strong>openssl s_client</strong></p>\n<p><code>openssl s_client -connect example.com:443 -servername example.com -brief</code> — capture cipher, protocol version, certificate subject.\nMap one line of output to “certificate transparency” or chain of trust in your notes.</p>\n<h3>Phase 4 — Control mapping (15 min)</h3>\n<ul>\n<li>For each major action, label it <strong>prevent / detect / respond</strong> in NIST CSF terms (high level).</li>\n</ul>\n<h3>Phase 5 — Lessons learned (10 min)</h3>\n<ul>\n<li>What would you automate next time? What would you <strong>never</strong> do on production without CAB approval?</li>\n</ul>\n<h4>Verify</h4>\n<p>Artifacts in notes: commands, outputs, and a short risk statement.</p>\n<h4>Common mistakes</h4>\n<p>Running scanners against networks you do not own—<strong>illegal</strong> and out of scope.</p>\n<h4>Going further</h4>\n<p>Map this lab to a Security+ objective domain in your own words.</p>\n<hr>\n</div>","labs-kali-sec-05":"<p class=\"lh-tg-source\"><strong>Lab SEC-05 — Firewall basics (nftables or ufw)</strong> · <span class=\"lh-tg-chapter\">Part 6 — Security+ aligned (on Kali)</span> · Lesson <code>labs-kali-sec-05</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab SEC-05 — Firewall basics (nftables or ufw)</h3>\n<p><strong>Suggested pace:</strong> 75–130 minutes</p>\n<p><strong>Goal:</strong> Build Security+ aligned instincts: evidence, least privilege, and safe lab boundaries.</p>\n<h4>Why this lab exists</h4>\n<p><strong>Ethics first:</strong> only targets you own or have <strong>written authorization</strong> to test. These labs default to <strong>localhost</strong> and learning exercises.</p>\n<h4>Background</h4>\n<p>Security work is <strong>evidence-based</strong>. When a command could affect remote systems, <strong>stop</strong> and validate scope. Prefer snapshots before firewall or SSH changes.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Ethics + scope (10 min)</h3>\n<ul>\n<li>Write <strong>scope</strong> in your notebook: “I will only touch 127.0.0.1 / my VM / files I own.” Sign it with the date.</li>\n<li>If a command could touch remote systems, <strong>stop</strong> and re-read the man page SYNOPSIS.</li>\n</ul>\n<h3>Phase 2 — Evidence discipline (10 min)</h3>\n<ul>\n<li>Decide filename convention for transcripts (<code>~/labs/securityplus/SEC-NN-notes.txt</code>).</li>\n<li>For every tool, save <strong>command + first 20 lines of output</strong>.</li>\n</ul>\n<h3>Phase 3 — Hands-on work</h3>\n<p><strong>Host firewall</strong></p>\n<p>Choose <code>ufw</code> <strong>or</strong> <code>nftables</code>. Default deny, allow <strong>22/tcp</strong> and <strong>80/tcp</strong> only from your lab subnet or host IP.\n<code>nmap</code> from host before/after; paste diff.</p>\n<h3>Phase 4 — Control mapping (15 min)</h3>\n<ul>\n<li>For each major action, label it <strong>prevent / detect / respond</strong> in NIST CSF terms (high level).</li>\n</ul>\n<h3>Phase 5 — Lessons learned (10 min)</h3>\n<ul>\n<li>What would you automate next time? What would you <strong>never</strong> do on production without CAB approval?</li>\n</ul>\n<h4>Verify</h4>\n<p>Artifacts in notes: commands, outputs, and a short risk statement.</p>\n<h4>Common mistakes</h4>\n<p>Running scanners against networks you do not own—<strong>illegal</strong> and out of scope.</p>\n<h4>Going further</h4>\n<p>Map this lab to a Security+ objective domain in your own words.</p>\n<hr>\n</div>","labs-kali-sec-06":"<p class=\"lh-tg-source\"><strong>Lab SEC-06 — fail2ban-style log review</strong> · <span class=\"lh-tg-chapter\">Part 6 — Security+ aligned (on Kali)</span> · Lesson <code>labs-kali-sec-06</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab SEC-06 — fail2ban-style log review</h3>\n<p><strong>Suggested pace:</strong> 75–130 minutes</p>\n<p><strong>Goal:</strong> Build Security+ aligned instincts: evidence, least privilege, and safe lab boundaries.</p>\n<h4>Why this lab exists</h4>\n<p><strong>Ethics first:</strong> only targets you own or have <strong>written authorization</strong> to test. These labs default to <strong>localhost</strong> and learning exercises.</p>\n<h4>Background</h4>\n<p>Security work is <strong>evidence-based</strong>. When a command could affect remote systems, <strong>stop</strong> and validate scope. Prefer snapshots before firewall or SSH changes.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Ethics + scope (10 min)</h3>\n<ul>\n<li>Write <strong>scope</strong> in your notebook: “I will only touch 127.0.0.1 / my VM / files I own.” Sign it with the date.</li>\n<li>If a command could touch remote systems, <strong>stop</strong> and re-read the man page SYNOPSIS.</li>\n</ul>\n<h3>Phase 2 — Evidence discipline (10 min)</h3>\n<ul>\n<li>Decide filename convention for transcripts (<code>~/labs/securityplus/SEC-NN-notes.txt</code>).</li>\n<li>For every tool, save <strong>command + first 20 lines of output</strong>.</li>\n</ul>\n<h3>Phase 3 — Hands-on work</h3>\n<p><strong>SSH log review</strong></p>\n<p><code>sudo journalctl -u ssh -n 80 --no-pager</code> or <code>/var/log/auth.log</code> — circle <strong>preauth</strong> vs <strong>accepted</strong> lines.\nWrite a one-page “incident note” as if you triaged automated scans.</p>\n<h3>Phase 4 — Control mapping (15 min)</h3>\n<ul>\n<li>For each major action, label it <strong>prevent / detect / respond</strong> in NIST CSF terms (high level).</li>\n</ul>\n<h3>Phase 5 — Lessons learned (10 min)</h3>\n<ul>\n<li>What would you automate next time? What would you <strong>never</strong> do on production without CAB approval?</li>\n</ul>\n<h4>Verify</h4>\n<p>Artifacts in notes: commands, outputs, and a short risk statement.</p>\n<h4>Common mistakes</h4>\n<p>Running scanners against networks you do not own—<strong>illegal</strong> and out of scope.</p>\n<h4>Going further</h4>\n<p>Map this lab to a Security+ objective domain in your own words.</p>\n<hr>\n</div>","labs-kali-sec-07":"<p class=\"lh-tg-source\"><strong>Lab SEC-07 — File permissions and ACLs</strong> · <span class=\"lh-tg-chapter\">Part 6 — Security+ aligned (on Kali)</span> · Lesson <code>labs-kali-sec-07</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab SEC-07 — File permissions and ACLs</h3>\n<p><strong>Suggested pace:</strong> 75–130 minutes</p>\n<p><strong>Goal:</strong> Build Security+ aligned instincts: evidence, least privilege, and safe lab boundaries.</p>\n<h4>Why this lab exists</h4>\n<p><strong>Ethics first:</strong> only targets you own or have <strong>written authorization</strong> to test. These labs default to <strong>localhost</strong> and learning exercises.</p>\n<h4>Background</h4>\n<p>Security work is <strong>evidence-based</strong>. When a command could affect remote systems, <strong>stop</strong> and validate scope. Prefer snapshots before firewall or SSH changes.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Ethics + scope (10 min)</h3>\n<ul>\n<li>Write <strong>scope</strong> in your notebook: “I will only touch 127.0.0.1 / my VM / files I own.” Sign it with the date.</li>\n<li>If a command could touch remote systems, <strong>stop</strong> and re-read the man page SYNOPSIS.</li>\n</ul>\n<h3>Phase 2 — Evidence discipline (10 min)</h3>\n<ul>\n<li>Decide filename convention for transcripts (<code>~/labs/securityplus/SEC-NN-notes.txt</code>).</li>\n<li>For every tool, save <strong>command + first 20 lines of output</strong>.</li>\n</ul>\n<h3>Phase 3 — Hands-on work</h3>\n<p><strong>chmod / umask / ACL demo</strong></p>\n<p>Create dirs under <code>/tmp/lhperm</code>. Show <code>umask</code> effect on new files. Fix a deliberate <code>chmod 777</code> using sane modes.\nOptional: <code>setfacl -m u:analyst:r-- file</code> and <code>getfacl</code>.</p>\n<h3>Phase 4 — Control mapping (15 min)</h3>\n<ul>\n<li>For each major action, label it <strong>prevent / detect / respond</strong> in NIST CSF terms (high level).</li>\n</ul>\n<h3>Phase 5 — Lessons learned (10 min)</h3>\n<ul>\n<li>What would you automate next time? What would you <strong>never</strong> do on production without CAB approval?</li>\n</ul>\n<h4>Verify</h4>\n<p>Artifacts in notes: commands, outputs, and a short risk statement.</p>\n<h4>Common mistakes</h4>\n<p>Running scanners against networks you do not own—<strong>illegal</strong> and out of scope.</p>\n<h4>Going further</h4>\n<p>Map this lab to a Security+ objective domain in your own words.</p>\n<hr>\n</div>","labs-kali-sec-08":"<p class=\"lh-tg-source\"><strong>Lab SEC-08 — Apache service hardening checklist</strong> · <span class=\"lh-tg-chapter\">Part 6 — Security+ aligned (on Kali)</span> · Lesson <code>labs-kali-sec-08</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab SEC-08 — Apache service hardening checklist</h3>\n<p><strong>Suggested pace:</strong> 75–130 minutes</p>\n<p><strong>Goal:</strong> Build Security+ aligned instincts: evidence, least privilege, and safe lab boundaries.</p>\n<h4>Why this lab exists</h4>\n<p><strong>Ethics first:</strong> only targets you own or have <strong>written authorization</strong> to test. These labs default to <strong>localhost</strong> and learning exercises.</p>\n<h4>Background</h4>\n<p>Security work is <strong>evidence-based</strong>. When a command could affect remote systems, <strong>stop</strong> and validate scope. Prefer snapshots before firewall or SSH changes.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Ethics + scope (10 min)</h3>\n<ul>\n<li>Write <strong>scope</strong> in your notebook: “I will only touch 127.0.0.1 / my VM / files I own.” Sign it with the date.</li>\n<li>If a command could touch remote systems, <strong>stop</strong> and re-read the man page SYNOPSIS.</li>\n</ul>\n<h3>Phase 2 — Evidence discipline (10 min)</h3>\n<ul>\n<li>Decide filename convention for transcripts (<code>~/labs/securityplus/SEC-NN-notes.txt</code>).</li>\n<li>For every tool, save <strong>command + first 20 lines of output</strong>.</li>\n</ul>\n<h3>Phase 3 — Hands-on work</h3>\n<p><strong>Apache hardening checklist</strong></p>\n<p>Turn <strong>Indexes</strong> off for a test directory, add <strong>ServerTokens Prod</strong> (if module available), hide version banner where possible.\nEach change: <strong>backup file</strong>, <code>apache2ctl configtest</code>, <code>systemctl reload apache2</code>.</p>\n<h3>Phase 4 — Control mapping (15 min)</h3>\n<ul>\n<li>For each major action, label it <strong>prevent / detect / respond</strong> in NIST CSF terms (high level).</li>\n</ul>\n<h3>Phase 5 — Lessons learned (10 min)</h3>\n<ul>\n<li>What would you automate next time? What would you <strong>never</strong> do on production without CAB approval?</li>\n</ul>\n<h4>Verify</h4>\n<p>Artifacts in notes: commands, outputs, and a short risk statement.</p>\n<h4>Common mistakes</h4>\n<p>Running scanners against networks you do not own—<strong>illegal</strong> and out of scope.</p>\n<h4>Going further</h4>\n<p>Map this lab to a Security+ objective domain in your own words.</p>\n<hr>\n</div>","labs-kali-sec-09":"<p class=\"lh-tg-source\"><strong>Lab SEC-09 — Wireshark: follow a TCP stream</strong> · <span class=\"lh-tg-chapter\">Part 6 — Security+ aligned (on Kali)</span> · Lesson <code>labs-kali-sec-09</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab SEC-09 — Wireshark: follow a TCP stream</h3>\n<p><strong>Suggested pace:</strong> 75–130 minutes</p>\n<p><strong>Goal:</strong> Build Security+ aligned instincts: evidence, least privilege, and safe lab boundaries.</p>\n<h4>Why this lab exists</h4>\n<p><strong>Ethics first:</strong> only targets you own or have <strong>written authorization</strong> to test. These labs default to <strong>localhost</strong> and learning exercises.</p>\n<h4>Background</h4>\n<p>Security work is <strong>evidence-based</strong>. When a command could affect remote systems, <strong>stop</strong> and validate scope. Prefer snapshots before firewall or SSH changes.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Ethics + scope (10 min)</h3>\n<ul>\n<li>Write <strong>scope</strong> in your notebook: “I will only touch 127.0.0.1 / my VM / files I own.” Sign it with the date.</li>\n<li>If a command could touch remote systems, <strong>stop</strong> and re-read the man page SYNOPSIS.</li>\n</ul>\n<h3>Phase 2 — Evidence discipline (10 min)</h3>\n<ul>\n<li>Decide filename convention for transcripts (<code>~/labs/securityplus/SEC-NN-notes.txt</code>).</li>\n<li>For every tool, save <strong>command + first 20 lines of output</strong>.</li>\n</ul>\n<h3>Phase 3 — Hands-on work</h3>\n<p><strong>Wireshark TCP stream</strong></p>\n<p>Capture loopback while <code>curl http://127.0.0.1/</code>. Follow <strong>TCP stream</strong>; highlight request line + Host header.\nExplain when TLS would change what you see.</p>\n<h3>Phase 4 — Control mapping (15 min)</h3>\n<ul>\n<li>For each major action, label it <strong>prevent / detect / respond</strong> in NIST CSF terms (high level).</li>\n</ul>\n<h3>Phase 5 — Lessons learned (10 min)</h3>\n<ul>\n<li>What would you automate next time? What would you <strong>never</strong> do on production without CAB approval?</li>\n</ul>\n<h4>Verify</h4>\n<p>Artifacts in notes: commands, outputs, and a short risk statement.</p>\n<h4>Common mistakes</h4>\n<p>Running scanners against networks you do not own—<strong>illegal</strong> and out of scope.</p>\n<h4>Going further</h4>\n<p>Map this lab to a Security+ objective domain in your own words.</p>\n<hr>\n</div>","labs-kali-sec-10":"<p class=\"lh-tg-source\"><strong>Lab SEC-10 — DNS recon (passive)</strong> · <span class=\"lh-tg-chapter\">Part 6 — Security+ aligned (on Kali)</span> · Lesson <code>labs-kali-sec-10</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab SEC-10 — DNS recon (passive)</h3>\n<p><strong>Suggested pace:</strong> 75–130 minutes</p>\n<p><strong>Goal:</strong> Build Security+ aligned instincts: evidence, least privilege, and safe lab boundaries.</p>\n<h4>Why this lab exists</h4>\n<p><strong>Ethics first:</strong> only targets you own or have <strong>written authorization</strong> to test. These labs default to <strong>localhost</strong> and learning exercises.</p>\n<h4>Background</h4>\n<p>Security work is <strong>evidence-based</strong>. When a command could affect remote systems, <strong>stop</strong> and validate scope. Prefer snapshots before firewall or SSH changes.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Ethics + scope (10 min)</h3>\n<ul>\n<li>Write <strong>scope</strong> in your notebook: “I will only touch 127.0.0.1 / my VM / files I own.” Sign it with the date.</li>\n<li>If a command could touch remote systems, <strong>stop</strong> and re-read the man page SYNOPSIS.</li>\n</ul>\n<h3>Phase 2 — Evidence discipline (10 min)</h3>\n<ul>\n<li>Decide filename convention for transcripts (<code>~/labs/securityplus/SEC-NN-notes.txt</code>).</li>\n<li>For every tool, save <strong>command + first 20 lines of output</strong>.</li>\n</ul>\n<h3>Phase 3 — Hands-on work</h3>\n<p><strong>Passive DNS</strong></p>\n<p><code>dig +trace</code> vs recursive query. Compare answers from <code>8.8.8.8</code> vs your local resolver if safe.\nDocument <strong>ethical</strong> use—no hammering authoritative servers.</p>\n<h3>Phase 4 — Control mapping (15 min)</h3>\n<ul>\n<li>For each major action, label it <strong>prevent / detect / respond</strong> in NIST CSF terms (high level).</li>\n</ul>\n<h3>Phase 5 — Lessons learned (10 min)</h3>\n<ul>\n<li>What would you automate next time? What would you <strong>never</strong> do on production without CAB approval?</li>\n</ul>\n<h4>Verify</h4>\n<p>Artifacts in notes: commands, outputs, and a short risk statement.</p>\n<h4>Common mistakes</h4>\n<p>Running scanners against networks you do not own—<strong>illegal</strong> and out of scope.</p>\n<h4>Going further</h4>\n<p>Map this lab to a Security+ objective domain in your own words.</p>\n<hr>\n</div>","labs-kali-sec-11":"<p class=\"lh-tg-source\"><strong>Lab SEC-11 — Certificate pinning concepts</strong> · <span class=\"lh-tg-chapter\">Part 6 — Security+ aligned (on Kali)</span> · Lesson <code>labs-kali-sec-11</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab SEC-11 — Certificate pinning concepts</h3>\n<p><strong>Suggested pace:</strong> 75–130 minutes</p>\n<p><strong>Goal:</strong> Build Security+ aligned instincts: evidence, least privilege, and safe lab boundaries.</p>\n<h4>Why this lab exists</h4>\n<p><strong>Ethics first:</strong> only targets you own or have <strong>written authorization</strong> to test. These labs default to <strong>localhost</strong> and learning exercises.</p>\n<h4>Background</h4>\n<p>Security work is <strong>evidence-based</strong>. When a command could affect remote systems, <strong>stop</strong> and validate scope. Prefer snapshots before firewall or SSH changes.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Ethics + scope (10 min)</h3>\n<ul>\n<li>Write <strong>scope</strong> in your notebook: “I will only touch 127.0.0.1 / my VM / files I own.” Sign it with the date.</li>\n<li>If a command could touch remote systems, <strong>stop</strong> and re-read the man page SYNOPSIS.</li>\n</ul>\n<h3>Phase 2 — Evidence discipline (10 min)</h3>\n<ul>\n<li>Decide filename convention for transcripts (<code>~/labs/securityplus/SEC-NN-notes.txt</code>).</li>\n<li>For every tool, save <strong>command + first 20 lines of output</strong>.</li>\n</ul>\n<h3>Phase 3 — Hands-on work</h3>\n<p><strong>Pinning discussion + curl</strong></p>\n<p>Essay (300–500 words): mobile vs browser pinning tradeoffs. If feasible, demo <code>curl --pinnedpubkey</code> against a host; otherwise dry-run with docs.</p>\n<h3>Phase 4 — Control mapping (15 min)</h3>\n<ul>\n<li>For each major action, label it <strong>prevent / detect / respond</strong> in NIST CSF terms (high level).</li>\n</ul>\n<h3>Phase 5 — Lessons learned (10 min)</h3>\n<ul>\n<li>What would you automate next time? What would you <strong>never</strong> do on production without CAB approval?</li>\n</ul>\n<h4>Verify</h4>\n<p>Artifacts in notes: commands, outputs, and a short risk statement.</p>\n<h4>Common mistakes</h4>\n<p>Running scanners against networks you do not own—<strong>illegal</strong> and out of scope.</p>\n<h4>Going further</h4>\n<p>Map this lab to a Security+ objective domain in your own words.</p>\n<hr>\n</div>","labs-kali-sec-12":"<p class=\"lh-tg-source\"><strong>Lab SEC-12 — Incident response tabletop + IOC table</strong> · <span class=\"lh-tg-chapter\">Part 6 — Security+ aligned (on Kali)</span> · Lesson <code>labs-kali-sec-12</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab SEC-12 — Incident response tabletop + IOC table</h3>\n<p><strong>Suggested pace:</strong> 75–130 minutes</p>\n<p><strong>Goal:</strong> Build Security+ aligned instincts: evidence, least privilege, and safe lab boundaries.</p>\n<h4>Why this lab exists</h4>\n<p><strong>Ethics first:</strong> only targets you own or have <strong>written authorization</strong> to test. These labs default to <strong>localhost</strong> and learning exercises.</p>\n<h4>Background</h4>\n<p>Security work is <strong>evidence-based</strong>. When a command could affect remote systems, <strong>stop</strong> and validate scope. Prefer snapshots before firewall or SSH changes.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Ethics + scope (10 min)</h3>\n<ul>\n<li>Write <strong>scope</strong> in your notebook: “I will only touch 127.0.0.1 / my VM / files I own.” Sign it with the date.</li>\n<li>If a command could touch remote systems, <strong>stop</strong> and re-read the man page SYNOPSIS.</li>\n</ul>\n<h3>Phase 2 — Evidence discipline (10 min)</h3>\n<ul>\n<li>Decide filename convention for transcripts (<code>~/labs/securityplus/SEC-NN-notes.txt</code>).</li>\n<li>For every tool, save <strong>command + first 20 lines of output</strong>.</li>\n</ul>\n<h3>Phase 3 — Hands-on work</h3>\n<p><strong>IR tabletop + IOC MariaDB table</strong></p>\n<p>Write phishing timeline. Create <code>iocs</code> table (type, value, first_seen, source). Insert three IOC rows.\nLink (conceptually) to incidents table if you already built it.</p>\n<h3>Phase 4 — Control mapping (15 min)</h3>\n<ul>\n<li>For each major action, label it <strong>prevent / detect / respond</strong> in NIST CSF terms (high level).</li>\n</ul>\n<h3>Phase 5 — Lessons learned (10 min)</h3>\n<ul>\n<li>What would you automate next time? What would you <strong>never</strong> do on production without CAB approval?</li>\n</ul>\n<h4>Verify</h4>\n<p>Artifacts in notes: commands, outputs, and a short risk statement.</p>\n<h4>Common mistakes</h4>\n<p>Running scanners against networks you do not own—<strong>illegal</strong> and out of scope.</p>\n<h4>Going further</h4>\n<p>Map this lab to a Security+ objective domain in your own words.</p>\n<hr>\n</div>","labs-kali-sec-13":"<p class=\"lh-tg-source\"><strong>Lab SEC-13 — Secure baselines (Ansible-style outline)</strong> · <span class=\"lh-tg-chapter\">Part 6 — Security+ aligned (on Kali)</span> · Lesson <code>labs-kali-sec-13</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab SEC-13 — Secure baselines (Ansible-style outline)</h3>\n<p><strong>Suggested pace:</strong> 75–130 minutes</p>\n<p><strong>Goal:</strong> Build Security+ aligned instincts: evidence, least privilege, and safe lab boundaries.</p>\n<h4>Why this lab exists</h4>\n<p><strong>Ethics first:</strong> only targets you own or have <strong>written authorization</strong> to test. These labs default to <strong>localhost</strong> and learning exercises.</p>\n<h4>Background</h4>\n<p>Security work is <strong>evidence-based</strong>. When a command could affect remote systems, <strong>stop</strong> and validate scope. Prefer snapshots before firewall or SSH changes.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Ethics + scope (10 min)</h3>\n<ul>\n<li>Write <strong>scope</strong> in your notebook: “I will only touch 127.0.0.1 / my VM / files I own.” Sign it with the date.</li>\n<li>If a command could touch remote systems, <strong>stop</strong> and re-read the man page SYNOPSIS.</li>\n</ul>\n<h3>Phase 2 — Evidence discipline (10 min)</h3>\n<ul>\n<li>Decide filename convention for transcripts (<code>~/labs/securityplus/SEC-NN-notes.txt</code>).</li>\n<li>For every tool, save <strong>command + first 20 lines of output</strong>.</li>\n</ul>\n<h3>Phase 3 — Hands-on work</h3>\n<p><strong>Baseline outline</strong></p>\n<p>Numbered list (20+ steps) for imaging a fresh Kali: users, sudo, updates, services to disable, auditd/syslog, backup.\nMap each group to CIS or STIG <strong>functions</strong> at high level.</p>\n<h3>Phase 4 — Control mapping (15 min)</h3>\n<ul>\n<li>For each major action, label it <strong>prevent / detect / respond</strong> in NIST CSF terms (high level).</li>\n</ul>\n<h3>Phase 5 — Lessons learned (10 min)</h3>\n<ul>\n<li>What would you automate next time? What would you <strong>never</strong> do on production without CAB approval?</li>\n</ul>\n<h4>Verify</h4>\n<p>Artifacts in notes: commands, outputs, and a short risk statement.</p>\n<h4>Common mistakes</h4>\n<p>Running scanners against networks you do not own—<strong>illegal</strong> and out of scope.</p>\n<h4>Going further</h4>\n<p>Map this lab to a Security+ objective domain in your own words.</p>\n<hr>\n</div>","labs-kali-sec-14":"<p class=\"lh-tg-source\"><strong>Lab SEC-14 — Log aggregation design</strong> · <span class=\"lh-tg-chapter\">Part 6 — Security+ aligned (on Kali)</span> · Lesson <code>labs-kali-sec-14</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab SEC-14 — Log aggregation design</h3>\n<p><strong>Suggested pace:</strong> 75–130 minutes</p>\n<p><strong>Goal:</strong> Build Security+ aligned instincts: evidence, least privilege, and safe lab boundaries.</p>\n<h4>Why this lab exists</h4>\n<p><strong>Ethics first:</strong> only targets you own or have <strong>written authorization</strong> to test. These labs default to <strong>localhost</strong> and learning exercises.</p>\n<h4>Background</h4>\n<p>Security work is <strong>evidence-based</strong>. When a command could affect remote systems, <strong>stop</strong> and validate scope. Prefer snapshots before firewall or SSH changes.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Ethics + scope (10 min)</h3>\n<ul>\n<li>Write <strong>scope</strong> in your notebook: “I will only touch 127.0.0.1 / my VM / files I own.” Sign it with the date.</li>\n<li>If a command could touch remote systems, <strong>stop</strong> and re-read the man page SYNOPSIS.</li>\n</ul>\n<h3>Phase 2 — Evidence discipline (10 min)</h3>\n<ul>\n<li>Decide filename convention for transcripts (<code>~/labs/securityplus/SEC-NN-notes.txt</code>).</li>\n<li>For every tool, save <strong>command + first 20 lines of output</strong>.</li>\n</ul>\n<h3>Phase 3 — Hands-on work</h3>\n<p><strong>Log aggregation sketch</strong></p>\n<p>Draw (ASCII) rsyslog forwarding to a collector VM. List <strong>three correlation keys</strong> (timestamp, host, session id).\nOptional: <code>logger</code> test message and show it in <code>journalctl</code>.</p>\n<h3>Phase 4 — Control mapping (15 min)</h3>\n<ul>\n<li>For each major action, label it <strong>prevent / detect / respond</strong> in NIST CSF terms (high level).</li>\n</ul>\n<h3>Phase 5 — Lessons learned (10 min)</h3>\n<ul>\n<li>What would you automate next time? What would you <strong>never</strong> do on production without CAB approval?</li>\n</ul>\n<h4>Verify</h4>\n<p>Artifacts in notes: commands, outputs, and a short risk statement.</p>\n<h4>Common mistakes</h4>\n<p>Running scanners against networks you do not own—<strong>illegal</strong> and out of scope.</p>\n<h4>Going further</h4>\n<p>Map this lab to a Security+ objective domain in your own words.</p>\n<hr>\n</div>","labs-kali-t-01":"<p class=\"lh-tg-source\"><strong>Lab T-01 — Filesystem treasure hunt</strong> · <span class=\"lh-tg-chapter\">Part 7 — Tech+ / IT fundamentals (on Kali)</span> · Lesson <code>labs-kali-t-01</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab T-01 — Filesystem treasure hunt</h3>\n<p><strong>Suggested pace:</strong> 60–95 minutes</p>\n<p><strong>Goal:</strong> Strengthen Linux administration fundamentals that underpin every other lab.</p>\n<h4>Why this lab exists</h4>\n<p>Slow OS fluency pays compounding returns: when web or DB labs fail, you will <strong>know where to look</strong>.</p>\n<h4>Background</h4>\n<p>Treat the shell as a <strong>precision instrument</strong>: read man pages (<code>man systemd.service</code>), use <code>--help</code>, and prefer non-destructive probes (<code>ls</code>, <code>stat</code>, <code>ss -lntp</code>) before changes.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Read the manual first (10 min)</h3>\n<ul>\n<li>Identify <strong>one</strong> man page relevant to today’s topic; read <strong>NAME</strong>, <strong>SYNOPSIS</strong>, and <strong>DESCRIPTION</strong> first paragraphs.</li>\n</ul>\n<h3>Phase 2 — Predict outputs (10 min)</h3>\n<ul>\n<li>Before each command, write your <strong>expected</strong> substring in the output.</li>\n</ul>\n<h3>Phase 3 — Execute + capture</h3>\n<p><strong>find / locate / tree</strong></p>\n<p><code>man find</code> first: run five different predicates (<code>-name</code>, <code>-path</code>, <code>-mtime</code>, <code>-size</code>, <code>-perm</code>).\n<code>sudo updatedb</code> then <code>locate passwd</code> (read-only). <code>tree -L 2 /etc/systemd</code>.\nNotebook: when <strong>find</strong> vs <strong>locate</strong> is appropriate.</p>\n<h3>Phase 4 — Correlation (15 min)</h3>\n<ul>\n<li>Tie at least two observations together (e.g. disk free space vs largest mount; process RSS vs available RAM).</li>\n</ul>\n<h3>Phase 5 — Runbook snippet (10 min)</h3>\n<ul>\n<li>Produce <strong>three</strong> bullet “if user reports X, run Y” steps grounded in what you actually ran.</li>\n</ul>\n<h4>Verify</h4>\n<p>You can explain <strong>one diagram</strong> in prose: what changed on disk or network and why it matters.</p>\n<h4>Common mistakes</h4>\n<p>Editing <code>/etc</code> without backup—always <code>sudo cp</code> first.</p>\n<h4>Going further</h4>\n<p>Link this lab to a Tech+ chapter you studied in the main track.</p>\n<hr>\n</div>","labs-kali-t-02":"<p class=\"lh-tg-source\"><strong>Lab T-02 — Package management mastery</strong> · <span class=\"lh-tg-chapter\">Part 7 — Tech+ / IT fundamentals (on Kali)</span> · Lesson <code>labs-kali-t-02</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab T-02 — Package management mastery</h3>\n<p><strong>Suggested pace:</strong> 60–95 minutes</p>\n<p><strong>Goal:</strong> Strengthen Linux administration fundamentals that underpin every other lab.</p>\n<h4>Why this lab exists</h4>\n<p>Slow OS fluency pays compounding returns: when web or DB labs fail, you will <strong>know where to look</strong>.</p>\n<h4>Background</h4>\n<p>Treat the shell as a <strong>precision instrument</strong>: read man pages (<code>man systemd.service</code>), use <code>--help</code>, and prefer non-destructive probes (<code>ls</code>, <code>stat</code>, <code>ss -lntp</code>) before changes.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Read the manual first (10 min)</h3>\n<ul>\n<li>Identify <strong>one</strong> man page relevant to today’s topic; read <strong>NAME</strong>, <strong>SYNOPSIS</strong>, and <strong>DESCRIPTION</strong> first paragraphs.</li>\n</ul>\n<h3>Phase 2 — Predict outputs (10 min)</h3>\n<ul>\n<li>Before each command, write your <strong>expected</strong> substring in the output.</li>\n</ul>\n<h3>Phase 3 — Execute + capture</h3>\n<p><strong>apt workflow</strong></p>\n<p><code>apt-cache policy apache2</code>, <code>apt show apache2 | sed -n &#39;1,25p&#39;</code>. Install then remove a harmless package (<code>sl</code> or similar).\nRead <code>/var/log/apt/history.log</code> excerpt after.</p>\n<h3>Phase 4 — Correlation (15 min)</h3>\n<ul>\n<li>Tie at least two observations together (e.g. disk free space vs largest mount; process RSS vs available RAM).</li>\n</ul>\n<h3>Phase 5 — Runbook snippet (10 min)</h3>\n<ul>\n<li>Produce <strong>three</strong> bullet “if user reports X, run Y” steps grounded in what you actually ran.</li>\n</ul>\n<h4>Verify</h4>\n<p>You can explain <strong>one diagram</strong> in prose: what changed on disk or network and why it matters.</p>\n<h4>Common mistakes</h4>\n<p>Editing <code>/etc</code> without backup—always <code>sudo cp</code> first.</p>\n<h4>Going further</h4>\n<p>Link this lab to a Tech+ chapter you studied in the main track.</p>\n<hr>\n</div>","labs-kali-t-03":"<p class=\"lh-tg-source\"><strong>Lab T-03 — Users and groups</strong> · <span class=\"lh-tg-chapter\">Part 7 — Tech+ / IT fundamentals (on Kali)</span> · Lesson <code>labs-kali-t-03</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab T-03 — Users and groups</h3>\n<p><strong>Suggested pace:</strong> 60–95 minutes</p>\n<p><strong>Goal:</strong> Strengthen Linux administration fundamentals that underpin every other lab.</p>\n<h4>Why this lab exists</h4>\n<p>Slow OS fluency pays compounding returns: when web or DB labs fail, you will <strong>know where to look</strong>.</p>\n<h4>Background</h4>\n<p>Treat the shell as a <strong>precision instrument</strong>: read man pages (<code>man systemd.service</code>), use <code>--help</code>, and prefer non-destructive probes (<code>ls</code>, <code>stat</code>, <code>ss -lntp</code>) before changes.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Read the manual first (10 min)</h3>\n<ul>\n<li>Identify <strong>one</strong> man page relevant to today’s topic; read <strong>NAME</strong>, <strong>SYNOPSIS</strong>, and <strong>DESCRIPTION</strong> first paragraphs.</li>\n</ul>\n<h3>Phase 2 — Predict outputs (10 min)</h3>\n<ul>\n<li>Before each command, write your <strong>expected</strong> substring in the output.</li>\n</ul>\n<h3>Phase 3 — Execute + capture</h3>\n<p><strong>Users and groups</strong></p>\n<p><code>sudo adduser techlab</code> (interactive). <code>sudo usermod -aG sudo techlab</code> <strong>or</strong> a custom group—justify choice.\nCompare <code>su -</code> vs <code>sudo -i</code> in your notes (environment, audit trail).</p>\n<h3>Phase 4 — Correlation (15 min)</h3>\n<ul>\n<li>Tie at least two observations together (e.g. disk free space vs largest mount; process RSS vs available RAM).</li>\n</ul>\n<h3>Phase 5 — Runbook snippet (10 min)</h3>\n<ul>\n<li>Produce <strong>three</strong> bullet “if user reports X, run Y” steps grounded in what you actually ran.</li>\n</ul>\n<h4>Verify</h4>\n<p>You can explain <strong>one diagram</strong> in prose: what changed on disk or network and why it matters.</p>\n<h4>Common mistakes</h4>\n<p>Editing <code>/etc</code> without backup—always <code>sudo cp</code> first.</p>\n<h4>Going further</h4>\n<p>Link this lab to a Tech+ chapter you studied in the main track.</p>\n<hr>\n</div>","labs-kali-t-04":"<p class=\"lh-tg-source\"><strong>Lab T-04 — Disk and memory (user level)</strong> · <span class=\"lh-tg-chapter\">Part 7 — Tech+ / IT fundamentals (on Kali)</span> · Lesson <code>labs-kali-t-04</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab T-04 — Disk and memory (user level)</h3>\n<p><strong>Suggested pace:</strong> 60–95 minutes</p>\n<p><strong>Goal:</strong> Strengthen Linux administration fundamentals that underpin every other lab.</p>\n<h4>Why this lab exists</h4>\n<p>Slow OS fluency pays compounding returns: when web or DB labs fail, you will <strong>know where to look</strong>.</p>\n<h4>Background</h4>\n<p>Treat the shell as a <strong>precision instrument</strong>: read man pages (<code>man systemd.service</code>), use <code>--help</code>, and prefer non-destructive probes (<code>ls</code>, <code>stat</code>, <code>ss -lntp</code>) before changes.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Read the manual first (10 min)</h3>\n<ul>\n<li>Identify <strong>one</strong> man page relevant to today’s topic; read <strong>NAME</strong>, <strong>SYNOPSIS</strong>, and <strong>DESCRIPTION</strong> first paragraphs.</li>\n</ul>\n<h3>Phase 2 — Predict outputs (10 min)</h3>\n<ul>\n<li>Before each command, write your <strong>expected</strong> substring in the output.</li>\n</ul>\n<h3>Phase 3 — Execute + capture</h3>\n<p><strong>df / lsblk / free</strong></p>\n<p>Correlate <code>df -hT</code> mount points with <code>lsblk -f</code> <strong>FSTYPE</strong>.\nRun <code>stress-ng --vm 1 --timeout 5s</code> if installed—or open several tabs—watch <code>free -h</code> sampling.</p>\n<h3>Phase 4 — Correlation (15 min)</h3>\n<ul>\n<li>Tie at least two observations together (e.g. disk free space vs largest mount; process RSS vs available RAM).</li>\n</ul>\n<h3>Phase 5 — Runbook snippet (10 min)</h3>\n<ul>\n<li>Produce <strong>three</strong> bullet “if user reports X, run Y” steps grounded in what you actually ran.</li>\n</ul>\n<h4>Verify</h4>\n<p>You can explain <strong>one diagram</strong> in prose: what changed on disk or network and why it matters.</p>\n<h4>Common mistakes</h4>\n<p>Editing <code>/etc</code> without backup—always <code>sudo cp</code> first.</p>\n<h4>Going further</h4>\n<p>Link this lab to a Tech+ chapter you studied in the main track.</p>\n<hr>\n</div>","labs-kali-t-05":"<p class=\"lh-tg-source\"><strong>Lab T-05 — Process lifecycle</strong> · <span class=\"lh-tg-chapter\">Part 7 — Tech+ / IT fundamentals (on Kali)</span> · Lesson <code>labs-kali-t-05</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab T-05 — Process lifecycle</h3>\n<p><strong>Suggested pace:</strong> 60–95 minutes</p>\n<p><strong>Goal:</strong> Strengthen Linux administration fundamentals that underpin every other lab.</p>\n<h4>Why this lab exists</h4>\n<p>Slow OS fluency pays compounding returns: when web or DB labs fail, you will <strong>know where to look</strong>.</p>\n<h4>Background</h4>\n<p>Treat the shell as a <strong>precision instrument</strong>: read man pages (<code>man systemd.service</code>), use <code>--help</code>, and prefer non-destructive probes (<code>ls</code>, <code>stat</code>, <code>ss -lntp</code>) before changes.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Read the manual first (10 min)</h3>\n<ul>\n<li>Identify <strong>one</strong> man page relevant to today’s topic; read <strong>NAME</strong>, <strong>SYNOPSIS</strong>, and <strong>DESCRIPTION</strong> first paragraphs.</li>\n</ul>\n<h3>Phase 2 — Predict outputs (10 min)</h3>\n<ul>\n<li>Before each command, write your <strong>expected</strong> substring in the output.</li>\n</ul>\n<h3>Phase 3 — Execute + capture</h3>\n<p><strong>jobs / fg / bg / kill</strong></p>\n<p>Start <code>sleep 300 &amp;</code>, list <code>jobs</code>, bring to foreground, suspend with <code>Ctrl+Z</code>, <code>bg</code>, then <code>kill %1</code>.\nDemonstrate <code>nice</code> / <code>renice</code> on a CPU loop script.</p>\n<h3>Phase 4 — Correlation (15 min)</h3>\n<ul>\n<li>Tie at least two observations together (e.g. disk free space vs largest mount; process RSS vs available RAM).</li>\n</ul>\n<h3>Phase 5 — Runbook snippet (10 min)</h3>\n<ul>\n<li>Produce <strong>three</strong> bullet “if user reports X, run Y” steps grounded in what you actually ran.</li>\n</ul>\n<h4>Verify</h4>\n<p>You can explain <strong>one diagram</strong> in prose: what changed on disk or network and why it matters.</p>\n<h4>Common mistakes</h4>\n<p>Editing <code>/etc</code> without backup—always <code>sudo cp</code> first.</p>\n<h4>Going further</h4>\n<p>Link this lab to a Tech+ chapter you studied in the main track.</p>\n<hr>\n</div>","labs-kali-t-06":"<p class=\"lh-tg-source\"><strong>Lab T-06 — Networking I: interfaces and routes</strong> · <span class=\"lh-tg-chapter\">Part 7 — Tech+ / IT fundamentals (on Kali)</span> · Lesson <code>labs-kali-t-06</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab T-06 — Networking I: interfaces and routes</h3>\n<p><strong>Suggested pace:</strong> 60–95 minutes</p>\n<p><strong>Goal:</strong> Strengthen Linux administration fundamentals that underpin every other lab.</p>\n<h4>Why this lab exists</h4>\n<p>Slow OS fluency pays compounding returns: when web or DB labs fail, you will <strong>know where to look</strong>.</p>\n<h4>Background</h4>\n<p>Treat the shell as a <strong>precision instrument</strong>: read man pages (<code>man systemd.service</code>), use <code>--help</code>, and prefer non-destructive probes (<code>ls</code>, <code>stat</code>, <code>ss -lntp</code>) before changes.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Read the manual first (10 min)</h3>\n<ul>\n<li>Identify <strong>one</strong> man page relevant to today’s topic; read <strong>NAME</strong>, <strong>SYNOPSIS</strong>, and <strong>DESCRIPTION</strong> first paragraphs.</li>\n</ul>\n<h3>Phase 2 — Predict outputs (10 min)</h3>\n<ul>\n<li>Before each command, write your <strong>expected</strong> substring in the output.</li>\n</ul>\n<h3>Phase 3 — Execute + capture</h3>\n<p><strong>ip route / ip neigh</strong></p>\n<p><code>ip -br link</code> then <code>ip -4 addr</code>. Draw default gateway, interface, and your VM’s IP.\n<code>ip neigh show</code> — explain stale vs reachable.</p>\n<h3>Phase 4 — Correlation (15 min)</h3>\n<ul>\n<li>Tie at least two observations together (e.g. disk free space vs largest mount; process RSS vs available RAM).</li>\n</ul>\n<h3>Phase 5 — Runbook snippet (10 min)</h3>\n<ul>\n<li>Produce <strong>three</strong> bullet “if user reports X, run Y” steps grounded in what you actually ran.</li>\n</ul>\n<h4>Verify</h4>\n<p>You can explain <strong>one diagram</strong> in prose: what changed on disk or network and why it matters.</p>\n<h4>Common mistakes</h4>\n<p>Editing <code>/etc</code> without backup—always <code>sudo cp</code> first.</p>\n<h4>Going further</h4>\n<p>Link this lab to a Tech+ chapter you studied in the main track.</p>\n<hr>\n</div>","labs-kali-t-07":"<p class=\"lh-tg-source\"><strong>Lab T-07 — Networking II: DNS and DHCP</strong> · <span class=\"lh-tg-chapter\">Part 7 — Tech+ / IT fundamentals (on Kali)</span> · Lesson <code>labs-kali-t-07</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab T-07 — Networking II: DNS and DHCP</h3>\n<p><strong>Suggested pace:</strong> 60–95 minutes</p>\n<p><strong>Goal:</strong> Strengthen Linux administration fundamentals that underpin every other lab.</p>\n<h4>Why this lab exists</h4>\n<p>Slow OS fluency pays compounding returns: when web or DB labs fail, you will <strong>know where to look</strong>.</p>\n<h4>Background</h4>\n<p>Treat the shell as a <strong>precision instrument</strong>: read man pages (<code>man systemd.service</code>), use <code>--help</code>, and prefer non-destructive probes (<code>ls</code>, <code>stat</code>, <code>ss -lntp</code>) before changes.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Read the manual first (10 min)</h3>\n<ul>\n<li>Identify <strong>one</strong> man page relevant to today’s topic; read <strong>NAME</strong>, <strong>SYNOPSIS</strong>, and <strong>DESCRIPTION</strong> first paragraphs.</li>\n</ul>\n<h3>Phase 2 — Predict outputs (10 min)</h3>\n<ul>\n<li>Before each command, write your <strong>expected</strong> substring in the output.</li>\n</ul>\n<h3>Phase 3 — Execute + capture</h3>\n<p><strong>resolv.conf / DHCP</strong></p>\n<p>Cat <code>/etc/resolv.conf</code>. Identify whether <strong>systemd-resolved</strong> stub is in use (<code>resolvectl status</code>).\nRenew lease (method depends on hypervisor); capture before/after <code>ip -4 addr</code> if it changes.</p>\n<h3>Phase 4 — Correlation (15 min)</h3>\n<ul>\n<li>Tie at least two observations together (e.g. disk free space vs largest mount; process RSS vs available RAM).</li>\n</ul>\n<h3>Phase 5 — Runbook snippet (10 min)</h3>\n<ul>\n<li>Produce <strong>three</strong> bullet “if user reports X, run Y” steps grounded in what you actually ran.</li>\n</ul>\n<h4>Verify</h4>\n<p>You can explain <strong>one diagram</strong> in prose: what changed on disk or network and why it matters.</p>\n<h4>Common mistakes</h4>\n<p>Editing <code>/etc</code> without backup—always <code>sudo cp</code> first.</p>\n<h4>Going further</h4>\n<p>Link this lab to a Tech+ chapter you studied in the main track.</p>\n<hr>\n</div>","labs-kali-t-08":"<p class=\"lh-tg-source\"><strong>Lab T-08 — SSH keys and config</strong> · <span class=\"lh-tg-chapter\">Part 7 — Tech+ / IT fundamentals (on Kali)</span> · Lesson <code>labs-kali-t-08</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab T-08 — SSH keys and config</h3>\n<p><strong>Suggested pace:</strong> 60–95 minutes</p>\n<p><strong>Goal:</strong> Strengthen Linux administration fundamentals that underpin every other lab.</p>\n<h4>Why this lab exists</h4>\n<p>Slow OS fluency pays compounding returns: when web or DB labs fail, you will <strong>know where to look</strong>.</p>\n<h4>Background</h4>\n<p>Treat the shell as a <strong>precision instrument</strong>: read man pages (<code>man systemd.service</code>), use <code>--help</code>, and prefer non-destructive probes (<code>ls</code>, <code>stat</code>, <code>ss -lntp</code>) before changes.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Read the manual first (10 min)</h3>\n<ul>\n<li>Identify <strong>one</strong> man page relevant to today’s topic; read <strong>NAME</strong>, <strong>SYNOPSIS</strong>, and <strong>DESCRIPTION</strong> first paragraphs.</li>\n</ul>\n<h3>Phase 2 — Predict outputs (10 min)</h3>\n<ul>\n<li>Before each command, write your <strong>expected</strong> substring in the output.</li>\n</ul>\n<h3>Phase 3 — Execute + capture</h3>\n<p><strong>SSH keys</strong></p>\n<p><code>ssh-keygen -t ed25519 -f ~/.ssh/lh_lab -N &quot;&quot;</code> then configure <code>~/.ssh/config</code> <strong>Host</strong> alias.\n<strong>Do not</strong> disable password auth on a shared machine—only describe the steps for an isolated lab with snapshot.</p>\n<h3>Phase 4 — Correlation (15 min)</h3>\n<ul>\n<li>Tie at least two observations together (e.g. disk free space vs largest mount; process RSS vs available RAM).</li>\n</ul>\n<h3>Phase 5 — Runbook snippet (10 min)</h3>\n<ul>\n<li>Produce <strong>three</strong> bullet “if user reports X, run Y” steps grounded in what you actually ran.</li>\n</ul>\n<h4>Verify</h4>\n<p>You can explain <strong>one diagram</strong> in prose: what changed on disk or network and why it matters.</p>\n<h4>Common mistakes</h4>\n<p>Editing <code>/etc</code> without backup—always <code>sudo cp</code> first.</p>\n<h4>Going further</h4>\n<p>Link this lab to a Tech+ chapter you studied in the main track.</p>\n<hr>\n</div>","labs-kali-t-09":"<p class=\"lh-tg-source\"><strong>Lab T-09 — Cron and timers</strong> · <span class=\"lh-tg-chapter\">Part 7 — Tech+ / IT fundamentals (on Kali)</span> · Lesson <code>labs-kali-t-09</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab T-09 — Cron and timers</h3>\n<p><strong>Suggested pace:</strong> 60–95 minutes</p>\n<p><strong>Goal:</strong> Strengthen Linux administration fundamentals that underpin every other lab.</p>\n<h4>Why this lab exists</h4>\n<p>Slow OS fluency pays compounding returns: when web or DB labs fail, you will <strong>know where to look</strong>.</p>\n<h4>Background</h4>\n<p>Treat the shell as a <strong>precision instrument</strong>: read man pages (<code>man systemd.service</code>), use <code>--help</code>, and prefer non-destructive probes (<code>ls</code>, <code>stat</code>, <code>ss -lntp</code>) before changes.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Read the manual first (10 min)</h3>\n<ul>\n<li>Identify <strong>one</strong> man page relevant to today’s topic; read <strong>NAME</strong>, <strong>SYNOPSIS</strong>, and <strong>DESCRIPTION</strong> first paragraphs.</li>\n</ul>\n<h3>Phase 2 — Predict outputs (10 min)</h3>\n<ul>\n<li>Before each command, write your <strong>expected</strong> substring in the output.</li>\n</ul>\n<h3>Phase 3 — Execute + capture</h3>\n<p><strong>cron</strong></p>\n<p><code>crontab -e</code> entry: append timestamp every 5 minutes to <code>~/labs/techplus/cron.log</code>.\n<code>grep CRON /var/log/syslog</code> or <code>journalctl -u cron</code> lines.</p>\n<h3>Phase 4 — Correlation (15 min)</h3>\n<ul>\n<li>Tie at least two observations together (e.g. disk free space vs largest mount; process RSS vs available RAM).</li>\n</ul>\n<h3>Phase 5 — Runbook snippet (10 min)</h3>\n<ul>\n<li>Produce <strong>three</strong> bullet “if user reports X, run Y” steps grounded in what you actually ran.</li>\n</ul>\n<h4>Verify</h4>\n<p>You can explain <strong>one diagram</strong> in prose: what changed on disk or network and why it matters.</p>\n<h4>Common mistakes</h4>\n<p>Editing <code>/etc</code> without backup—always <code>sudo cp</code> first.</p>\n<h4>Going further</h4>\n<p>Link this lab to a Tech+ chapter you studied in the main track.</p>\n<hr>\n</div>","labs-kali-t-10":"<p class=\"lh-tg-source\"><strong>Lab T-10 — systemd unit for a personal script</strong> · <span class=\"lh-tg-chapter\">Part 7 — Tech+ / IT fundamentals (on Kali)</span> · Lesson <code>labs-kali-t-10</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab T-10 — systemd unit for a personal script</h3>\n<p><strong>Suggested pace:</strong> 60–95 minutes</p>\n<p><strong>Goal:</strong> Strengthen Linux administration fundamentals that underpin every other lab.</p>\n<h4>Why this lab exists</h4>\n<p>Slow OS fluency pays compounding returns: when web or DB labs fail, you will <strong>know where to look</strong>.</p>\n<h4>Background</h4>\n<p>Treat the shell as a <strong>precision instrument</strong>: read man pages (<code>man systemd.service</code>), use <code>--help</code>, and prefer non-destructive probes (<code>ls</code>, <code>stat</code>, <code>ss -lntp</code>) before changes.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Read the manual first (10 min)</h3>\n<ul>\n<li>Identify <strong>one</strong> man page relevant to today’s topic; read <strong>NAME</strong>, <strong>SYNOPSIS</strong>, and <strong>DESCRIPTION</strong> first paragraphs.</li>\n</ul>\n<h3>Phase 2 — Predict outputs (10 min)</h3>\n<ul>\n<li>Before each command, write your <strong>expected</strong> substring in the output.</li>\n</ul>\n<h3>Phase 3 — Execute + capture</h3>\n<p><strong>systemd user or system unit</strong></p>\n<p>Create a <strong>oneshot</strong> or simple service running <code>python3 -m http.server 9999 --bind 127.0.0.1</code>.\n<code>systemctl --user daemon-reload</code> if user unit; else system unit with <code>sudo</code>. Enable, start, status, stop.</p>\n<h3>Phase 4 — Correlation (15 min)</h3>\n<ul>\n<li>Tie at least two observations together (e.g. disk free space vs largest mount; process RSS vs available RAM).</li>\n</ul>\n<h3>Phase 5 — Runbook snippet (10 min)</h3>\n<ul>\n<li>Produce <strong>three</strong> bullet “if user reports X, run Y” steps grounded in what you actually ran.</li>\n</ul>\n<h4>Verify</h4>\n<p>You can explain <strong>one diagram</strong> in prose: what changed on disk or network and why it matters.</p>\n<h4>Common mistakes</h4>\n<p>Editing <code>/etc</code> without backup—always <code>sudo cp</code> first.</p>\n<h4>Going further</h4>\n<p>Link this lab to a Tech+ chapter you studied in the main track.</p>\n<hr>\n</div>","labs-kali-t-11":"<p class=\"lh-tg-source\"><strong>Lab T-11 — Documentation: runbook</strong> · <span class=\"lh-tg-chapter\">Part 7 — Tech+ / IT fundamentals (on Kali)</span> · Lesson <code>labs-kali-t-11</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab T-11 — Documentation: runbook</h3>\n<p><strong>Suggested pace:</strong> 60–95 minutes</p>\n<p><strong>Goal:</strong> Strengthen Linux administration fundamentals that underpin every other lab.</p>\n<h4>Why this lab exists</h4>\n<p>Slow OS fluency pays compounding returns: when web or DB labs fail, you will <strong>know where to look</strong>.</p>\n<h4>Background</h4>\n<p>Treat the shell as a <strong>precision instrument</strong>: read man pages (<code>man systemd.service</code>), use <code>--help</code>, and prefer non-destructive probes (<code>ls</code>, <code>stat</code>, <code>ss -lntp</code>) before changes.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Read the manual first (10 min)</h3>\n<ul>\n<li>Identify <strong>one</strong> man page relevant to today’s topic; read <strong>NAME</strong>, <strong>SYNOPSIS</strong>, and <strong>DESCRIPTION</strong> first paragraphs.</li>\n</ul>\n<h3>Phase 2 — Predict outputs (10 min)</h3>\n<ul>\n<li>Before each command, write your <strong>expected</strong> substring in the output.</li>\n</ul>\n<h3>Phase 3 — Execute + capture</h3>\n<p><strong>Runbook: Apache won’t start</strong></p>\n<p>Write ordered checks: <code>systemctl status</code>, <code>journalctl -u apache2 -n 50</code>, <code>apache2ctl configtest</code>, <code>ss -lntp | grep :80</code>.\nInclude <strong>one</strong> fictional symptom + resolution.</p>\n<h3>Phase 4 — Correlation (15 min)</h3>\n<ul>\n<li>Tie at least two observations together (e.g. disk free space vs largest mount; process RSS vs available RAM).</li>\n</ul>\n<h3>Phase 5 — Runbook snippet (10 min)</h3>\n<ul>\n<li>Produce <strong>three</strong> bullet “if user reports X, run Y” steps grounded in what you actually ran.</li>\n</ul>\n<h4>Verify</h4>\n<p>You can explain <strong>one diagram</strong> in prose: what changed on disk or network and why it matters.</p>\n<h4>Common mistakes</h4>\n<p>Editing <code>/etc</code> without backup—always <code>sudo cp</code> first.</p>\n<h4>Going further</h4>\n<p>Link this lab to a Tech+ chapter you studied in the main track.</p>\n<hr>\n</div>","labs-kali-t-12":"<p class=\"lh-tg-source\"><strong>Lab T-12 — Backup strategy comparison</strong> · <span class=\"lh-tg-chapter\">Part 7 — Tech+ / IT fundamentals (on Kali)</span> · Lesson <code>labs-kali-t-12</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab T-12 — Backup strategy comparison</h3>\n<p><strong>Suggested pace:</strong> 60–95 minutes</p>\n<p><strong>Goal:</strong> Strengthen Linux administration fundamentals that underpin every other lab.</p>\n<h4>Why this lab exists</h4>\n<p>Slow OS fluency pays compounding returns: when web or DB labs fail, you will <strong>know where to look</strong>.</p>\n<h4>Background</h4>\n<p>Treat the shell as a <strong>precision instrument</strong>: read man pages (<code>man systemd.service</code>), use <code>--help</code>, and prefer non-destructive probes (<code>ls</code>, <code>stat</code>, <code>ss -lntp</code>) before changes.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Read the manual first (10 min)</h3>\n<ul>\n<li>Identify <strong>one</strong> man page relevant to today’s topic; read <strong>NAME</strong>, <strong>SYNOPSIS</strong>, and <strong>DESCRIPTION</strong> first paragraphs.</li>\n</ul>\n<h3>Phase 2 — Predict outputs (10 min)</h3>\n<ul>\n<li>Before each command, write your <strong>expected</strong> substring in the output.</li>\n</ul>\n<h3>Phase 3 — Execute + capture</h3>\n<p><strong>tar vs rsync backup</strong></p>\n<p>Time <code>tar czf</code> of <code>~/labs</code> vs <code>rsync -a</code> to <code>/tmp/lh-backup</code>.\nDocument restore commands for both.</p>\n<h3>Phase 4 — Correlation (15 min)</h3>\n<ul>\n<li>Tie at least two observations together (e.g. disk free space vs largest mount; process RSS vs available RAM).</li>\n</ul>\n<h3>Phase 5 — Runbook snippet (10 min)</h3>\n<ul>\n<li>Produce <strong>three</strong> bullet “if user reports X, run Y” steps grounded in what you actually ran.</li>\n</ul>\n<h4>Verify</h4>\n<p>You can explain <strong>one diagram</strong> in prose: what changed on disk or network and why it matters.</p>\n<h4>Common mistakes</h4>\n<p>Editing <code>/etc</code> without backup—always <code>sudo cp</code> first.</p>\n<h4>Going further</h4>\n<p>Link this lab to a Tech+ chapter you studied in the main track.</p>\n<hr>\n</div>","labs-kali-t-13":"<p class=\"lh-tg-source\"><strong>Lab T-13 — Performance baseline</strong> · <span class=\"lh-tg-chapter\">Part 7 — Tech+ / IT fundamentals (on Kali)</span> · Lesson <code>labs-kali-t-13</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab T-13 — Performance baseline</h3>\n<p><strong>Suggested pace:</strong> 60–95 minutes</p>\n<p><strong>Goal:</strong> Strengthen Linux administration fundamentals that underpin every other lab.</p>\n<h4>Why this lab exists</h4>\n<p>Slow OS fluency pays compounding returns: when web or DB labs fail, you will <strong>know where to look</strong>.</p>\n<h4>Background</h4>\n<p>Treat the shell as a <strong>precision instrument</strong>: read man pages (<code>man systemd.service</code>), use <code>--help</code>, and prefer non-destructive probes (<code>ls</code>, <code>stat</code>, <code>ss -lntp</code>) before changes.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Read the manual first (10 min)</h3>\n<ul>\n<li>Identify <strong>one</strong> man page relevant to today’s topic; read <strong>NAME</strong>, <strong>SYNOPSIS</strong>, and <strong>DESCRIPTION</strong> first paragraphs.</li>\n</ul>\n<h3>Phase 2 — Predict outputs (10 min)</h3>\n<ul>\n<li>Before each command, write your <strong>expected</strong> substring in the output.</li>\n</ul>\n<h3>Phase 3 — Execute + capture</h3>\n<p><strong>ab / hey baseline</strong></p>\n<p>If <code>ab</code> exists: <code>ab -n 200 -c 10 http://127.0.0.1/</code>. Capture RPS and failed requests.\nRelate results to VM CPU count (<code>nproc</code>).</p>\n<h3>Phase 4 — Correlation (15 min)</h3>\n<ul>\n<li>Tie at least two observations together (e.g. disk free space vs largest mount; process RSS vs available RAM).</li>\n</ul>\n<h3>Phase 5 — Runbook snippet (10 min)</h3>\n<ul>\n<li>Produce <strong>three</strong> bullet “if user reports X, run Y” steps grounded in what you actually ran.</li>\n</ul>\n<h4>Verify</h4>\n<p>You can explain <strong>one diagram</strong> in prose: what changed on disk or network and why it matters.</p>\n<h4>Common mistakes</h4>\n<p>Editing <code>/etc</code> without backup—always <code>sudo cp</code> first.</p>\n<h4>Going further</h4>\n<p>Link this lab to a Tech+ chapter you studied in the main track.</p>\n<hr>\n</div>","labs-kali-t-14":"<p class=\"lh-tg-source\"><strong>Lab T-14 — Hypervisor literacy</strong> · <span class=\"lh-tg-chapter\">Part 7 — Tech+ / IT fundamentals (on Kali)</span> · Lesson <code>labs-kali-t-14</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h3>Lab T-14 — Hypervisor literacy</h3>\n<p><strong>Suggested pace:</strong> 60–95 minutes</p>\n<p><strong>Goal:</strong> Strengthen Linux administration fundamentals that underpin every other lab.</p>\n<h4>Why this lab exists</h4>\n<p>Slow OS fluency pays compounding returns: when web or DB labs fail, you will <strong>know where to look</strong>.</p>\n<h4>Background</h4>\n<p>Treat the shell as a <strong>precision instrument</strong>: read man pages (<code>man systemd.service</code>), use <code>--help</code>, and prefer non-destructive probes (<code>ls</code>, <code>stat</code>, <code>ss -lntp</code>) before changes.</p>\n<h4>Procedure (go slowly)</h4>\n<h3>Phase 1 — Read the manual first (10 min)</h3>\n<ul>\n<li>Identify <strong>one</strong> man page relevant to today’s topic; read <strong>NAME</strong>, <strong>SYNOPSIS</strong>, and <strong>DESCRIPTION</strong> first paragraphs.</li>\n</ul>\n<h3>Phase 2 — Predict outputs (10 min)</h3>\n<ul>\n<li>Before each command, write your <strong>expected</strong> substring in the output.</li>\n</ul>\n<h3>Phase 3 — Execute + capture</h3>\n<p><strong>Hypervisor NIC modes</strong></p>\n<p>Diagram NAT vs bridged vs host-only for your setup. Which IP does the host use to reach the VM web server?\nList one troubleshooting step if host cannot ping VM gateway.</p>\n<h3>Phase 4 — Correlation (15 min)</h3>\n<ul>\n<li>Tie at least two observations together (e.g. disk free space vs largest mount; process RSS vs available RAM).</li>\n</ul>\n<h3>Phase 5 — Runbook snippet (10 min)</h3>\n<ul>\n<li>Produce <strong>three</strong> bullet “if user reports X, run Y” steps grounded in what you actually ran.</li>\n</ul>\n<h4>Verify</h4>\n<p>You can explain <strong>one diagram</strong> in prose: what changed on disk or network and why it matters.</p>\n<h4>Common mistakes</h4>\n<p>Editing <code>/etc</code> without backup—always <code>sudo cp</code> first.</p>\n<h4>Going further</h4>\n<p>Link this lab to a Tech+ chapter you studied in the main track.</p>\n<hr>\n</div>","labs-kali-appendix-ref":"<p class=\"lh-tg-source\"><strong>Appendix A — Apache quick reference (Kali)</strong> · <span class=\"lh-tg-chapter\">Reference — command sheets</span> · Lesson <code>labs-kali-appendix-ref</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h2>Appendix A — Apache quick reference (Kali)</h2>\n<p><strong>Pace:</strong> 30–45 minutes to walk through each command with <code>man</code> or <code>--help</code>.</p>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo systemctl status apache2\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Shows <strong>state</strong>, recent log lines, and whether the unit is enabled.</li>\n<li><strong>Why we use it here:</strong> First triage when “the website is down.”</li>\n</ul>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo systemctl restart apache2\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Hard <strong>restart</strong> of the daemon (drops connections).</li>\n<li><strong>Why we use it here:</strong> Use when module loads require full restart vs <code>reload</code>.</li>\n</ul>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo apache2ctl -S\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> Dumps <strong>virtual host configuration</strong> mapping.</li>\n<li><strong>Why we use it here:</strong> Reveals duplicate vhosts and unexpected <code>ServerName</code> matches.</li>\n</ul>\n<p><strong>Command:</strong></p>\n<pre><code class=\"language-bash\">sudo tail -f /var/log/apache2/access.log\n</code></pre>\n<ul>\n<li><strong>What it does:</strong> <code>tail -f</code> streams new lines as requests arrive.</li>\n<li><strong>Why we use it here:</strong> Correlate browser clicks with server-side evidence.</li>\n</ul>\n<hr>\n</div>","labs-kali-appendix-pace-capstone":"<p class=\"lh-tg-source\"><strong>Pacing, capstone, and wrap-up</strong> · <span class=\"lh-tg-chapter\">Reference — pacing &amp; capstone</span> · Lesson <code>labs-kali-appendix-pace-capstone</code> · Source: <code>docs/KALI_HANDS_ON_LAB_CURRICULUM.md</code></p><hr class=\"teach-hr lh-md-start\"/><div class=\"lh-tg-root tech-prose lh-tg-markdown lh-chapter-markdown\"><h2>Pacing, capstone, and wrap-up</h2>\n<h2>Appendix B — MariaDB quick reference (Kali)</h2>\n<pre><code class=\"language-bash\">sudo mariadb\nSHOW DATABASES;\nUSE soc_training;\nSHOW TABLES;\n</code></pre>\n<p><strong>What this block does:</strong> connects as admin, lists databases, selects a schema, lists tables—your “am I in the right place?” ritual.</p>\n<h2>Appendix C — Suggested <strong>slow</strong> weekly pace</h2>\n<p>Treat each <strong>numbered lab</strong> as roughly <strong>one session</strong> (some may stretch). Eight months is safer than eight weeks if you are doing all command explorations and notes.</p>\n<table>\n<thead>\n<tr>\n<th align=\"right\">Month</th>\n<th>Focus</th>\n</tr>\n</thead>\n<tbody><tr>\n<td align=\"right\">1</td>\n<td>Global setup + HTML H-01–H-06</td>\n</tr>\n<tr>\n<td align=\"right\">2</td>\n<td>HTML H-07–H-12 + CSS C-01–C-04</td>\n</tr>\n<tr>\n<td align=\"right\">3</td>\n<td>CSS C-05–C-12</td>\n</tr>\n<tr>\n<td align=\"right\">4</td>\n<td>JavaScript J-01–J-06</td>\n</tr>\n<tr>\n<td align=\"right\">5</td>\n<td>JavaScript J-07–J-12</td>\n</tr>\n<tr>\n<td align=\"right\">6</td>\n<td>Python P-01–P-06</td>\n</tr>\n<tr>\n<td align=\"right\">7</td>\n<td>Python P-07–P-12</td>\n</tr>\n<tr>\n<td align=\"right\">8</td>\n<td>SQL S-01–S-06</td>\n</tr>\n<tr>\n<td align=\"right\">9</td>\n<td>SQL S-07–S-12</td>\n</tr>\n<tr>\n<td align=\"right\">10</td>\n<td>Security+ SEC-01–SEC-07</td>\n</tr>\n<tr>\n<td align=\"right\">11</td>\n<td>Security+ SEC-08–SEC-14</td>\n</tr>\n<tr>\n<td align=\"right\">12</td>\n<td>Tech+ T-01–T-07</td>\n</tr>\n<tr>\n<td align=\"right\">13</td>\n<td>Tech+ T-08–T-14</td>\n</tr>\n<tr>\n<td align=\"right\">14</td>\n<td>Capstone + review notebooks</td>\n</tr>\n</tbody></table>\n<h2>Capstone (optional, combines topics)</h2>\n<p><strong>Pace:</strong> 8–20 hours across multiple days.</p>\n<p>Build <strong>“SOC Training Portal”</strong> on Kali:</p>\n<ol>\n<li>Apache2 hosts HTML/CSS/JS under <code>/var/www/html/soc-portal/</code>.</li>\n<li>MariaDB stores <code>courses</code>, <code>labs_completed</code>, <code>notes</code>.</li>\n<li>Python <code>import_labs.py</code> reads JSON metadata and <strong>parameterized</strong> SQL upserts rows.</li>\n<li>Write a <strong>hardening memo</strong> (headers, firewall, SSH policy) and a <strong>Tech+ runbook</strong> for operations.</li>\n</ol>\n<hr>\n<p><strong>End of curriculum.</strong> Extend labs with your own fictional org stories—narrative context makes the same commands stick longer.</p>\n</div>"};
  window.LEARN_HUB_KALI_MD = window.LEARN_HUB_KALI_MD || {};
  for (var k in C) {
    if (Object.prototype.hasOwnProperty.call(C, k)) window.LEARN_HUB_KALI_MD[k] = C[k];
  }
})();
